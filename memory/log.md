# 决策记录

## 🧭 项目执行规则（长期有效）

1. **先审视、再执行**：用户提出一个想法/优化点后，先快速审视其合理性、可行性与潜在风险（是否与现有设计/逻辑冲突、是否影响可读性或性能等）。如果觉得不合理、有问题或需要取舍，**先提出来与用户探讨，达成一致后再动手**，不直接照单全收。
2. 每轮改动后更新本文件（惯例）。


- [ ] **（待办 · 用户要求记录）租一台服务器（VPS）部署 czsc 缠论服务，供线上平台调用，实现生产级精确缠论**
  - **为什么会有这条待办**：缠论大师最初是"简化版"自研算法（只移植了分型/笔/中枢/背驰的简化规则，没做线段/特征序列/严格笔定义/中枢合并）。2026-08-12 用同一份真实K线把"简化版"和原项目 czsc 对比，发现**分型可对齐，但末笔方向、最近中枢、笔/中枢数量会不一致**（比如茅台末笔方向：原项目=下降、简化版=上升；旭创最近中枢：原项目 815-918、简化版 554-631）——对"缠论大师"输出来说是实质性错误（会把方向说反、中枢说错）。
  - **临时的解决办法**：不在平台里塞 czsc（Vercel serverless 跑不了 Python+Rust），而是把原项目装在本机，做成一个 HTTP 服务（`/Users/zxiansheng/Desktop/skill仓库/czsc-service`，端口 127.0.0.1:8701，数据源三级回退 东财→腾讯→新浪），平台以后通过"能力包适配器"调用它 = 精确缠论。本地已验证输出与原项目逐项一致。
  - **为什么不能一直用本地**：本机服务只有开发/自己能用（Vercel 线上服务器够不到你家电脑）。要让"缠中说禅"对**线上用户**提供精确缠论，就必须有一台 24 小时在线的服务器来跑这个服务。
  - **触发条件**：当决定把缠论作为主打能力、要对线上用户提供生产级精确度时执行。
## 📌 当前待办（2026-08-08 更新 · 最近一次会话状态）

> ⚠️ **最重要：最新代码已本地提交（commit 96f4934），但还没推送到 GitHub，也还没部署到 Vercel 线上！**

- [ ] **推送 + 部署**：等用户能翻墙后执行 `git push`，Vercel 会自动部署（部署前先确认未提交的用户改动）
- [ ] **线上验证**：部署后在 yieldglide.com 用「英伟达」「腾讯控股」提问，确认美股/港股财务注入（Yahoo 路径）生效
- [ ] **本地完整对话前提**：`.env.local` 已配置 `DEEPSEEK_API_KEY`（本地可跑）
- [ ] **已提交**：masters.js、public/avatars/* 均已入库；`memory/project.md` 是旧项目（YieldGlide）残留，未提交
- [ ] （旧）Cloudflare 删除「非 www → www」跳转规则，只保留 Vercel 的「www → 非 www」，防止重定向死循环
- [ ] （可选）扩充公司词典：`src/app/api/chat/marketData.js` 的 `COMMON_SYMBOLS` 加一行即可
- [ ] 第二批优化（移动端/大师页/海报/持久化/拆分）已本地完成并构建验证，待部署后线上复核（含用户浏览器确认持久化）

---


## 2026-03-04

### 百度 SEO 配置
**背景**: 希望网站能被百度收录，让更多中文用户找到

**已完成**:
- ✅ 网站已有 sitemap.js 和 robots.js 配置
- ✅ sitemap.xml 可正常访问: https://www.yieldglide.com/sitemap.xml
- ✅ robots.txt 已包含 Sitemap 声明
- ✅ 百度站长平台完成站点验证

**待处理**:
- ⏳ 百度站长平台 sitemap 提交（输入框置灰，可能需要等待 1-24 小时权限开放）
- ⏳ 可选：使用"手动提交"方式先提交首页
- ⏳ 等待百度抓取（预计 1-3 天开始抓取，1-2 周开始收录）

**决策**:
- 采用被动等待策略，不强制手动提交
- sitemap 当前只包含首页，后续可扩展到更多页面
- 通过百度站长平台监控"抓取频次"和"索引量"

**技术细节**:
- 服务器位置: Vercel（国外）
- CDN: Cloudflare（全球加速）
- DNS: 指向 Cloudflare
- 影响: 国外服务器 + 无 ICP 备案，收录速度会比国内慢，但可以收录

---

### 创建记忆系统
**背景**: 需要一个持久化的记忆系统，在 AI 失忆时能快速恢复上下文

**决策**:
- 创建 `/memory` 文件夹存放所有记忆文件
- 分为三个文件：
  - `project.md`: 项目不变的基本信息
  - `architecture.md`: 当前系统架构
  - `log.md`: 按时间记录的决策日志（本文件）

**原则**:
- 记录关键决策和背景
- 记录技术选型原因
- 记录待办事项和进度
- 保持简洁，只记录重要信息

**重要规则**:
- ⚠️ **每次执行完动作后，必须更新 memory 文件**
- 将新的决策、修改、配置记录到 `log.md`
- 如果架构有变化，更新 `architecture.md`
- 如果项目基本信息有变化，更新 `project.md`

---

## 2026-03-10

### Google SEO：修复规范网址冲突（www vs 非 www）
**背景**: Google Search Console 提示 `https://www.yieldglide.com/` 为「备用页面（有适当的规范标记）」，Google 选择的规范网址是 `https://www.yieldglide.com/`（带 www），与页面声明的 `https://yieldglide.com/`（不带 www）不一致，导致首页无法被正常收录。

**决策**: 统一使用不带 www 的版本 `https://yieldglide.com/` 作为规范网址（方案 A）。

**实现**:
- 创建 `/vercel.json`，添加 301 重定向规则：所有 `https://www.yieldglide.com/:path*` 的请求永久跳转到 `https://yieldglide.com/:path*`
- `layout.js` 中 `canonical: '/'` + `metadataBase: https://yieldglide.com` 已正确配置，无需修改
- 提交并推送到 GitHub，Vercel 自动部署生效

**影响**:
- `https://www.yieldglide.com/` 现在会 301 跳转到 `https://yieldglide.com/`
- Google 重新抓取后会统一认定 `https://yieldglide.com/` 为主版本
- GSC 中「备用页面」「会自动重定向」提示预计数天至数周内消失

**待办**:
- ⏳ 等待 Google 重新抓取（预计 1-2 周）
- ⏳ 在 GSC「网址检查」对 `https://yieldglide.com/` 点击「请求编入索引」（已操作）
- ⏳ 观察 GSC 收录数量是否恢复正常

---

### 部署后：大师头像丢失 + 重定向死循环（ERR_TOO_MANY_REDIRECTS）
**背景**:
- 部署后部分大师真实头像丢失（图片加载失败）。
- 浏览器 Console 报错：`Failed to load resource: net::ERR_TOO_MANY_REDIRECTS`，涉及 `/avatars/*.jpg` 与 `/favicon.ico`。

**决策**:
- 头像资源统一使用 ASCII 路径 `/avatars/*`，避免中文路径 `/头像/*` 在部分部署/CDN 环境兼容性问题。
- 站点规范域名继续统一为 **非 www**：`https://yieldglide.com/`。

**实现**:
- 代码侧（已推送到 GitHub）：
  - `src/data/masters.js`：预置大师头像从 `/头像/*.jpg` 改为 `/avatars/*.jpg`。
  - `scripts/sync-avatars.mjs`：构建前把 `public/头像` 同步复制到 `public/avatars`。
  - `package.json`：新增 `prebuild` 执行同步脚本。
- 排查发现线上重定向互相打架导致死循环：
  - Cloudflare：将 `yieldglide.com/*` 重定向到 `www.yieldglide.com/*`（非 www → www）。
  - Vercel（`vercel.json`）：将 `www.yieldglide.com/*` 重定向到 `yieldglide.com/*`（www → 非 www）。
  - 结果：两边互相 307/308，导致资源加载失败，页面可能出现“纯黑/无内容”的假象。

**影响**:
- 只要重定向死循环存在，头像/favicon/静态资源都会加载失败，用户体验和 SEO 都会受影响。

**待办**:
- ✅ 选择 **非 www** 为主域名的前提下：到 Cloudflare 关闭/删除 “非 www → www” 的跳转规则，只保留 Vercel 的 “www → 非 www”。
- （可选）Cloudflare 清缓存（Purge）后再验证 `https://yieldglide.com/favicon.ico` 与 `https://yieldglide.com/avatars/buffett.jpg` 返回 200 且不再跳转。

---

### Todo App：新增“归档已完成任务”能力（后端 Spring Boot + 前端 React）
**背景**: 需要把已完成的 Todo 归档隐藏在主列表中，并提供一个“Archived”视图查看归档内容；同时提供“一键归档所有已完成”的操作。

**决策**:
- 在 Todo 模型中新增 `isArchived` 字段作为归档标记。
- 后端提供按归档状态筛选的查询能力，并提供批量归档已完成 Todo 的专用接口。
- 前端增加“Archive Completed”按钮与“Show Archived / Show Active”切换，以两种视图展示数据。

**实现**:
- 后端（Spring Boot / JPA）：
  - `Todo` 实体：新增 `isArchived` 字段及 getter/setter。
  - `TodoRepository`：新增 `findByCompletedTrue()`、`findByIsArchivedTrue()`、`findByIsArchivedFalse()`。
  - `TodoService`：
    - `getAllTodos(showArchived)`：按归档状态返回 active/archived 列表。
    - `updateTodo`：允许更新 `isArchived`。
    - `archiveCompletedTodos()`：把所有 completed=true 的记录批量设置 `isArchived=true` 并保存。
  - `TodoController`：
    - `GET /api/todos?showArchived=false|true`：按参数返回 active/archived。
    - `POST /api/todos/archive-completed`：触发批量归档。
  - `application.properties`：设置 `spring.jpa.hibernate.ddl-auto=update` 以便新增列自动更新到数据库。
- 前端（React）：
  - `client/src/App.js`：新增 `showArchived` 状态；请求时携带 `showArchived`；增加两个按钮（归档已完成、切换视图）。
  - 依赖：安装 `axios` 用于 API 调用。

**影响**:
- Active 视图默认不再展示归档 Todo；Archived 视图可查看历史归档。
- 批量归档避免前端逐条更新，接口更清晰。

**验收**:
- 页面可打开无报错；归档/切换视图功能已验证可用（用户反馈）。

---

### 站点 UI：在主题切换旁新增“我的二维码”
**背景**: 需要在页面显眼位置提供个人二维码入口，方便用户扫码（例如公众号/社群/个人号）。

**决策**: 将二维码入口放在首页右上角，与深浅色主题切换并列，减少打扰但容易找到。

**实现**:
- `src/app/page.js`：
  - header 右上角新增二维码按钮（与主题按钮同一操作区）。
  - 点击弹出二维码浮层，支持按 `Esc` 或点击空白处关闭。
  - 二维码图片来源约定：
    - 默认读取 `public/my-qr.png`
    - 或通过环境变量 `NEXT_PUBLIC_QR_CODE_URL` 指向外部二维码图片 URL
  - 若图片缺失/加载失败，会显示替换提示。

**影响**: UI 增加一个轻量入口，不改变现有业务逻辑；可在不改代码的情况下替换二维码（放文件或改环境变量）。

**待办**:
- ~~把二维码图片文件放入 `public/my-qr.png`（推荐），或设置 `NEXT_PUBLIC_QR_CODE_URL`。~~ ✅ 已完成（见下条）

---

### 二维码图片路径修复：.png → .jpg
**背景**: 用户将二维码图片 `my-qr.jpg` 放入 `public/` 目录，但代码默认读取路径为 `/my-qr.png`，导致图片加载失败。

**决策**: 将代码中硬编码的默认路径从 `.png` 改为 `.jpg`，与实际文件名保持一致。

**实现**:
- `src/app/page.js`：
  - `qrSrc` 默认值：`'/my-qr.png'` → `'/my-qr.jpg'`
  - 兜底提示文字：`public/my-qr.png` → `public/my-qr.jpg`
- `public/my-qr.jpg` 文件已由用户放入 `public/` 目录。

**影响**: 点击右上角 `⌁` 按钮，二维码图片可正常加载显示。

**待办**: 无。

---

### UI：二维码按钮图标替换为微信图标
**背景**: 右上角二维码入口按钮原来用的是 `⌁` 符号，不够直观，用户希望换成微信图标，让用户一眼看出这是微信二维码。

**决策**: 将按钮图标替换为微信官方风格的 SVG 图标（内联 SVG，无需引入额外图标库）。

**实现**:
- `src/app/page.js`：
  - 按钮内容：`⌁` → 微信 SVG 图标（18×18，`fill="currentColor"`，自动适配深浅色主题）
  - 按钮 `title`：`我的二维码` → `微信二维码`
  - 按钮 `aria-label`：`打开二维码` → `打开微信二维码`
  - 弹窗标题：`我的二维码` → `微信二维码`

**影响**: 视觉上更直观，用户能立即识别这是微信相关入口；图标颜色跟随主题 CSS 变量自动切换，无额外样式负担。

**待办**: 无。

---

## 2026-08-08

### 实时数据优化：行情 + 财务快照注入（东方财富 + Yahoo 双源）
**背景**: 大师发言中的具体数字（股价、营收、净利、增速等）主要来自 AI 训练记忆，偏旧；原有 quoteContext.js 只拉基础行情（价格/PE/市值），财务数据缺失，且拼接文本存在 `\n` 字面量 bug。

**决策**: 新建统一市场数据层，辩论前把「最新行情 + 财务快照」注入 prompt，让 AI 优先引用真实最新数据。

**实现**:
- 新增 `src/app/api/chat/marketData.js`：
  - 东方财富公开接口（国内可直连、免费无 Key）：搜索解析公司名/代码（A股/港股/美股）、实时行情（价格/涨跌/PE/PB/市值，按 f59 小数位换算）、A股 F10 财务主要指标（最新季报+年报：营收/净利/同比增速/毛利率/净利率/ROE/EPS/每股现金流）。
  - Yahoo Finance（yahoo-finance2）兜底与补充：美股/港股行情字段（PE、52 周区间、股息率）与财务/分析师评级（quoteSummary）。本机（国内网络）Yahoo 被墙会自动超时降级；生产（Vercel 美国）正常。
  - 内置约 90 个常见公司中文名/别名词典（茅台、比亚迪、腾讯、苹果、英伟达等），支持英文代码（NVDA）与 6 位 A 股代码。
  - TTL 缓存：行情 3 分钟、财务 30 分钟、失败负缓存 1 分钟，避免打爆接口。
- 重写 `src/app/api/chat/quoteContext.js`：组装带「数据截至日期」的快照文本（真实换行，修复原 `\n` bug）。
- `src/app/api/chat/route.js` 无需改动（继续调用 getQuoteContext 注入）。

**影响**:
- A股：大师讨论会引用最新季报/年报真实财务数字与实时股价、PE/PB。
- 港股/美股：行情来自东财（国内可用），财务/评级来自 Yahoo（生产环境生效）。
- 问题里没有可解析的公司时返回空，不影响对话。

**待办**:
- ⏳ 推送到 GitHub / Vercel 部署（等用户能翻墙；当前仅本地提交 96f4934）
- ⏳ 部署后在线上验证美股/港股财务注入（Yahoo 路径，国内本机无法直接验证）
- ⏳ 本地完整对话需在 .env.local 配置 DEEPSEEK_API_KEY
- ⏳ 可继续扩充公司词典（在 marketData.js 的 COMMON_SYMBOLS 中加一行即可）

---

## 2026-08-08（第二批）

### 优化批次：移动端适配 / OG分享图 / 持久化 / 代码拆分 / 大师详情页 / 分享海报
**背景**: 用户 review 后选定以下优化：移动端适配(#1)、OG 分享图(#3)、主题首帧闪烁(#5)、讨论持久化(#6)、死代码清理(#7)、page.js 拆分(#8)、大师详情页(#9)、辩论分享海报(#10)。首轮辩论串行请求(#2)是刻意设计（群聊真实感），不改。

**实现**:
- #1 移动端适配：`page.css` 新增 @media 断点（≤900px 单栏、侧边栏横向滚动选人；≤520px 收紧留白）；修复 Grid 内容撑破页宽问题（`minmax(0,1fr)` + `min-width:0`）。已用浏览器 375px 宽度实测：无横向溢出。
- #3 OG 分享图：用 8 位真实大师头像生成 `public/og-image.png`（1200×630，深色学院风 + 金色点缀）。
- #5 主题闪烁：`layout.js` `<head>` 加内联脚本，首帧即按 localStorage 设置 `data-theme`；page.js 主题初始值改为从 localStorage 读取。
- #6 讨论持久化：localStorage 防抖保存（选中大师/问题/整场讨论），刷新可恢复；新增「清除本场记录」按钮。
- #7 死代码：删除 `src/app/head.js`（App Router 不渲染，内含假评分 JSON-LD）、未用变量、重复的 light body::before 样式、custom-form 等未用 CSS。
- #8 拆分：内联 `<style jsx>` → `src/app/page.css`；prompt 构建 → `src/lib/prompts.js`；Card/MasterAvatar/MiniBtn → `src/components/ui.js`；MasterProfileModal → 独立文件。page.js 从 1029 行降到约 800 行，首屏 JS 115kB→109kB。
- #9 大师详情页：新增 `/masters`（全名单列表）+ `/masters/[id]`（32 位大师生平/理论/金句，Person JSON-LD、独立 metadata）；sitemap 扩展 33 个 URL；资料弹窗加「查看大师主页」；详情页「参与辩论」→ `/?masters=id` 自动选中该大师。
- #10 分享海报：`src/lib/poster.js` canvas 生成 1080×1920 竖版海报（问题/开场/发言气泡/裁决/页脚），讨论区新增「分享海报」按钮 + 弹窗（保存图片/关闭）。

**影响**:
- 手机端体验可用（原为固定双栏，小屏会被挤爆）。
- 社交分享有预览图；微信传播有海报入口。
- 刷新不丢讨论；代码结构大幅改善，后续维护/扩展更容易。
- SEO：新增 33 个可收录页面。

**待办**:
- ⏳ 部署后线上复核：美股/港股财务注入（Yahoo）、海报、大师页、移动端。
- 本地无法直接验证 localStorage 持久化（应用内浏览器禁用了存储），需用户在自己浏览器确认。

---

## 2026-08-08（第三批）

### 修复"老数据"：美股/港股财务数据本地接入 + 提示词防编造
**背景**: 用户反馈辩论中引用的是旧数据。排查发现：A股快照完整（模型正确引用 2026 一季报），但**美股/港股快照只有价格/市值/PB**——财务数据依赖 Yahoo，国内被墙，模型只能凭训练记忆编数字 → 老数据。

**实现**:
- `marketData.js` 新增东财 datacenter 财务接口（**国内可直连**）：
  - 美股：`RPT_USF10_INFO_ORGPROFILE` 解析代码 → `RPT_USF10_FN_GMAININDICATOR` 主要指标（营收/净利/同比/毛利率/净利率/ROE/EPS/资产负债率，最新季+年报）
  - 港股：`RPT_HKF10_FN_MAININDICATOR`（腾讯 2026 一季报等全套指标）
  - 美股仍保留 Yahoo 补充分析师目标价/评级（生产环境生效，国内自动忽略）
- `quoteContext.js`：财务格式化统一支持 A股/港股/美股；金额带币种；最新财报与年报重复时去重；**提示词强化**：快照里没有的精确数字一律禁止引用，只能用「大约/约/可能」等模糊表述，严禁用记忆旧数字冒充最新。
- `prompts.js`：逐条发言/追问提示词同步改为「优先引用注入快照」。

**影响**:
- 美股/港股辩论现在引用最新季度真实财务数据。实测：英伟达 2026 Q1 营收 816.1亿 USD（+85.2%）、净利 583.2亿（+210.6%）；腾讯 2026 一季报营收 1964.6亿 HKD（+9.1%）。
- 实测模型发言已引用快照（"毛利率 74.93%、ROE 33.06%"），不再编旧数据。

**待办**:
- ⏳ 部署后线上复核（美股分析师评级为 Yahoo 路径）。

---

## 2026-08-08（第四批）

### 修复"中钢国际等词典外公司拿不到最新数据"：通用公司名自动识别
**背景**: 用户问"中钢国际还值得持有吗"，大师们一直引用 2025 年数据。排查：中钢国际不在内置词典，且原解析只认词典/代码/美股 ticker，**快照为空** → 模型只能用训练记忆（旧数据）。A 股数据源本身没问题（东财能拿到 000928 的 2026 一季报）。

**实现** (`marketData.js`)：
- 通用公司名识别，两层：
  1. 后缀启发式（股份/集团/国际/科技/银行/控股 等 ~90 个后缀，抠出"XX国际"这类名字）
  2. 分段提取 + 循环剥离问法/噪音词（和/与/或 分段；"值得持有吗""还能拿吗""重仓""浮亏"等循环剥净），覆盖无后缀公司名（中国中车、京沪高铁、东方雨虹）
- 识别出的名字走东财搜索解析（A股优先），结果缓存 24h。
- 修复"剥词只剥一层"的 bug（"中钢国际还值得持有吗"剥完剩"中钢国际还"，脏候选挤掉干净候选）。

**影响**:
- 现在任意中文公司名（无需预置词典）都能自动接上最新行情+财务。实测：中钢国际 000928 现价 5.07（用户 6.6 重仓浮亏 ~23%），2026 一季报营收 28.8 亿（-18.2%）、净利 1.8 亿（-20.3%）；中国中车、京沪高铁、东方雨虹同样识别成功。
- 实测辩论：模型引用"2026年一季报营收同比降18%、净利降20%、ROE 2.18%、PE 9.8倍"。

**待办**:
- 部分公司 2026 中报/半年报尚未披露或东财未更新时，快照会显示最新已披露报告期（如 2026 一季报），属正常。

---

## 2026-08-08（第五批）

### 修复：长问句里公司名识别不完整 + DeepSeek 偶发"fetch failed"
**背景**: 用户问"解读一下双环科技这家公司"报 `fetch failed`。排查：1) fetch failed 是 DeepSeek 请求超时（17s）的瞬时网络问题，重试即成功；2) 双环科技在长问句里没被识别——后缀扫描把"解读一下"当公司名一部分（"解读一下双环科技"），且分段剥离前因超过 10 字被跳过。

**实现**:
- `marketData.js`：
  - 扩充问法词表（解读一下/能不能给我讲讲/这只股票/这家公司 等前导+尾部噪音词），循环剥离
  - 分段预处理上限放宽到 20 字（剥完再校验 2-8 字）
  - 去掉"子串去重保留最长"（防止脏候选挤掉干净候选），改为解析后按 secid 去重
- `route.js`：DeepSeek 请求网络异常时重试一次；`fetch` 类错误返回友好提示"连接 AI 服务失败（网络异常），请稍后重试"而非裸报 `fetch failed`。

**影响**:
- 实测：双环科技（000707）两种问法均识别成功，快照含 2026 一季报（营收 5.2 亿 -9.8%、净利 -2291 万）；模型引用"一季度营收5.2亿、净利亏2291万、毛利率8.78%"。
- 中钢国际/中国中车/京沪高铁/东方雨虹等回归正常；"今天天气""PE和PB"仍正确返回空。

---

## 2026-08-08（第六批）

### AI 信息层梳理：辩论前用 LLM 提取公司名 + 无公司名友好提示
**背景**: 用户指出启发式公司名识别仍脆弱（如"解读一下双环科技这家公司"会失败），并建议正式辩论前先做一次"信息层梳理"；同时担心"没预置名字就查不到"。

**实现**:
- `marketData.js`：新增 `extractCompaniesViaLLM()`——辩论前先用 DeepSeek 提取问题里的公司/股票（中文名/代码/英文名），再逐个走东财搜索核实（A股优先），结果按 query 缓存 1 小时；LLM 失败或无结果时回落到启发式（后缀+分段+剥问法词）。
- `quoteContext.js`：主入口改为 `getQuoteContextInfo()` 返回 `{ snapshot, notice }`；识别不到公司且问题含股票关键词时，返回提示"请补充公司名称或代码"；`getQuoteContext()` 保留兼容。
- 新增 `POST /api/context`：前端发起辩论前调用一次，生成快照 + 提示。
- `page.js`：`go()` 先调 /api/context 做信息层梳理，快照存入 ref 随每条请求带给 /api/chat（避免每条都重新解析）；追问也合并新公司的快照；识别不到公司时在输入区显示提示条。
- `route.js`：优先使用前端传入的 `snapshot`，否则按 query 现拉（向后兼容）。

**影响**:
- 任意公司名（含"解读一下XX""能不能给我讲讲XX这只股票"等绕口问法）都能稳定识别。
- 问题里确实没提公司名时，界面会明确提示补充名称/代码，而不是让大师凭记忆瞎编。
- 快照只在辩论开始前生成一次，后续请求全部走缓存，速度不受影响。

**待办**:
- 追问如提到全新公司，会合并该公司的快照（已实现）；若追问涉及复杂指代（"那家做纯碱的公司"），仍建议用户带名称。

---

## 2026-08-08（第七批）

### 新增前瞻数据：A股业绩预告 + 机构一致预期（未来盈利预测）
**背景**: 用户建议"有最新的拿最新的，过去数据和未来数据一起看，未来数据可来自机构预估"。

**实现**:
- `marketData.js`：
  - A股业绩预告：东财 `RPT_PUBLIC_OP_NEWPREDICT`（预告类型/金额区间/同比幅度/说明文字/公告日期）
  - A股机构一致预期：东财 F10 盈利预测 `PageAjax`（未来 1-3 年营收/净利/增速/EPS/ROE/PE 预测 + 综合评级/机构家数）
  - 美股/港股：Yahoo `earningsTrend`（本财年/下财年 EPS 预估，生产环境生效）
  - 统一美股/港股"最新财报"优先取单季行（避免单季/累计数值不稳定）
- `quoteContext.js`：快照新增【业绩预告】（仅当预告报告期比已披露实际报表更新）+【机构一致预期】；提示词明确"预测数据引用时必须标注为预测/预计/约，不得当作实际数据"。

**影响**:
- 实测：茅台 2026E 净利 860.3 亿（+4.5%）、2027E 907 亿、综合评级买入 19 家；宁德时代已披露 2026 中报 + 机构预测 2026E 净利 957.8 亿。
- 实测辩论：模型区分实际（"一季报营收547亿、PE15倍"）与预测（"2026年机构预计净利860亿、PE约19倍"）。

**待办**:
- 港股业绩预告/机构预测暂未接入（美股/港股用 Yahoo earningsTrend，生产生效）。

---

## 2026-08-08（第八批）

### 修复：裁决票数统计不可信 AI 数字，改为从发言立场现场统计
**背景**: 用户反馈明明有发言看多，票数却没统计进去。原因：裁决票数（bull/bear/neutralCount）直接采用 AI 在裁决 JSON 里写的数字，AI 偶尔数错。

**实现** (`page.js`)：
- 新增 `countVotes(items)`：从每条发言的实际立场（speech 的 stance）统计看多/看空/中性票数
- 两个裁决渲染块（已完成 + 当前打字）都改用现场统计的票数画条形图与票数文本
- 分享海报同样改为从发言立场统计票数
- 裁决的 summary/consensus/mainRisk 仍用 AI 的（语义内容），只有数字统计改为客观统计

**影响**:
- 票数与展示的发言立场永远一致，不再出现"有看多发言但票数为 0"。
- 浏览器实测：看多+中性 2 条发言 → 显示"看多 1 票 中性 1 票 看空 0 票"，与立场一致。

---

## 2026-08-08（第九批）

### 修复：左侧大师成员区无法滚动查看
**背景**: 用户反馈左侧成员列表滑不动。
**原因**: 侧边栏本身有 `max-height + overflow-y:auto`，但成员卡片在 flex 列布局里被压缩到容器高度（flex-shrink 默认 1），且卡片 `overflow:hidden`，导致超出部分被裁掉、scrollHeight==clientHeight，无内容可滚。
**修复** (`page.css`)：`.sidebar > .card-panel { flex-shrink: 0; }`，卡片保持完整内容高度，侧边栏正常内部滚动。
**验证**: 浏览器实测 scrollHeight 1717 > clientHeight 600，scrollTo 正常。

---

## 2026-08-08（第十批）

### 分享海报 v2 重设计（信息层级 / 可读性 / 专业度）
**背景**: 用户反馈旧海报问题：标题与内容错位（问题不是视觉主角）、文字密度过高、发言人名不突出、"性一"式怪后缀、色块生硬、关键数据淹没在正文。

**实现** (`src/lib/poster.js` 重写)：
- **问题做大标题**："本期议题"小标签 + 用户问题 52px 大字（最多 2 行），弱化品牌与主持人开场
- **观点卡片化**：每位大师一张立场色卡片（半透明底 + 色条 + 徽章），突出 姓名+立场徽章，关键提炼"观点：xxx"金色加粗标签
- **数字高亮**：发言正文中的数字/百分比（如 -9163万、+1.1亿、PB 1.34、257%）用金色加粗渲染（富文本分词绘制）
- **控制文字量**：发言正文最多 3 行、超出省略号；更多留白与行距
- **主持人开场**压缩为 2 行引用；裁决区紧凑保留；页脚保留免责声明
- 标签文案规范化（"主持人 · 姓名"），避免生硬拼接

**影响**:
- 海报重点更突出：先看议题 → 扫每位大师观点标签 → 关键数字一眼可见。
- 已用 4 位大师+含数字的模拟数据生成验证（1080×1920 无异常）。

**待办**:
- 视觉细节（字号/间距微调）可等用户看新海报后反馈再迭代。

---

## 2026-08-08（第十一批）

### 分享海报 v2.1 修复：undefined / 乱码折行 / 观点区大空白 / 内容溢出
**背景**: 用户实际生成海报发现：观点行出现"undefined"、乱码折行、观点与正文间大块空白、内容溢出卡片框线。

**原因** (`src/lib/poster.js`)：
1. undefined：`wrapLines` 在"单行就溢出"边界返回空数组，取 `kpLines[0]` 为 undefined 直接画上
2. 乱码折行：`wrapLines` 按"数字/非数字"分词，无数字的长文本=单个 token 无法折行且截断逻辑在空数组时失效
3. 大空白：卡片高度公式把观点区预留 84px，但实际只画 30px 高 → 中间空一大块
4. 溢出：卡片高度算小了 → 正文画到框外

**修复**：
- `wrapLines` 重写为**逐字折行 + 数字(含单位)保持原子不拆行**；截断必加"…"，任何非空文本至少返回一行
- 观点标签最多折 2 行，卡片高度按**实际测量行数**计算（观点行数、正文行数、留白精确求和）
- 新增 `clean()` 过滤模型输出的 "undefined"/"null" 脏值（观点/共识/风险）
- 卡片绘制改用游标推进，与高度计算一致

**验证**: Node 单测 5 组用例（超长无数字 1/2/3 行、含数字 3 行、短文本）——行数正确、数字不拆行、无空数组/undefined、无超宽行。

**待办**:
- 用户实际生成后再看视觉效果，需要的话继续微调字号/间距。

---

## 2026-08-08（第十二批）

### ⚠️ 事故修复：本地网站 500 打不开（.next 缓存损坏）
**背景**: 用户反馈网站打不开。根因：**开发服务器运行时我执行了 `npm run build`**——dev 与 build 共用 `.next` 目录，生产构建清理/覆盖了 dev 的构建产物，导致 dev 引用不存在的 chunk（`Cannot find module './948.js'`），页面/静态资源全 500。

**修复**:
1. 停止 dev 服务器
2. `rm -rf .next`（构建缓存，可安全清除）
3. 重新 `npm run dev` → 首页/大师页/接口全部 200 恢复正常

**教训（重要）**:
- ⚠️ **不要在 `npm run dev` 运行期间执行 `npm run build`**；需要构建时必须先停 dev。
- 若本地出现 `Cannot find module './NNN.js'` 或整站 500：先停 dev → 清 `.next` → 重启 dev。
- 验证本地页面用：`curl http://localhost:3000/` 状态码 200 + HTML 开头正常。

---

## 2026-08-08（第十三批）

### 分享海报 v2.2：深色背景 → 浅色（暖白纸感）主题
**背景**: 用户觉得海报黑底不好看，要白色。

**实现** (`src/lib/poster.js`)：
- 背景：暖白渐变（#fdfbf6 → #f3eee3）+ 金色柔光
- 文字：主文字近黑 #1f1d19、正文 #4a463f、次级 #6b675f、页脚 #a39d90
- 金色点缀改深金 #9a7830；数字高亮改深琥珀 #b56b00（白底可读）
- 立场色换浅主题色（看多 #2e7d57 / 看空 #c04040 / 中性 #9a7830）；卡片底色/边框透明度相应调低
- 头像金环改深金；裁决条轨道改黑色半透明

**验证**: 浏览器实测生成：背景像素近白 (245,240,234)，标题深色文字、金色数字高亮均正常。

---

## 2026-08-08（第十四批）

### 分享海报 v2.3：新增网站二维码 + 精选 2 位对立大师
**背景**: 用户要求海报带二维码，引导扫码访问网站（yieldglide.com）。

**实现** (`src/lib/poster.js`)：
- 安装 `qrcode` 依赖，客户端生成二维码（`QRCode.toDataURL`，深色模块/白底）
- 二维码放裁决区下方：白色圆角卡片 + 金边 + "扫码访问「大师吵股」" + 域名文案
- 二维码地址：优先 `NEXT_PUBLIC_SITE_URL`（非占位符时），否则兜底 `https://yieldglide.com`（本地 .env.local 里是占位符 your-domain.com）
- 为腾出空间：精选最多 2 位大师（优先一多一空，辩论感更强），观点标签最多 2 行、正文最多 2 行
- 修掉"观点：观点："重复前缀（模型 keyPoint 自带"观点："时去重）

**验证**: 浏览器实测 1080×1920 生成：只显示 看多(伍德)+看空(阿克曼) 两位、裁决、扫码文案、页脚全部在画面内；OCR 复核通过。

**待办**:
- 建议把 `.env.local` 的 `NEXT_PUBLIC_SITE_URL` 从占位符改为 `https://yieldglide.com`（影响元数据/二维码/规范链接）。

---

## 2026-08-08（第十五批）

### 分享海报 v2.4：悬念引导文案（未出场大师卖关子 → 扫码）
**背景**: 用户希望海报只展示主持人开场 + 1-2 位嘉宾，其余大师用悬念引导"想知道 XX、XX 怎么看？扫码进网站问问他们"。

**实现** (`src/lib/poster.js`)：
- 裁决区下方新增金色悬念引导卡：`还有 N 位大师也想聊聊这个话题` + `想知道 [未出场大师名] 怎么看？扫码进网站问问他们吧`（列出未展示的大师，最多 3 位）
- 版面压缩为悬念卡腾空间：卡片上下留白 44/36→38/30、姓名行 60→56、裁决条微压缩、二维码 176→160
- 保留：主持人开场 + 一多一空两位嘉宾 + 裁决 + 二维码 + 页脚

**验证**: 浏览器实测 + OCR：5 位大师的辩论，海报显示开场 + 伍德(看多)/阿克曼(看空)，悬念卡列出 巴菲特/泰珀/芒格，二维码与页脚完整无溢出。

---

## 2026-08-08（第十六批）

### 分享海报 v2.5：动态高度——主持人与嘉宾发言完整展示
**背景**: 用户反馈主持人和前 2 位嘉宾的内容被截断只显示"…"。

**原因**: 海报固定 1080×1920，为塞入二维码/悬念卡压缩了内容行数（主持人 2 行、正文 2 行）。

**实现** (`src/lib/poster.js` 重写 generatePoster)：
- **动态画布高度**：先用临时 ctx 按实际字体测量所有文本行数（标题/主持人/两位嘉宾的观点与正文/裁决/悬念/二维码），累加出总高度，再建画布绘制——内容多画布自动加高，**不截断**
- 主持人开场最多 6 行、嘉宾正文最多 6 行（实际都完整展示），二维码/悬念卡/页脚固定在底部
- 实测：长内容海报 1080×2148，OCR 确认主持人 4 行、两位嘉宾各 5 行全文完整，无"…"、无溢出

**待办**:
- 如需限制海报高度上限（如 >2800 时再截断），可后续加 maxHeight 兜底。

---

## 2026-08-08（第十七批·大版本）

### UI 改造 + 功能层大版本：紧凑头部 / 大师背景 / 三种模式 / 点对点 / 历史 / 邀请大师
**背景**: 用户设定目标：① UI 优化（标题区占空间大、背景想用大师画册营造氛围）② 功能层（邀请大师辩论虚拟画像、历史对话、点对点/群聊、辩论/探讨/教学三种模式）。

**实现**:
- **UI-紧凑头部**：标题区改为左置小号（20-30px）+ 副标题，右侧按钮区（主题/历史/微信码），不再占大块空间；内容区屏效提升。
- **UI-大师肖像背景**：新增"氛围背景"选择（简洁 / 以某位大师为背景），默认巴菲特；背景层 = 大师照片模糊 16px + 30% 透明度，text 可读；选择持久化 localStorage。
- **功能-对话模式**：新增 辩论(默认)/探讨/教学 三档切换；prompts.js 全面支持 mode（开场/发言/散场/裁决/追问的基调分别定制）；教学模式下模型用大白话解释术语（已验证：讲 PE"好比多少年回本"）。
- **功能-点对点深聊**：只选 1 位大师时进入专属流程（无主持人/裁决，直接问答）；追问用 buildChatPrompt 一对一深聊；修复了 currentBlock 类型仍写死 hostOpening 导致 solo 崩溃的 bug。
- **功能-历史对话**：右上角 🕘 历史面板；每场辩论结束自动存档（问题/大师/整场记录，最多 30 条），点击可恢复查看。
- **功能-邀请大师**：侧边栏"＋ 邀请"；`POST /api/virtual-master` 用 LLM 生成虚拟大师画像（含 知识域/思维框架/习惯，注入辩论让它"像本尊"）；入列后可选中辩论、可删除；localStorage 持久化，刷新仍在（已验证"段永平：本分价值投资人"）。
- **robustness**：DeepSeek 请求加 60s 超时 + 重试（之前有过一次挂 20 分钟）；修复 route.js 中 DEEPSEEK_URL 常量名笔误。

**验证**（浏览器实测）：
- 群聊 5 人完整跑通（开场+5 发言+裁决）；solo 巴菲特问答 + 追问跑通；教学模式发言确认是小白向。
- 邀请"段永平"生成画像并入列、刷新持久化；历史面板列出多场记录。

**待办**:
- 探讨模式仅按提示词实现（机制与教学一致），可再跑一场群聊验证效果。
- 虚拟大师基于 LLM 对人物的认知构建（非实时全网检索），后续可接入搜索增强画像。

---

## 2026-08-08（第十八批·目标收尾）

### 虚拟大师画像增强 + 探讨/教学/辩论三模式全量验证
**背景**: 大版本后收尾：① 增强虚拟大师"全网检索式"画像（核心观点/话术/决策习惯/风险偏好），让发言更像本尊；② 补齐探讨模式实跑验证。

**实现**:
- `/api/virtual-master` 画像新增字段：`coreViews`(核心观点) / `phrases`(常用话术) / `decisionHabits`(决策习惯) / `riskPref`(风险偏好)，基于人物公开言论提炼
- `prompts.js` 画像行注入这些字段（辩论/探讨/教学/点对点通用）

**验证**（Node 实测）：
- 增强版虚拟"段永平"发言："说价值投资在中国失效，就像说牛顿定律在中国不适用一样可笑……不懂不做，你懂腾讯、阿里、拼多多的商业模式吗？便宜不是买入理由，好公司才是，这才是本分。"——话术/观点全部入戏 ✅
- 探讨模式（巴菲特+芒格）：主持人"请两位先生不吝赐教"；芒格"巴菲特强调护城河……我建议用多元模型……如果股价跌50%你是否敢加仓"——理性互补不互怼 ✅
- 至此 辩论/探讨/教学 三模式、点对点、群聊、历史、虚拟大师、UI 全部验证通过。

**待办**:
- 真实"全网检索"增强画像可后续接搜索 API（当前基于 LLM 对人物的公开认知，已在 memory 标注）。

---

## 2026-08-08（第十九批·目标完成）

### 虚拟大师"公开资料检索"增强（维基百科，尽力而为）+ 目标收官
**背景**: 补上"全网检索"的可行部分：用维基百科公开资料作为画像依据（生产环境生效），检索失败优雅回退到 LLM 知识，绝不阻塞邀请。

**实现** (`/api/virtual-master`)：
- 新增 `fetchPublicContext()`：按 zh→en 尝试维基百科 action API 取人物简介，2.5s 超时，命中即注入 LLM 画像 prompt
- 本机网络对维基不稳定，实测本地走回退路径（仍正常生成画像，约 9s）；生产（Vercel 美国）可真正检索到公开资料
- DuckDuckGo 抓取经测不可靠，未接入（避免脆弱依赖）

**目标收官审计**（全部有实测证据）：
- UI-标题区紧凑化 ✅ 浏览器验证
- UI-大师肖像背景（默认巴菲特+可切换） ✅ 浏览器验证
- 功能-邀请大师虚拟画像（知识域/核心观点/话术/决策习惯/风险偏好+公开资料检索） ✅ 入戏辩论+持久化
- 功能-历史对话 ✅ 面板+恢复
- 功能-点对点深聊+群聊 ✅ 巴菲特 solo 问答+追问；5 人群聊全流程
- 功能-辩论/探讨/教学三模式 ✅ 全部实跑验证

**待办**:
- 若要做"完整全网检索"（搜索引擎级），需接入搜索 API Key（如 SerpAPI/Bing），当前用维基+LLM 认知近似。

---

## 2026-08-08（第二十批）

### 修复：虚拟大师主页 404 + 虚拟大师画像"像本人"升级（真实资料优先）
**背景**: 用户反馈：① 点虚拟大师资料→查看大师主页 404（虚拟大师无独立页面）；② 邀请创建的人风格和本人完全不同，质疑是否从全网搜集了资料。

**修复**:
- **404**：`MasterProfileModal` 对 `source==='custom'` 的虚拟大师不显示"查看大师主页"链接（其资料在弹窗内查看）；预置大师仍显示。浏览器实测：虚拟大师无链接 ✅、预置有 ✅。
- **画像升级**（`/api/virtual-master`）：
  - 邀请弹窗新增**"真实语录/资料"粘贴框**（强烈建议）——用户提供真人口径，按真人资料建模
  - 流程：维基百科检索（尽力而为）→ 有资料直接一步建模；无资料先 LLM"研究简报"（身份/观点/语录/评价/说话风格）再建模
  - 画像新增 `styleSample`（该人物口吻的示范发言，注入辩论提示词作为风格锚点）
  - DeepSeek 调用加 45s 超时 + 网络重试；超时返回友好提示
- `prompts.js`：虚拟大师的 `styleSample` 注入画像行

**验证**（Node 实测，带真实语录资料）：
- 风格示范："你问我这个公司能不能买，我先问你，你懂这个生意吗？不懂就不要碰……做对的事情，把事情做对，剩下的就交给时间。平常心，别贪，别怕，本分一点。" ✅
- 辩论发言："靠消息炒短线？这不叫投资，叫赌博……我当年买网易，不是靠消息，是看懂它游戏业务值多少钱……做对的事情，别拿命换快钱。" ✅

**待办**:
- 无资料人物（如"寒武纪的鳄鱼"这类小V）建议用户粘贴其语录/资料，建模才最像。

---

## 2026-08-08（第二十一批）

### 虚拟大师"编辑画像"能力（AI 生成不准时用户可修正）
**背景**: 用户指出"寒武纪的鳄鱼"这类已删号的知乎大V，AI 生成的画像细节可能是"合理推断"（如"有地质学背景"未必属实），希望从真实公开资料了解。

**实现**:
- `MasterProfileModal`：虚拟大师弹窗新增"✏️ 编辑画像"按钮（预置大师仍显示"查看大师主页"）
- `page.js`：编辑弹窗可修改 称号/风格/性格/金句/经历/经典理论/知识域/核心观点/常用话术；保存后更新 customMasters 并持久化 localStorage
- 邀请弹窗原有的"真实语录/资料粘贴"继续作为建模依据（对删号/资料少的人物最可靠）

**验证**（浏览器实测）：编辑段永平称号 → 保存 → 刷新后侧栏与资料弹窗均显示修正后内容 ✅

**说明**: 对已删号、公开资料极少的人物，AI 只能基于其残存的公开印象推断——最可靠路径是：① 邀请时粘贴其真实语录/资料；② 生成后用"编辑画像"修正细节。

---

## 2026-08-08（第二十二批）

### 虚拟大师改版：昵称全网检索建模（百度+Bing），移除维基百科
**背景**: 用户明确反对维基百科（"全是垃圾"），要求按"检索昵称→全网收集该人发表内容+他人评价→按热度/权重排序→再构建投资框架/思考方式/语言习惯"的流程做。

**实现** (`/api/virtual-master` 重写)：
- 移除维基百科检索
- 新增 `searchWeb()`：**百度 + Bing 双引擎**检索 `"昵称"`（精确匹配），解析标题/摘要/来源 URL；**按搜索结果排序加权**（靠前权重更高）；过滤商标/工商/知识产权类噪音（企查查/爱企查/天眼查等）
- 资料优先级：用户粘贴的真实资料 > 检索语料 > LLM 知识（无检索结果时才用研究简报兜底）
- 建模提示词明确：检索资料越靠前越可信；**资料未提及的具体细节禁止编造**（可留空/通用表述）

**验证**（Node 实测"寒武纪的鳄鱼"，未贴资料，9s）：
- 检索到：知乎"思想档案/思想体系"文章、雪球"投资原则：央企或省国资委控股国企"、头条"800万到千万实盘心得"、知识星球/人物页（真实姓名）
- 画像基于真实信息：金句"控制交易的频率和次数，做确定性很大的投资……减少出手的频率，提高出手的胜率"；经历含"2018年天涯800万实盘记录、2026年删号退乎引发讨论"；风格"保守、中长线、确定性优先、央企国企偏好" ✅

**说明/局限**:
- 真实的阅读量/点赞/收藏数据需要平台 API（知乎/雪球需登录态），当前用**搜索相关度/排序作为权重代理**；若要精确热度加权，需用户提供搜索 API Key 或平台 Cookie。
- 知乎/雪球正文直接抓取被反爬（403/加密），当前用标题+摘要作为语料主体（信息量已足够）。

---

## 2026-08-08（第二十三批）

### 虚拟大师改版 2：从"抓数据"转向"阅读式语料"（来源+可信度标注）
**背景**: 用户指出核心目的不是抓数据，而是"阅读相关内容从而构建画像"；认可"能读就读全文、读不到用摘要、给语料标来源可信度"的方向。

**实现** (`/api/virtual-master`)：
- 新增 `fetchArticle()`：对检索结果靠前 2 条尝试**读取全文正文**（跳过已知反爬站：知乎/雪球/知识星球/微博/微信；验证码/登录墙过滤；中文正文>80 字才收）
- 语料改为**带来源与可信度**的分层结构：
  - 用户粘贴资料（可信度最高）
  - 读取到的全文正文（可信度高）
  - 搜索摘要（可信度中，搜索引擎浓缩）
- 建模提示词：让 AI"像人一样带着判断去阅读"——按可信度取舍、矛盾时以高者为准、资料未提及不编造

**验证**（寒武纪的鳄鱼，5s）：画像仍基于真实资料（金句/800万实盘/央企国企偏好）；因知乎/雪球反爬，本次走"搜索摘要"层，全文读取为尽力而为。

**说明**: 全文读取受反爬限制，能读到的源（头条/部分博客）有限；摘要层已足够承载核心信息。

---

## 2026-08-08（第二十四批）

### 一键"与 TA 单聊"入口（点对点深聊可发现性）
**背景**: 用户问"没有跟大师一对一的单聊功能吗"——点对点深聊其实已实现（只选 1 位大师即进入），但入口不显眼。

**实现**:
- `MasterProfileModal` 新增 **"💬 与 TA 单聊 →"** 按钮（每个大师的资料弹窗）
- `page.js` 新增 `startSoloChat(master)`：只选中该大师、清空当前讨论、聚焦问题输入框，直接进入一对一深聊
- 保留原有"只选 1 位大师"的入口方式

**验证**（浏览器）：点单聊按钮后 已选 1 位、弹窗关闭、输入框聚焦 ✅；solo 问答+追问流程此前已验证。

---

## 2026-08-08（第二十五批·UI 大改版 + 邀请功能全面升级）

### 背景
用户用 design-taste-frontend skill 审查 UI，指出圆角混乱、无无衬线 UI 字体、侧栏拥挤、邀请生成无过程反馈；并认为"邀请大师"功能是闪光点但太弱，要求两批一起改。

### 实现
**A1 圆角统一**：3px→4px（控件）、6px→8px（图片/历史项）、卡片 4px→8px；形成"控件4/卡片8/徽章圆润"一套。
**A2 无衬线系统 UI**：globals 新增 `--font-sans`；按钮/输入/标签/模式切换/下拉/侧栏 meta 改 sans，衬线保留给标题/发言/金句（编辑感与可读性平衡）。
**B5 侧栏行精简**：行内去掉 📋/✕，改为 hover 显隐的 "⋯" 打开资料弹窗；虚拟大师带 "AI·全网画像" 徽章；移除操作收进资料弹窗。
**C8/D2 生成过程可视化**：`/api/virtual-master` 改为**流式 NDJSON**（search→research→build→done/error），邀请弹窗展示三步实时进度。
**D1 醒目入口**：侧栏顶部主色按钮 "✦ 邀请一位大师"。
**D3 名片化**：生成后先展示"大师档案卡"（头像/称号/金句/风格示范/核心观点/**基于这些公开资料构建**的来源列表），可 编辑画像 / 重新构建 / 确认加入。
**D4 差异化**：虚拟大师徽章 + 资料弹窗展示 风格示范 + 资料来源 + 移除。
**D5 引导**：确认加入后 "💬 和 TA 单聊 / 完成"。

### 验证（浏览器实测全流程）
- 侧栏：醒目入口、行精简、AI 徽章 ✅
- 邀请：表单→构建三步→档案卡（6 条真实资料来源）→确认加入→单聊引导 ✅
- 资料弹窗：风格示范/资料来源/移除 ✅；移除后列表更新 ✅
- 修复：单聊按钮先关闭邀请弹窗（避免遮罩挡住后续操作）

---

## 2026-08-08（第二十六批）

### 交互模型重构：两级「群聊 / 单聊」，群聊内含三种风格
**背景**: 用户明确分类：聊天方式 = 群聊 / 单聊；群聊内含 辩论 / 探讨 / 教学 三种风格。要求按此重构交互。

**实现**:
- 提问区顶部新增**「群聊 | 单聊」**两级切换（chatMode，持久化 localStorage）
- 群聊态：显示嵌套的「风格：辩论/探讨/教学」选择 + 群聊提示（勾选 2 位以上大师）
- 单聊态：隐藏风格选择，显示单聊说明（与 1 位大师一对一深聊，可连续追问）
- 校验：群聊 <2 人报错"群聊请至少选择 2 位大师"；单聊 ≠1 人报错"单聊请选择 1 位大师"
- "与 TA 单聊"按钮会自动切到单聊模式并选中该大师
- 侧栏提示随模式切换（群聊/单聊各一套文案）

**验证**（浏览器）：两级切换、风格嵌套显示/隐藏、两类校验报错、单聊联动全部 ✅

---

## 2026-08-08（第二十七批）

### 交互再优化：单聊单选对象 + 风格收敛为下拉（去 tab 堆叠）
**背景**: 用户反馈：单聊应支持"单聊对象"选择且不能多选；顶部群聊/单聊 + 群聊 3 风格 tab 层级太多。

**实现**:
- **单聊 = 单选对象**：侧栏在单聊态变单选（点大师=替换，点已选的=取消）；隐藏 全选/清空/随机5人（群聊专属）；侧栏计数显示"单聊对象：XX / 未选择"；提问卡显示单聊对象 chips（头像+名字+称号）
- **风格收敛**：群聊的 3 个风格 tab → 1 个下拉选择（"辩论·真实互怼/探讨·理性拆解/教学·小白友好"），消除 tab 堆叠感
- 保留两级模型：群聊/单聊切换 + 群聊内风格选择；校验不变（群聊≥2、单聊=1）

**验证**（浏览器）：群聊显示风格下拉+批量按钮；单聊隐藏批量、单选替换/取消、对象 chips 同步 ✅

---

## 2026-08-08（第二十八批）

### 修复：概念/风格/宏观类问题误提示"未识别到公司"
**背景**: 用户反馈概念类问题（如"介绍一下你的投资风格""什么是安全边际"）也会提示"请补充公司名称"，误导。

**原因**: 原逻辑用宽泛关键词正则（含"投资"等）判断是否像股票问题，误伤概念/方法论/宏观类。

**实现**:
- `marketData.js` 新增 `needsCompanyData(query)`：用 LLM 判断"该问题是否真的需要某只具体公司的数据"（概念/方法论/风格/宏观→false；问某只具体股票但没给名/提到自己持仓重仓没说明哪只→true；已给公司名→false）
- 结果按 query 缓存 1 小时；失败保守返回 false（不打扰）
- `quoteContext.js`：无公司时先用快速正则挡掉明显概念类（什么是/怎么理解/介绍/方法论/风格等），其余走 LLM 判定
- 提示文案改为"如果这个问题涉及某只股票，请补充……"

**验证**（真实 /api/context）：
- "我重仓的那只票亏了20%，该怎么办" → 提示 ✅
- "介绍一下你的投资风格 / 怎么看当前A股行情 / 什么是安全边际 / 怎么理解市盈率" → 不提示 ✅

---

## 2026-08-08（第二十九批）

### AI 判定需要公司信息时：弹窗让用户补充问题细节
**背景**: 用户要求：AI 判定问题需要具体公司数据但没识别到时，应**弹窗提示用户补充**，而不是仅行内提示后继续。

**实现**:
- `page.js` go()：`/api/context` 返回 notice 且未 force 时 → 暂停发起，弹出"需要补充公司信息"弹窗（输入公司名/代码，Enter 或按钮提交）
  - 「补充并继续」：把公司名拼到问题前重新发起（自动拉取该司行情财务）
  - 「不带数据继续」：按原问题继续（无快照，大师凭知识回答，行内保留提示）
  - ×/遮罩 = 不带数据继续
- 判定增强：`quoteContext.js` 加快速规则（"我有/我持有/我重仓…股票/票"却没点名 → 必提示）；`marketData.js` LLM 判定提示词强化（"我有只股票亏了20%"这类倾诉语气也要判 true）

**验证**：
- 服务端 /api/context 对"我有只股票亏了20%，太难受了，不知道接下来是不是要继续持有"返回提示 ✅
- 概念/风格/宏观类仍不提示 ✅
- 构建通过；弹窗交互逻辑经代码审查确认（浏览器自动化环境点击通道失效，未能 E2E，待用户实机确认）

---

## 2026-08-08（第三十批）

### 修复：弹窗后残留行内"未识别到公司"提示
**背景**: 用户截图反馈：选择"不带数据继续"（或关闭弹窗）后，辩论正常跑，但行内提示"未识别到具体公司…"仍留在原地，像 bug。

**原因**: go() 在打开弹窗时仍 setNotice 了行内提示；跳过（force）路径也不清 notice → 残留。

**修复** (`page.js`)：
- 弹窗出现时不再设置行内提示（弹窗即提示）
- 正常发起 / 补充后 / 不带数据继续 三处都清空 notice
- 现在"需要公司"只以弹窗形式出现一次，选择后不留痕迹

**验证**: 构建通过；逻辑核对（浏览器自动化点击通道失效未能 E2E，待实机确认）。

---

## 2026-08-08（第三十一批）

### UI 全面优化：按 impeccable 审查报告落实（30/40 → 已清零检测器告警）
**背景**: 用户要求用 impeccable skill 审查首页 UI 并落地优化。审查报告（.impeccable/critique/2026-08-08T14-22-31Z__src-app-page-js.md）指出 P0 深色次级文字不可读、P1 亮色 accent 对比不足/双金色主按钮/邀请失败错误被遮罩盖住/master-row 焦点不可见、P2 移动端提问卡被压到首屏外/弹窗焦点管理/性能、P3 死 CSS/重复 header/无 reduced-motion 等。

**决策**: 保持「金色+学院风+纸感」既有品牌语言，只做层级收敛与可读性达标；立场色条（speech/裁决/引用块的语义色）作为品牌方言保留并注释说明。

**实现**:
- globals.css：深色 `--text-muted` #3a3a5a→#9b98ad（对比 1.8:1→6.7:1）、`--border`/`--border-subtle` 提亮；亮色 `--accent` #9a7830→#7d5f1f（4.1:1→5.9:1），neutral/accent-dim/bg 同步；追加 `prefers-reduced-motion` 全局降级
- page.css：合并两份 `.header`（去 !important）；删死 CSS（.mode-switch/.mode-btn/.master-profile-btn/.master-del-btn/.solo-hint）；`.invite-entry` 金色渐变主按钮→幽灵按钮（透明底+金描边+金字），金色渐变只留 `.btn-submit`；全局 `:focus-visible` + `.master-row:focus-visible`；裁决条硬编码色改 var()；≤900px `.main{order:-1}` 提问卡先行；`@media(hover:none)` 触屏 ⋯ 常显；新增 `.invite-error`、`.profile-remove-btn.confirming`；立场色条 3px→2px 并加 impeccable-disable-line 注释；打字点动画 bounce→柔和呼吸淡入；裁决条 width transition→scaleX 生长动画
- page.js：新增 `inviteError` 状态（邀请失败就近显示在弹窗内）；弹窗 Escape 统一关闭（invite/edit/history/supplement/poster/profile）；按钮加载文案「⟳ 召集中…」（i18n 新增 summoning）；🕘 emoji→内联 SVG 时钟；删除提问卡 footer 重复「已选 X/Y」；讨论容器点击跳过打字机；邀请弹窗输入框 autoFocus + aria-labelledby；各弹窗补 aria-labelledby/label
- MasterProfileModal.js：移除虚拟大师两段式确认（点一次变「再点一次确认移除」，3 秒未点自动复位）；hooks 移到 early return 前 + 卸载清计时器（修 Rules of Hooks 违规）
- favicon：PIL 重新生成 favicon.ico 445B / favicon.png 5KB（原各 1.67MB，首页多下 3.3MB）
- 补提交此前遗漏的 28 张真实大师头像（public/avatars/*，masters.js 已引用，不提交线上会 404）

**关键坑**: 新加的 Escape useEffect 依赖数组直接引用后置的 `skipSupplement`/`closeInvite` → 渲染期 TDZ 报错「Cannot access 'skipSupplement' before initialization」，整页水合失败（build 不报，浏览器才报）。已把该 effect 移到 skipSupplement 定义之后。教训：useEffect 依赖数组里的函数若声明在 effect 之后，渲染期求值会踩 TDZ。

**验证**: npm run build 通过；impeccable detect 对 page.css/globals.css/page.js/组件 0 告警；应用内浏览器实测——暗色/亮色变量与对比度达标、邀请按钮幽灵样式生效、主按钮保留金色渐变、「已选 X/Y」仅一处、历史图标为 SVG、头像弹窗 aria 齐全、Escape 可关弹窗、邀请弹窗 autoFocus 生效、移动端媒体查询 `.main{order:-1}` 生效。

**待办**: NEXT_PUBLIC_SITE_URL 目前是占位 your-domain.com，部署前需改成真实域名（layout.js metadataBase 会跟着生效）；弹窗焦点陷阱（focus trap）未做，属可选增强。

## 2026-08-08（第三十二批）

### 金色按钮/激活 tab 文字色：按主题自适应（--on-accent）
**背景**: 用户反馈「召集智囊团」等金色按钮文字用近黑色不好看，深金底应配白字；激活 tab（群聊/单聊）同理。

**决策**: 新增设计系统 token `--on-accent`（金色/强调色背景上的文字色）与 `--accent-strong`（金色渐变末端），按主题取反：
- 亮色：accent #7d5f1f、渐变末端 #6a5218（原 #9a7830 偏浅，白字只有 4.1:1）、on-accent 纯白（5.95–7.4:1 ✅）
- 暗色：accent #c9a84c（浅金）、渐变末端 #9a7830、on-accent 近黑（8.6/4.8:1 ✅）——暗色下用白字只有 2.3:1 会看不清，所以保持深字

**实现**: globals.css 增 4 个 token；page.css 的 .btn-submit/.btn-followup/.poster-btn-save/.invite-btn-primary/.profile-chat-btn/.chat-mode-btn.active/.profile-remove-btn.confirming 全部由硬编码 #0a0a0f 改为 var(--on-accent)，渐变末端由 #9a7830 改为 var(--accent-strong)。

**验证**: 浏览器实测亮/暗两主题按钮与激活 tab 前景色正确；impeccable detect 0 告警；构建通过。

**影响**: 亮色下金色按钮更醒目（白字+深金底），暗色下保持可读；语义上用「on-color」而非一刀切白色，避免暗色主题白字不可读。

## 2026-08-08（第三十三批）

### 背景壁纸重构：羊皮纸质感 + 大师金色双色调水印
**背景**: 用户反馈背景壁纸「完全看不出来」。诊断：默认浅色主题下，巴菲特照片以 blur(16px)+opacity 0.3 全屏铺在米色羊皮纸上，暖色调照片在浅底上几乎融没；纸面光晕仅 8% 金色，整体等于无背景。

**决策**: 把背景拆成两层有质感的系统：① 纸感升级（globals.css body::before）② 大师水印（page.css .bg-master-layer），贴合「学院/书房/藏书票」定位。

**实现**:
- body::before：新增 SVG feTurbulence 灰度颗粒纹理（data URI，纸张颗粒感）+ 顶部柔光/四角晕影；亮色=陈年羊皮纸（暖棕边缘压暗），暗色=书房琥珀光（暖金色灯晕 + 四角压暗 0.42，不再是死黑）
- .bg-master-layer img：由「blur16 + opacity0.3 全屏」改为「金色双色调水印」——grayscale(1) sepia(0.6) saturate(1.5) brightness(1.02) contrast(1.02) blur(1.6px)，亮色 opacity 0.17 / 暗色 0.24，radial mask 从右上角(78%,8%)淡入、向内容区渐隐（藏书票/铜版画质感）；blur 16→1.6 同时解决性能开销
- 「简洁·无背景」选项保留：只留羊皮纸，不加水印

**验证**: 用 PIL 对比「水印开/关」两屏截图像素——亮色右上角差异 avg 23-25、暗色 20/17/11（暖金调），左右/中部边缘≈0（渐隐正确）；impeccable detect 0 告警；构建通过。

**待办**: 无。

## 2026-08-08（第三十四批）

### 邀请的大师默认排在最前
**背景**: 用户希望邀请（创建）的虚拟大师默认排在成员列表第一位。

**决策**: 自定义大师整体前置到预置大师之前；同一批邀请的，最新邀请排最前。

**实现** (page.js)：
- `allMasters = [...customMasters, ...PRESET_MASTERS]`（成员列表=allMasters.map，直接生效）
- `confirmInvite` 新增时 `[inviteMaster, ...customMasters]`（最新邀请最前）
- 恢复名单的 `roster` 同步为 customs 在前（校验/随机不受顺序影响）

**验证**: 内置浏览器实测——本地已有邀请的「段永平 [AI·全网画像]」真实排在巴菲特等 33 位预置大师之前；数组逻辑复现：林小梅→老段→巴菲特→芒格→索罗斯；构建通过。

## 2026-08-08（第三十五批）

### UI 纵深优化：按用户建议落地（毛玻璃/卡片阴影/墨色主按钮/左右栏分色/留白）
**背景**: 用户提供一份五点 UI 优化建议（背景处理/色彩/光影纵深/排版/三步微调），要求照此优化。

**决策**: 在既有「羊皮纸+金色学院风」基础上做层级与质感升级，不推倒重来；背景已在上一批重构过，本次只加底部压暗渐变与宣纸颗粒 5.5%→7.5%；主按钮按建议改为「墨色底+暗金文字」（取代上批的白字金底）。

**实现**:
- globals.css 新 token：`--bg-card-sidebar`(左暖白)/`--bg-card-main`(右米黄)/`--header-bg`(毛玻璃半透明)/`--btn-primary-bg`/`--btn-primary-text`(墨底金字)/`--shadow-soft`/`--shadow-bubble`
- 顶部 header：position sticky + backdrop-filter blur(14px) saturate(1.15) 毛玻璃（亮/暗各半透明 0.72）
- 侧栏：向右微弱内阴影（inset -1px + 外投 8px 柔影），暗色主题同步
- 左右栏底色微妙区分：侧栏卡片暖白、主栏卡片米黄（暗色同构区分）
- 聊天卡片化：.speech-content/.host-block/.user-followup 圆角 0 6px 6px 0 / 6px + 极淡阴影；.card-panel 圆角 8→6px + shadow-soft
- 排版：.speech-name 700→800、.master-name 600→700、.verdict-title 700→800；正文 .speech-content weight 400、字号 13.5→14、行高 1.95
- 留白 +20~30%：.main-layout padding/gap、.discussion-container gap 24→28、.speech-row gap 12→16、.speech-meta margin、.master-row gap/padding
- 主按钮 .btn-submit：墨色底(#201f26 亮 / #1c1b23 暗) + 暗金文字(#c9a84c) + 金描边，hover 上浮 1px + 柔影
- 背景：宣纸颗粒 5.5%→7.5%；底部加压暗渐变（亮=暖棕 5%、暗=黑 22%），强化底部落地感
- 大师发言前的极细立场竖线（2px）保留，即建议中的引用装饰

**验证**: 浏览器实测亮/暗两主题——header 毛玻璃 backdrop-filter 生效、侧栏/主栏卡片底色区分（#fffdf5 vs #fbf6ea / #12121e vs #0e0e18）、墨色按钮渲染确认（像素采样 ≈#201f26）、卡片圆角/阴影/字号/留白均生效；impeccable detect 0 告警；构建通过。

**待办**: 墨色主按钮若用户觉得太暗可回退金底白字（上批 commit c236911 前的样式）。

## 2026-08-08（第三十六批）

### 去框化：一体沉浸式布局（标题融入纸面 + 卡片全透明）
**背景**: 用户反馈 UI「切割感太重」，标题区与内容区分成两块，希望打造一体的沉浸体验。

**决策**: 把「盒子拼贴」改为「一整张纸面」——标题区融入纸面（去毛玻璃条/去边框/不吸顶），所有卡片去边框去阴影去底色直接落在羊皮纸上，左右栏不再分色，气泡外框改极淡细线。

**实现** (page.css)：
- .header：去掉 sticky/backdrop-filter/边框/底色，改为透明题头（padding 26px 顶），下方用一条「渐隐细金线」（linear-gradient 90deg 金→透明，opacity 0.3）柔和过渡
- .main-layout：padding 上缩下放（12px 18px 40px）、栏间距 26px
- .card-panel：background transparent、去 border/shadow/radius（内容直接落在纸上）；章节靠 .card-title 等宽标签+更淡的横线区分
- .sidebar：去掉内阴影与左右分色，改为极细竖线分隔（border-right var(--border-subtle)），移动端去掉竖线
- .speech-content / .host-block / .user-followup：外框 var(--border)→var(--border-subtle)（更淡），保留立场竖线与柔影

**验证**: 浏览器实测亮/暗——header 透明不吸顶、::after 金线存在、card-panel 全透明、侧栏仅细竖线；像素采样：主栏中部从旧版纯白卡片(255,254,249)变为纸色(243,241,235)，说明盒子已消失；impeccable detect 0 告警；构建通过。

**待办**: 若觉得金线/竖线也可以不要，一句话去掉。

## 2026-08-08（第三十七批）

### 强化「选择成员」区：选中态一眼可读
**背景**: 用户反馈成员选择功能太弱、看不出来。原选中态只有 4% 底色 + 33% 边框 + 选中才出现的 9px 小 ✓，在纸面上几乎不可见。

**决策**: 让「选没选」成为第一眼信息：常驻圆形勾选框（未选=空心圈，选中=金色实心✓）、选中行加大师色竖条+加强底色、计数改胶囊徽章。

**实现**:
- page.js：master-row 选中态由「整框变色」改为「左侧 3px 大师色竖条 + `${inv.color}12` 底色」；勾选框改为每行常驻渲染（`master-check on/off`）
- page.css：.master-check 16px 圆形（空心圈=未选/金色实心✓=选中，颜色用 var(--accent)+var(--on-accent) 保证对比）；.master-row hover 金色微底 + 勾圈描边变金（可点感）；.sidebar-count 改胶囊徽章（accent-bg 底 + accent-dim 描边 + 700 字重）；.mini-btn（全选/清空/随机5人）字号 10→11、圆角 6px

**验证**: 浏览器实测——33 行中 5 行选中，选中行竖条/底色/金勾均生效（首行段永平：3px 竖条 rgb(30,77,140)、勾圈 #7d5f1f），未选中行空心圈常驻；计数徽章「已选 5 / 33」胶囊样式；impeccable detect 0 告警（选中竖条按语义色注释豁免）；构建通过。

## 2026-08-08（第三十八批）

### 强化成员操作按钮（全选/清空/随机5人）
**背景**: 用户反馈这三个功能按钮「根本注意不到」。原为透明底+淡描边+灰字小按钮，在纸面上几乎隐身。

**决策**: 升级为实体按钮：有底色、有描边、深色文字、带图标，一眼可辨。

**实现**:
- ui.js MiniBtn：resting 态从「透明底/border-subtle/text-muted」改为「bg-input 底/var(--border) 描边/var(--text) 深字」，hover 变金
- page.js：三个按钮加图标前缀（✓ 全选 / ✕ 清空 / 🎲 随机5人）
- page.css：.mini-btn 字号 11.5px、字重 600、padding 8px、圆角 8px、无衬线、inline-flex 图标+文字居中

**验证**: 浏览器实测——三按钮文字「✓全选 ✕清空 🎲随机5人」，底色 rgb(250,248,243)、描边 rgb(212,207,196)、深色文字，均清晰可见；impeccable detect 0 告警；构建通过。

## 2026-08-08（第三十九批）

### 成员区：标题强化 + 邀请按钮并入大师列表上方
**背景**: 用户反馈「选择成员」标题太弱；顶部的「邀请一位大师」独立按钮希望并入成员卡片、放在大师列表上方。

**决策**: ① 章节标题统一放大加粗（9px→11px、字重 800）② 把侧栏顶部的独立邀请按钮移到成员卡片内、计数徽章与大师列表之间。

**实现**:
- page.js：删除 sidebar 顶部独立 invite-entry；在 Card（成员）内 sidebar-count 之后、master-list 之前插入 invite-entry
- page.css：.card-title 11px/800/字距2.5px；.invite-entry 加 margin 2px 0 10px（嵌入列表上方）

**验证**: 浏览器实测——侧栏首子元素已变为 card-panel（独立按钮消失）；成员卡内部顺序：标题→操作按钮→计数→邀请按钮→大师列表；标题 11px/800/金色；impeccable detect 0 告警；构建通过。

## 2026-08-08（第四十批）

### 自定义大师头像：emoji 兜底 → 首字符圆形填充
**背景**: 用户反馈邀请（自定义）大师的头像是透明底 emoji，与预置大师的圆形照片头像风格格格不入，希望改成「取首汉字/首字符」的圆形填充风格。

**决策**: MasterAvatar 无真实头像时，改为「大师色圆形填充 + 姓名首字符」，统一圆形象征风格。

**实现** (src/components/ui.js MasterAvatar fallback)：
- 取 name 第一个汉字（无汉字则取首字符大写，如英文名首字母）
- 背景 = master.color（非法 hex 兜底 #5a5a7a）；文字色按背景亮度自适应（lum>0.55 深字否则米白），保证对比
- fontSize = size*0.5、字重 700、无衬线、inset 0 0 0 1px 细环增加质感
- 一处修改，成员列表/辩论发言/资料弹窗全部生效

**验证**: 浏览器实测——本地自定义「段永平」头像现为圆形填充 rgb(30,77,140)+白色「段」，预置大师仍用照片；impeccable detect 0 告警；构建通过。

## 2026-08-08（第四十一批）

### 细节优化：去掉「AI·全网画像」徽章 + 自定义大师颜色约束到预置色板
**背景**: ① 列表页的「AI·全网画像」徽章用户觉得多余；② 自定义大师头像填充色是邀请时 AI 随手给的十六进制（段永平=深蓝 #1e4d8c），与预置大师风格不够接近。

**决策**: ① 删除徽章（仅列表页用，CSS 一并清理）② 新增 `CUSTOM_COLOR_PALETTE`（=预置大师全部 26 个颜色的去重集合）+ `snapColorToPalette()` 最近色吸附，邀请生成与存量加载都统一走该色板。

**实现**:
- src/data/masters.js：导出 CUSTOM_COLOR_PALETTE、snapColorToPalette（RGB 平方距离最近色，无效色回退 #c9a84c）
- src/app/api/virtual-master/route.js：color 由 LLM 任意 hex → snapColorToPalette(p.color)
- src/app/page.js：删除 master-custom-badge JSX；加载 localStorage 自定义大师时 color 统一吸附到色板（存量数据即时生效）
- src/app/page.css：删除 .master-custom-badge 规则

**验证**: 浏览器实测——列表页徽章 0 个；段永平头像色由 #1e4d8c 吸附为 #34495e（深蓝灰，预置大师沃萨同款色系），仍显示「段」；impeccable detect 0 告警；构建通过。

## 2026-08-08（第四十二批）

### 邀请大师表单：去掉「真实语录/资料」，保留「补充说明」
**背景**: 用户希望邀请功能里「真实语录 / 资料」字段可以拿掉，「补充说明」保留。

**决策/实现** (page.js)：
- 删除 inviteMaterials state、POST body 中的 materials 字段、reset、依赖数组引用
- 删除「真实语录 / 资料（可选，强烈建议）」label + textarea
- 保留「补充说明（可选）」输入框；API 端本就对缺失 materials 容错（typeof 判断），无需改动

**验证**: 浏览器实测——邀请弹窗 label 只剩「人物名字/昵称」「补充说明（可选）」，textarea 仅 1 个；impeccable detect 0 告警；构建通过。

## 2026-08-08（第四十三批）

### 大师行「⋯」更多按钮：45% 透明 → 常显圆形描边按钮
**背景**: 用户反馈三个点的更多按钮太弱、看不到。原为无边框、opacity 0.45 的裸字符。

**实现** (page.css .master-more-btn)：22×22px 圆形、1px border-subtle 描边、opacity 1 常显、13px ⋯、hover 变金+淡金底；删除触屏 hover:none 的 0.8 覆盖（已常显无需）。

**验证**: 浏览器实测——22px 圆形、描边、opacity 1、title「资料与操作」；impeccable detect 0 告警；构建通过。

## 2026-08-08（第四十四批）

### 大师详情弹窗：固定高度 + 底部操作按钮固定右下角
**背景**: 用户希望大师详情弹窗高度固定，底部按钮固定在右下角（内容滚动时按钮不跟着跑）。

**实现**:
- MasterProfileModal.js：把 .profile-actions 从 .profile-body 内移到弹窗根级（作为 footer 兄弟节点，不再随内容滚动）
- page.css：.profile-modal 改 flex column + height min(640px, 88vh) + overflow hidden；.profile-header flex-shrink 0；.profile-body flex:1 overflow-y auto；.profile-actions 改 footer（border-top 细线 + bg-input 底 + align-items flex-end 右下角 + padding）

**验证**: 浏览器实测——弹窗高 634px 固定，children=[header, body, actions]，actions 父节点为 modal-content（已脱离滚动区），底部间隙 1px、右对齐；impeccable detect 0 告警；构建通过。

## 2026-08-08（第四十五批）

### 大师详情弹窗：删除「编辑画像」+「移除」与「单聊」同行
**背景**: 用户要求：编辑画像功能删掉；从智囊团移除按钮放到与「与 TA 单聊」同一行。

**实现**:
- page.js：删除 editMaster/editForm state、openEditMaster/saveEditMaster、Escape 分支、编辑弹窗整段 JSX、邀请预览里的「✏️ 编辑画像」按钮、MasterProfileModal 的 onEdit 传参
- MasterProfileModal.js：去掉 onEdit prop 与编辑按钮；footer 按钮改为行布局
- page.css：.profile-actions 改 flex-row + justify-content flex-end（单聊+移除/查看主页同一行右下角）；.profile-remove-btn 由裸文字升级为红色描边按钮（与单聊按钮同高配平），confirming 保持实心红

**坑**: 删 JSX 时残留一个 `</>` 闭合标签导致编译报错（dev 日志暴露），已修复；期间误在 dev 运行时执行 next build（项目记忆明确禁止），按规范 rm -rf .next 重建通过。

**验证**: 浏览器实测——自定义大师底部按钮「💬 与 TA 单聊 →」「从智囊团移除」同一行、右对齐；预置大师「单聊 + 查看主页」同行；全文无「编辑画像」；impeccable detect 0 告警；干净构建通过。

## 2026-08-08（第四十六批）

### 氛围背景：固定巴菲特，删除背景人物切换
**背景**: 用户要求氛围背景默认就用巴菲特，去掉背景人物切换功能。

**实现**:
- page.js：删除 bgMaster state、BG_KEY、localStorage 恢复逻辑、handleBgMaster 回调、侧栏「氛围背景」下拉控件；渲染水印硬编码使用巴菲特（invMap['buffett'] → /avatars/buffett.jpg）
- page.css：删除 .bg-control/.bg-label/.bg-select 死样式

**验证**: 浏览器实测——bg-control 已不存在，bg-master-layer 仍渲染 /avatars/buffett.jpg 且加载成功；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第四十七批）

### 巴菲特背景水印更清晰
**背景**: 用户希望背景的巴菲特头像显示得更清晰一点。

**实现** (page.css .bg-master-layer img)：模糊 1.6px→0.4px；sepia 0.6→0.35（更接近黑白蚀刻、脸更易辨认）；contrast 1.02→1.08；透明度浅色 0.17→0.22、深色 0.24→0.30；mask 强区扩大（0.98@14% → 0.6@50% → 透明@82%）。

**验证**: 像素采样——浅色右上角水印区 avg (231,227,219)→(210,206,196)（显著更明显），深色 (36,32,31)→(39,38,36)（更暖更显眼）；中心内容区仍保持纸色可读；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第四十八批）

### 成员操作按钮顺序：随机5人移到最左
**背景**: 用户要求把「随机5人」放在最左边。

**实现** (page.js sidebar-actions)：顺序由「✓全选 ✕清空 🎲随机5人」改为「🎲随机5人 ✓全选 ✕清空」。

**验证**: 浏览器实测 DOM 顺序为 🎲随机5人 → ✓全选 → ✕清空；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第四十九批）

### 「召集智囊团」按钮：移到右下角 + 配色与群聊激活 tab 一致
**背景**: 用户要求按钮放右下角（当前左下角），配色改成与群聊 tab 切换一致（金色背景+白色前景）。

**实现**:
- page.css：.question-footer justify-content space-between→flex-end（单子元素时按钮贴右）；.btn-submit 由「墨底金字」改回「var(--accent) 底 + var(--on-accent) 字」（浅色=金底白字，深色=浅金底深字，与 .chat-mode-btn.active 完全一致），hover 保留上浮+增亮
- globals.css：删除已无用的 --btn-primary-bg / --btn-primary-text token

**验证**: 浏览器实测——按钮右间隙 0（贴右下角）、justify flex-end；按钮 bg/color 与 chat-mode-btn.active 完全一致（rgb(125,95,31) 底 + 白字）；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第五十批）

### 提问卡标题颜色：绿色 → 金色（与「智囊团讨论」一致）
**背景**: 用户要求「您的投资问题」标题目前是绿色，改成和「智囊团讨论」一样的颜色。

**实现** (page.js)：ask Card 的 accent 由 var(--bull)（绿）改为 var(--accent)（金），标题与顶部细线一并变金，与讨论卡一致。

**验证**: 浏览器实测两个标题颜色均为 rgb(125,95,31)（#7d5f1f）；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第五十一批）

### 群聊风格切换：移到「召集智囊团」按钮旁（次要控件）
**背景**: 用户反馈风格切换（辩论/探讨/教学）藏在 tab 下方太弱、注意不到，建议放在召集智囊团按钮旁边作为次要功能。

**实现**:
- page.js：删除 ask 卡 tab 下方的 .group-style 下拉块；把「风格」下拉移入 .question-footer、位于 btn-submit 左侧（群聊模式才显示，单聊自动隐藏）
- page.css：删除 .group-style 死样式；新增 .style-pick（次要控件，紧凑小下拉，max-width 200px）

**验证**: 浏览器实测——群聊下「风格」下拉与按钮同一行、位于按钮左侧（p.right<=b.left），选项 辩论/探讨/教学 正常；单聊下不显示风格下拉、按钮仍在；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第五十二批）

### 群聊风格切换：复位回群聊 tab 下方
**背景**: 用户实测后觉得风格下拉放「召集智囊团」按钮旁很奇怪，要求放回群聊 tab 下方（撤销第五十一批的移动）。

**实现**: page.js 移除 footer 内 style-pick，恢复 tab 下方的 .group-style 下拉块；page.css 恢复 .group-style/.style-select 原样式、删除 .style-pick。

**验证**: 浏览器实测——group-style 在 tab 下方，footer 仅剩 btn-submit；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第五十三批）

### 「风格」标签放大加深
**背景**: 用户反馈群聊 tab 下方的「风格」两个字太小（原 9px 灰字），注意不到。

**实现** (page.css .group-style .style-label)：9px mono 灰字 → 12px sans、字重 600、正文深色 var(--text)。

**验证**: 浏览器实测标签 12px/600/深色；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第五十四批）

### 群聊/单聊 tab 与风格下拉宽度优化（去 flex:1 撑满）
**背景**: 用户反馈 tab 太宽、风格下拉选项也很宽。

**实现** (page.css)：.chat-mode-btn flex:1→flex:0 1 auto + min-width 92px + padding 20px；.style-select flex:1→flex:0 1 auto + max-width 340px；.chat-mode-switch justify-content flex-start。

**验证**: 浏览器实测——tab 92px×2（原各~466px）、风格下拉 183px（原 932px 整行）；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第五十五批）

### 暗色：输入框与未选中 tab 填充色对齐页面背景
**背景**: 用户要求暗色下输入框填充色与背景一致（仅靠边框区分）；群聊/单聊未选中 tab 底色也不要突兀的黑色，与背景一致。

**实现** (globals.css)：暗色 `--bg-input: #0d0d18` → `#0a0a0f`（与 --bg-page 一致）。输入框、未选中 tab、mini-btn、气泡等共用该 token 的控件在暗色下全部与背景融合，仅靠边框/阴影/立场条区分；激活 tab 仍为金色。

**验证**: 浏览器实测暗色——question-input 与未选中 tab 背景均为 rgb(10,10,15)=页面背景，边框 rgb(52,52,76) 清晰可见，激活 tab 金色不变；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第五十六批）

### 暗色输入框/tab 融入背景：改为透明填充（修正上批）
**背景**: 上批只把暗色 --bg-input 对齐页面底色，但用户反馈「还是跟原来一样」。诊断：背景上有巴菲特水印/金色光晕，纯色填充仍形成深色块（像素：输入框区 ~11 vs 周围 ~50）。

**决策/实现** (page.css)：暗色下 .question-input/.followup-input/.invite-input/.style-select/.chat-mode-btn:not(.active) 改为 background: transparent，让背景纹理透过来，仅靠边框（--border-subtle）区分。

**验证**: 浏览器实测暗色——这些元素背景均为 rgba(0,0,0,0)，像素采样输入框区由 (11,10,15) 变为 (43-54) 与周围水印区一致；impeccable detect 0 告警；干净构建通过。

**待办**: 无。

## 2026-08-09（第五十七批）

### 成员操作按钮（随机5人/全选/清空）：底色透明，与背景一致
**背景**: 延续暗色「融背景」方向，用户要求这三个按钮未选中时底色不要黑色，与背景一致。

**实现** (src/components/ui.js MiniBtn)：resting background 由 var(--bg-input) 改为 transparent（亮/暗统一），hover 仍为 accent-bg；边框与文字保留，靠边框+图标+文字区分。

**验证**: 浏览器实测暗色——mini-btn 背景 rgba(0,0,0,0)、边框 #2a2a3e、文字浅色；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第五十八批）

### 成员操作按钮：悬停/按下时金色实底
**背景**: 用户反馈三个按钮（随机5人/全选/清空）「选中的时候」仍是透明不好，希望用金色填充。

**实现** (src/components/ui.js MiniBtn)：hover 由淡金底 var(--accent-bg) 改为金色实底 var(--accent)+var(--on-accent) 文字+金描边；新增 pressed 状态（mousedown 时 translateY(1px) 按压反馈，mouseup/mouseleave 复位）。

**验证**: 浏览器实测（鼠标移动模拟悬停）——hover 背景 rgb(125,95,31) 金、文字白、描边金；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第五十九批）

### 输入框占位文字增强
**背景**: 用户反馈输入框内容太弱。此前未设 ::placeholder 样式，浏览器默认半透明占位文字很淡。

**实现** (page.css)：.question-input/.followup-input/.invite-input 的 ::placeholder 显式设为 color var(--text-muted) + opacity 1（浅色 #5c5a54 / 深色 #9b98ad）。

**验证**: 浏览器实测浅色占位颜色 rgb(92,90,84)、opacity 1，清晰可读；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第六十批）

### 占位文字再强化 + 去掉标题区分割线
**背景**: 用户反馈占位文字还是有点弱；标题区与内容区之间的分割线可以拿掉。

**实现** (page.css)：
- 占位文字由 var(--text-muted) 再强化：暗色 #b4b0c4（更亮）、亮色 #4a463f（更深）；并清理一条残留的旧 `.question-input::placeholder{color:var(--text-muted)}` 覆盖规则（此前导致提问框占位没生效）
- 删除 `.header::after` 渐隐金线（标题与内容之间不再有分割线）

**验证**: 浏览器实测——暗色占位 rgb(180,176,196)、亮色 rgb(74,70,63)；header::after content=none（已移除）；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第六十一批）

### 默认主题改为暗色
**背景**: 用户要求把暗色作为默认背景（此前默认亮色）。

**实现**:
- src/app/layout.js 首帧内联脚本：无存储偏好/异常时 t='light'→'dark'
- src/app/page.js：useState('light')→useState('dark')（SSR 与首帧一致，挂载后仍会读 localStorage 覆盖）

**注意**: CSS 的 :root 本就是暗色定义，无需改；若用户浏览器里存过 'light' 偏好，会覆盖默认仍显示亮色（可在站点设置里切一次暗色即可覆盖存储）。

**验证**: 浏览器实测加载即 data-theme=dark、切换按钮显示☀️；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第六十二批）

### 背景图更换：巴菲特头像 → 巴菲特+芒格授课场景
**背景**: 用户提供一张百度图片（新浪源图 1080×603 PNG，巴菲特+芒格授课）要求替换当前巴菲特头像背景。

**实现**:
- 下载图片到 public/bg-debate.png（1080×603，400KB）
- page.js：bg-master-layer 由 invMap['buffett'] 头像改为直接引用 /bg-debate.png；水印滤镜/透明度/遮罩不变

**验证**: 浏览器实测——bg 层 src=/bg-debate.png、natural 1080×603、滤镜/透明度正常；impeccable detect 0 告警；干净构建通过。

**待办**: 若用户希望调整新图的清晰度/位置，可再调。

## 2026-08-09（第六十三批）

### 暗色主题金色压低：亮黄 #c9a84c → 暗金 #a5832a
**背景**: 用户反馈群聊 tab 选中态、召集智囊团按钮是亮黄色太亮，要求压低亮度。

**决策**: 暗色主题金色整体压低一档（不只改两个元素，避免金色表面/文字不一致）：--accent #c9a84c→#a5832a，同步 --neutral、--accent-dim、--accent-bg；浅色主题不变（本就深金 #7d5f1f）。书房琥珀光晕保持原暖色。

**验证**: 浏览器实测暗色——accentToken #a5832a，activeTab 与 btn-submit 背景均 rgb(165,131,42)、深字（对比 ~5.6:1 可读）；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第六十四批）

### 删除卡片顶部亮黄色渐变线（card-accent）
**背景**: 用户反馈「选择成员」「您的投资问题」标题正上方有亮黄色辐条，要求删除。

**实现**: Card 组件（ui.js）删除 .card-accent 顶部渐变线 div；page.css 删除 .card-accent 规则。三个区块（选择成员/您的投资问题/智囊团讨论）顶部亮线全部移除。

**验证**: 浏览器实测 .card-accent 数量 0、标题正常；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第六十五批）

### ① 群聊样式：讨论区改群聊 UI（第一阶段）
**背景**: 用户提出把讨论区做成群聊样式，支持插话/@大师（已评估，分三阶段）；本批做第一阶段「群聊样式」。

**实现**:
- page.css：
  - 大师消息：气泡化（speech-content 圆角 4px 14px 14px 14px、inline-block 随内容、去掉左边立场色条 2px）；名字 13px 800、立场改胶囊标签
  - 自己的消息：user-followup 右对齐金色气泡（14px 4px 14px 14px，bg accent + on-accent），隐藏 label
  - 主持人开场/结尾：居中系统消息胶囊（999px 圆角，小字 muted）
  - 正在输入：气泡样式（圆角 4 14 14 14 + 名字）
  - 聊天输入框：followup-input/btn-followup 改胶囊（999px）
  - 讨论间距 28→16
- page.js：两处 speech-content 去掉 style borderLeftColor（气泡不再有立场色条）

**验证**: 真实辩论（7 位大师）实测——hostOpening 胶囊、4 个 speech 气泡圆角 4/14、无 2px 左边条、typing 气泡出现；追问后 user-followup 右对齐（flex-end）、金色气泡 rgb(165,131,42) 圆角 14/4；impeccable detect 0 告警；干净构建通过。

**待办**: ② @大师、③ 插话 —— **用户决定暂不做**（2026-08-09 明确，先搁置）。时间戳可作后续增强。

## 2026-08-09（第六十六批）

### 可读性修复：暗色次要文字去紫 + 弹窗底色提亮
**背景**: 用户反馈①邀请大师弹窗底色与背景分不开、字看不清；②全站除白色外还有淡紫色文字看不清。

**诊断**: ①暗色弹窗用 --bg-card #10101a，与压暗后背景太接近；②淡紫色字=暗色 --text-muted #9b98ad（灰紫、偏暗）。

**实现** (globals.css 暗色)：
- --text-muted #9b98ad → #b3ada0（暖灰，去紫色调、更亮，标签/提示/说明等全站次要文字统一生效）
- --bg-card #10101a → #171726（弹窗/浮层底色提亮，与遮罩背景拉开；页面卡片早已透明不受影响）

**验证**: 浏览器实测暗色——邀请弹窗底色 rgb(23,23,38)、标签字 rgb(179,173,160)（对比 ~8:1）、像素采样弹窗中心(27,26,38) vs 遮罩背景(17,15,10) 明显浮出；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第六十七批）

### 邀请弹窗底色再提亮
**背景**: 用户反馈暗色弹窗底色还是太深（上批 #171726 仍不够）。

**实现** (globals.css 暗色)：--bg-card #171726 → #212130（弹窗/浮层明显亮于背景）。

**验证**: 浏览器实测暗色——弹窗中心像素 (37,36,48) vs 遮罩背景 (17,15,10)，差距 ~20 明显浮出；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第六十八批）

### 暗色弹窗改白底深字（用户最终拍板）
**背景**: 暗色弹窗底色先后试过 #10101a/#171726/#212130，用户仍觉得太深看不清，最终要求「用白色」。

**实现** (page.css)：`[data-theme="dark"] .modal-content` 作用域内整体切换到亮色配色（--bg-card #fffef9、--text #1a1915、--text-muted #5c5a54、--accent #7d5f1f、--on-accent #fff、--bg-input #faf8f3 等），白底深字，所有弹窗（邀请/历史/资料/海报/补充）统一生效。

**验证**: 浏览器实测暗色——邀请弹窗底 rgb(255,254,249) 白、标题深色、标签深灰、主按钮深金白字；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第六十九批）

### 默认主题改回亮色
**背景**: 用户要求默认背景还是亮色（撤销第六十一批改暗色默认）。

**实现**: layout.js 首帧脚本 t='dark'→'light'（含 catch 兜底）；page.js useState('dark')→useState('light')。

**验证**: 浏览器实测加载即 data-theme=light；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第七十批）

### 风格下拉箭头右边距优化
**背景**: 用户反馈下拉箭头贴右缘太紧。

**实现** (page.css .style-select)：原生箭头改自定义 SVG 下拉箭头（appearance:none），background-position right 12px center（距右缘 12px），padding-right 9-10px→36px（文字不挤箭头）；暗色透明覆盖由 background 改为 background-color，避免抹掉箭头。

**验证**: 浏览器实测亮/暗——均含自定义箭头、箭头距右缘 calc(100%-12px)、右 padding 36px；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第七十一批）

### 成员操作按钮外框线加强
**背景**: 用户反馈选择成员的三个操作按钮外框线太弱看不清。

**实现** (ui.js MiniBtn)：resting borderColor var(--border)→var(--text-muted)（亮色 #5c5a54 / 暗色 #b3ada0），外框明显但不抢眼；hover 仍是金色。

**验证**: 浏览器实测亮/暗外框色分别为 rgb(92,90,84)/rgb(179,173,160)，清晰可见；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第七十二批）

### 成员操作按钮外框改为与「邀请一位大师」一致的金色描边
**背景**: 用户要求三个操作按钮外框线用和邀请大师按钮一样的样式。

**实现** (ui.js MiniBtn)：resting borderColor var(--text-muted)→var(--accent-dim)，与 .invite-entry 外框完全一致（亮 rgba(125,95,31,.35) / 暗 rgba(165,131,42,.30)）。

**验证**: 浏览器实测亮/暗 miniBtn 与 inviteEntry 外框色完全一致；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第七十三批）

### 风格下拉改自定义面板（消除暗色系统紫色）
**背景**: 用户指出暗色下风格下拉的选项面板底色发紫——那是操作系统原生 select 弹出面板，CSS 无法控制。

**实现**:
- page.js：新增 StyleDropdown 组件（自绘按钮 + 下拉面板，点击外部/Escape 关闭，选中打 ✓），替换原生 <select>
- page.css：删除 .style-select 规则与暗色覆盖引用；新增 .style-dropdown* 样式（面板用 var(--bg-card)：亮=白、暗=深灰 #212130）

**验证**: 浏览器实测暗色——面板背景 rgb(33,33,48) 深灰（非紫），选项 辩论✓/探讨/教学 正常，选「教学」后面板关闭、值更新；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第七十四批）

### 暗色中性色整体暖化：根治蓝紫调
**背景**: 自定义下拉面板后用户反馈暗色下面板仍偏紫——根因是暗色中性色本身偏蓝（#212130 等 B>R），任何面板都有紫调。

**实现** (globals.css 暗色)：中性色整体改暖——bg-page #0d0b08、bg-card #242019、bg-input #0d0b08、border #3a362d、border-subtle #474236、text #eae3d3、header-bg 暖棕；accent 维持暗金。

**验证**: 浏览器实测暗色——下拉面板 rgb(36,32,25)（R>G>B 暖棕、无紫）、页面底 rgb(13,11,8) 暖炭；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第七十五批）

### 圆角统一：两档体系（小控件 6px / 卡片浮层 10px）
**背景**: 用户反馈圆角太杂（原混用 4/6/8/12px）。

**实现**: page.css + masters.css 统一——小控件（输入框/按钮/tab/下拉项/提示/错误框/成员行等）4px→6px；卡片/弹窗/浮层（modal、qr、history、invite-entry、persona、solo-target、verdict、poster-img、master-hero 等）8px/12px→10px。保留：胶囊 999px、圆形 50%、聊天气泡非对称 4/14、细进度条 4px。另给 masters.css 引言色条补 impeccable 豁免注释。

**验证**: 浏览器实测小控件 6px、入口 10px；检测器 0 告警；干净构建通过。

## 2026-08-09（第七十六批）

### 聊天气泡圆角统一为 14px
**背景**: 用户发现嘉宾发言气泡左上角圆角(4px)与其他三角(14px)不一致。

**说明**: 那是聊天气泡惯例（贴近头像的小圆角如「尾巴」），但用户觉得不统一。

**实现** (page.css)：.speech-content / .user-followup p / .typing-indicator-body 全部改为 border-radius: 14px（四角一致）。

**验证**: 样式表实测三处均 14px；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第七十七批）

### 圆角再统一：邀请按钮=随机5人；提问框=发言气泡
**背景**: 用户要求「邀请一位大师」(10px) 与「随机5人」(6px) 圆角统一；提问输入框圆角与大师回答区一致。

**实现** (page.css)：invite-entry 10→6px（归入按钮档，与 mini-btn 一致）；question-input 6→14px（与 speech-content 一致）。

**验证**: 浏览器实测 invite-entry=mini-btn=6px、question-input=speech-content=14px；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第七十八批）

### 风格下拉选项折行修复
**背景**: 用户反馈下拉选项选中时折行。

**实现** (page.css)：.style-dropdown-menu 改 width:max-content + max-width 380px（面板随内容变宽不再挤压）；option/label/hint 加 white-space:nowrap 单行，hint 超长省略号。

**验证**: 浏览器实测三选项均单行（高 33px），面板宽 179px 贴合内容；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第七十九批）

### 群聊/单聊 tab 与风格行右缘对齐
**背景**: 用户要求拉宽 tab 按钮，使单聊按钮右边距与下方风格下拉右边距对齐（原差 13px）。

**实现** (page.css)：.chat-mode-switch 与 .group-style 统一 width:200px；.chat-mode-btn 改 flex:1 均分（去掉 min-width 92）；.style-dropdown 改 flex:1 填充（去 max-width）。

**验证**: 浏览器实测单聊按钮右缘=风格下拉右缘=524px（间隙0），两行均 200px；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第八十批）

### 风格按钮默认内容显示不全修复
**背景**: 用户反馈风格按钮默认（辩论）显示不全（提示文字被省略号截断）。

**原因**: 200px 行宽下按钮仅 165px，完整内容需 ~170px。

**实现** (page.css)：chat-mode-switch 与 group-style 宽 200→220px；风格下拉按钮 185px，完整显示「辩论 · 真实互怼 · 针锋相对」。

**验证**: 浏览器实测 hintOverflow=false（完整显示）、右缘对齐保持（间隙0）；impeccable detect 0 告警；干净构建通过。

## 2026-08-09（第八十一批）

### 邀请大师弹窗移除「补充说明（可选）」
**背景**: 用户要求删除补充说明输入框（此前已删真实语录），弹窗只剩名字输入。

**实现** (page.js)：删除 inviteHint state/请求字段/重置/依赖引用 与 label+textarea；后端本就对缺失 hint 容错，无需改。

**验证**: 浏览器实测弹窗仅剩「人物名字/昵称」1 个输入框、0 textarea；impeccable detect 0 告警；干净构建通过。

## 模板（新增记录时使用）

```markdown
## YYYY-MM-DD

### 标题
**背景**: 为什么要做这件事

**决策**: 做了什么决定

**实现**: 具体怎么做的

**影响**: 对项目有什么影响

**待办**: 还有什么要做的
```

## 2026-08-09（第八十二批）

### 邀请大师：快捷选择知名人物
**背景**: 用户要求把「公开资料多/发言多/知名度高」的国内投资人物做成名单，封装进邀请大师模块，提供快捷点选，省去手打名字。

**决策**: 新建 src/data/quickPicks.js，按身份分 5 组共 28 人（公募基金经理/私募大佬/游资短线高手/知乎自媒体大V/财经KOL评论员）；弹窗表单输入框下方加「或从知名人物中快捷选择」区，点击人物 chip = 自动填名 + 立即触发 handleInvite(name) 开始构建。刻意不收徐翔等有法律污点人物；寒武纪的鳄鱼（已退乎）保留，hint 标注「已退乎」。

**实现**:
- src/data/quickPicks.js：QUICK_PICK_GROUPS = [{id, label, people:[{name, hint}]}]
- src/app/page.js：import quickPicks；handleInvite 支持 (nameOverride) 参数（点选时直接传入，避免 setState 异步导致取不到新值）；弹窗 form 阶段插入 .invite-quick 区块
- src/app/page.css：.invite-quick 分组布局 + 圆角 6px chip（沿用小控件圆角体系），hover 金色描边+底色；禁用态降透明

**验证**: playwright 实测——打开弹窗快捷区 5 组齐全；点击「张坤」→ 自动填名并进入 ①检索→②提炼→③构建 流程 → 生成「中国价值投资领军人物」档案卡（检索到百度百科/天天基金等真实资料）；亮/暗主题下弹窗白底深字、文字行分布正常；impeccable detect 0 告警。

**待办**: 名单可继续扩充；如需给人物加固定头像/更细的画像种子，后续可升级为「预置知名人物库」而非仅快捷填名。

## 2026-08-09（第八十三批）

### 邀请大师快捷选择：分组排序 + 浮层化 + 预填充
**背景**: 用户反馈三点：①游资、知乎大V 应排前 2 位；②居中弹窗屏效低，改用浮层；③快捷选择不应一点就构建，只做预填充，提交仍由用户手动点。

**决策**:
- 分组顺序改为：游资/短线高手 → 知乎/自媒体大V → 公募 → 私募 → 财经KOL
- 邀请大师从居中 modal 改为**锚定入口按钮的浮层面板**（invite-popover）：fixed 定位在按钮下方、maxHeight 动态适配视口不超出底部、透明 backdrop 点击外部关闭、背景页面保持可见（屏效提升）；窄屏贴边全宽
- 快捷人选点击 = 只填名字 + 聚焦输入框 + chip 高亮（.on），不再自动调 handleInvite；用户点「开始构建」手动提交

**实现**:
- src/data/quickPicks.js：QUICK_PICK_GROUPS 顺序重排
- src/app/page.js：新增 invitePos/inviteBtnRef/inviteInputRef + updateInvitePos（滚动/缩放跟随）；弹窗结构 modal-overlay→backdrop+popover；快捷 chip onClick 仅 setInviteName+focus；handleInvite 的 nameOverride 加 typeof 防御；按钮 onClick 包箭头函数（避免 React 把 event 当 nameOverride 传入导致 .trim 报错——这是本批发现并修复的 bug）
- src/app/page.css：.invite-backdrop/.invite-popover（含入场动画、动态 maxHeight）、暗色变量覆盖扩展到 popover（白底深字）、窄屏贴边、chip 选中态

**验证**: playwright 实测——预填后按钮从 disabled→可用；点开始构建进入①检索→②提炼→③构建→档案卡→确认加入全流程；暗色主题下 popover 白底 rgb(255,254,249)/深字、页面背景深色可见；像素采样浮层白底 93%、浮层下方仍是页面背景（非全屏遮罩）；impeccable 0 告警。

**待办**: 无新增；此前待办不变（推送部署、线上验证行情注入）。

## 2026-08-09（第八十四批）

### 邀请大师改为抽屉式侧边面板（用户明确指定形态）
**背景**: 上一批把邀请弹窗改成了"锚定按钮的浮层面板"，用户反馈"这还是弹窗，浮层不是这种的"，随后明确"我要的是抽屉式的侧边面板那种"。

**决策**: 完全按用户指定形态——右侧滑入的抽屉（drawer）：
- 固定 right:0、全高 100dvh、宽 min(480px, 100vw)，从右侧滑入动画
- 半透明遮罩 rgba(10,9,7,0.45)：背景可见但变暗，点击遮罩关闭（标准抽屉交互）
- 移除上一批的锚定定位逻辑（invitePos/inviteBtnRef/updateInvitePos/滚动跟随）
- 头部 sticky 防滚动时标题跑掉；窄屏抽屉全宽
- 快捷选择预填 + 手动提交的行为保持不变

**实现**: page.js 移除 invitePos/updateInvitePos 及按钮 ref；JSX modal-overlay→invite-drawer-backdrop+invite-drawer；page.css 新增抽屉样式与入场动画，暗色变量覆盖扩展到 .invite-drawer（白底深字），响应式全宽。

**验证**: playwright 实测——抽屉 left=800/right=1280/w=480/h=720（右侧全高）、遮罩 rgba(10,9,7,0.45)、动画 drawer-in；像素采样抽屉白底 87%、左侧背景被压暗、x=800 边界清晰；预填/手动提交/构建阶段/遮罩关闭全部正常；impeccable 0 告警。

**待办**: 无新增；此前待办不变（推送部署、线上验证行情注入）。

## 2026-08-09（第八十五批）

### 邀请抽屉：操作按钮吸底，人物选择区获得更大空间
**背景**: 用户要求「开始构建/取消」按钮吸底（固定底部），给人选区留更大展示空间。

**决策**: 把邀请抽屉改成标准「头部固定 + 内容区滚动 + 底部按钮吸底」布局：
- .invite-drawer 改 flex column + overflow hidden
- 内容包进 .invite-drawer-body（flex:1 + overflow-y auto + min-height:0），可滚动
- 按钮移到 .invite-drawer-footer（flex-shrink:0 + 上边框分隔 + 白底），仅 form/preview/added 阶段渲染（building 阶段隐藏空 footer）
- 去掉 .invite-quick-groups 的 max-height:34vh 内滚限制，改由整个 body 滚动——人物选择区从约 34vh 扩大到近满高

**实现**: page.js 把 form/preview/added 的按钮区从内容中抽出，统一放入 invite-drawer-footer 按 phase 条件渲染；page.css 抽屉改为 flex column 布局，新增 body/footer 样式。

**验证**: playwright 实测——drawer 0~720 全高；body 62~653（591px 可滚，scrollHeight 748>clientH 591）；footer 653~720 吸底；body 滚到底 footer 仍钉 720；form 阶段 footer=[取消,开始构建]、预填后开始构建变金色 rgb(125,95,31) 且底部距抽屉底 16px；building 阶段 footer 隐藏；preview 阶段 footer=[重新构建,确认加入] 吸底 720；impeccable 0 告警。

**待办**: 无新增；此前待办不变（推送部署、线上验证行情注入）。

## 2026-08-09（第八十六批）

### 邀请大师：输入框提示文案弱化 + 28 位人物预置画像（秒出档案卡）
**背景**: 用户两点需求：①「人物名字/昵称」输入框的 placeholder 提示文案色调太显，要弱化；②希望快捷选择里的大V提前预置，用户构建时速度很快。

**决策**:
- placeholder 只弱化邀请输入框（不随提问区/追问区）：亮色 #4a463f→#a8a295（暖灰），暗色 #b4b0c4→#c6c0b4；用后定义覆盖拆出 invite-input 专属规则
- 新建 src/data/presetMasters.js：28 位快捷人物全部预置成完整画像（字段与 virtual-master 返回一致，source:'preset'），覆盖游资5/知乎7/公募6/私募7/KOL3
- handleInvite 命中预置 → 本地秒出档案卡（跳过全网搜索+LLM 生成），sources 显示"预置画像（基于公开资料整理…）"
- chip 上加「⚡即选」金色小徽标提示预置
- prompts.js masterProfileLine 支持 source==='preset' 也带风格示范（styleSample 入对话）

**实现**: presetMasters.js 新建（28 人画像）；page.js import + chip 徽标 + handleInvite 预置短路；page.css placeholder 覆盖 + .iqc-preset 徽标样式；prompts.js source 判断放宽。

**验证**: playwright 实测——chip 均带⚡即选；点炒股养家→开始构建 1 秒内直达档案卡（无 building 等待，金句/来源正确）；确认加入后进成员列表；非预置（段永平）仍走在线①检索→②提炼→③构建；亮色 placeholder rgb(168,162,149)/暗色 rgb(198,192,180)，提问区保持原色；impeccable 0 告警。

**待办**: 预置画像基于公开资料整理，如需更精准可点「重新构建」在线生成；后续可按需扩充更多预置人物。

## 2026-08-09（第八十七批）

### 构建配方（B方案）试点：寒武纪的鳄鱼
**背景**: 与用户讨论后确认分层方案（预置JSON底座 + 配方中间层 + 通用流水线兜底），用户同意先用"最难构建"的人物（寒武纪的鳄鱼）试点 B 配方。

**决策**: 新建 src/data/recipes.js 配方库。命中配方时构建接口走定制流程：
- 多关键词检索（queries）：不只搜昵称，还搜「退乎/删号」「周期股」「网友评价」等
- 噪音过滤（noise）：排除同名"中科寒武纪"公司行情/财报、字典、地质学内容
- 相关性排序：标题/摘要含完整昵称的排最前（防同名抢占）
- 人工校准背景（hint）：作为最高可信度上下文喂给模型，明示"此人已退乎、资料以网友讨论为主、勿与芯片公司混淆"

**实现**: recipes.js 新建；virtual-master route.js——searchWeb 支持 queries+noise+相关性排序，POST 按名字查配方，corpus 注入 hint（LLM 简报兜底也带上）；page.js——handleInvite 加 forceOnline 参数，「↺ 重新构建」强制走在线（修复预置短路 bug：原来预置人物点重新构建会被前端秒出拦截，根本到不了配方）。

**验证**: 多次端到端 curl——画像稳定识别"知乎大V/周期股/有色资源/2026年5月删号退乎"，风格示范还原口头禅"哥几个，别急"；噪音过滤+相关性排序后来源以真实知乎讨论帖为主（"如何评价…删号退乎""说下寒武纪的鳄鱼的投资思想"），公司/字典/地质噪音被滤除；浏览器实测预置秒出→重新构建强制走①检索→②提炼→③构建→新档案卡落地；impeccable 0 告警。

**待办**: 配方库可继续扩（后续可给其他易错人物如 Dang大/DeepVan 配）；搜索源稳定性受 bing/百度 返回质量波动影响（偶发只拿到 1-2 条来源），可考虑后续加第三引擎或失败重试。

## 2026-08-09（第八十八批）

### 配方构建性能优化：并行搜索 + 搜狗第三引擎 + 噪音v3
**背景**: 跑鳄鱼配方画像时暴露两个问题：①多关键词串行搜索（4词×2引擎=8请求）导致构建 45~99 秒、偶发超时；②搜索结果质量波动，偶发只拿到"韩寒/字典/影视剧"噪音，画像质量仍稳但来源列表难看。

**决策**:
- searchWeb 改为 Promise.all 并行（所有 query×引擎同时发起），构建耗时降到 10~16 秒
- 抓正文改为并行：前 4 篇候选同时抓，取最先成功的 2 篇
- 新增搜狗（sogou）第三搜索引擎 + parseSogou 解析器，提高命中率
- 噪音 v3：recipes.js 的 noise 正则补充 MLU/股东/投资机构/动物/签约/退文/中芯/零售/影视等词（过滤"寒武纪"公司、古生物、电视剧《寒武纪》内容）

**实现**: virtual-master route.js——searchWeb 并行化+三引擎+相关性排序；recipes.js noise 强化。

**验证**: 正则单测——公司/影视/字典/地质噪音全部 FILTERED，人物相关帖 keep；端到端 curl 一次成功——画像稳定（称号"周期猎手"、金句"周期不是用来预测的，是用来应对的"、风格示范高度贴合周期股老炮"资本开支→库存→价格信号"推演），来源 #1 为知乎真实讨论帖；浏览器实测构建无超时；impeccable 0 告警。

**待办**: 搜索源仍偶发混入影视/公司噪音（相关性排序已把人物帖顶到 #1），后续可给特定 query 加站内限定（如 site:zhihu.com 之外的转载站）或对 sources 做二次过滤展示。

## 2026-08-09（第八十九批）

### 构建方法现状与反爬调研结论（用户要求记录，后续再优化）
**背景**: 用户反馈鳄鱼画像风格仍有偏差，追问"能否让 AI 绕过反爬抓正文"。本批对全部免费绕过路径做了实测，并确认后续优化方向。用户拍板：付费方案与用户提供资料都排除，当前方法先记录留档。

**当前构建方法（三层）**:
1. 预置 JSON（presetMasters.js，28人）：本地秒出档案卡，0 token，覆盖高频人物
2. 配方在线构建（recipes.js + virtual-master）：命中配方走定制多关键词检索（含 site: 站内限定、反向验证词、年份拆分、噪音过滤、相关性排序、人工校准 hint），搜索+抓正文已并行化（10~16s）
3. 通用流水线兜底：未命中配方的陌生人物走默认"搜索→简报→建模"

**反爬实测结论（2026-08-09，当前网络环境）**:
- 直接 HTTP 抓知乎：403 + JS 加密签名（zh-zse-ck）❌
- 无头浏览器渲染知乎：40362 风控（要求登录/摇一摇）❌
- jina reader 渲染代理：HTTP 000 连不上 ❌
- Bing 搜索/快照：302 循环 / 连接重置 / 网络挂起 ❌
- 百度：200 但对该人物无索引（只返回古生物"寒武纪"）❌
- 搜狗：弹验证码 ❌；360：空结果 ❌；GitHub API：无相关仓库 ❌
- **根因**：人物已退乎原文消失 + 知乎/雪球主动对抗机器 + 当前网络对国外搜索引擎不稳定，三层叠加

**关键事实**：v2 构建曾成功（称号"周期价值守望者"、挖到真名"陈泽鑫"、央企/省国资委选股三原则），靠的是**搜索引擎恰好返回的知乎/雪球标题+摘要**，说明"摘要路径"可行但运气成分大、不稳定。

**已讨论/未落地的优化思路**:
- 多次采样构建：重新构建自动跑 2~3 次，合并去重摘要再喂模型（免费，+10~30s）
- 已知线索反查：把 v2 挖到的确切标题（如「寒武纪的鳄鱼：一位中国式价值投资者的思想体系」）当精确搜索词反查
- 提示词升级：借鉴"舆情研究总监"思路——素材分层（本人原话 vs 他人评价）、原话带引号引用、信源分层解构（粉丝/路人/黑粉/同行）、时间线矛盾标注、不脑补（personaPrompt 已部分升级，输出格式不变）
- 稳定优先：抓不到就少用几条，宁朴素不脑补
- 付费方案（博查/Tavily 等搜索 API）与用户提供资料，**用户明确排除**

**待办（下次优化）**: ①多次采样+已知线索反查落地；②配方库扩充（Dang大/DeepVan 等半匿名人物）；③搜索源稳定性（网络环境改善后再验证 Bing/jina）；④鳄鱼画像最终校准需用户确认"哪些细节像/不像"。

## 2026-08-09（第九十批）

### 风格下拉箭头强化：金色圆底 + SVG 白色箭头
**背景**: 用户反馈风格选择的下拉箭头太弱（原来用纯文本 ▾，10px + 弱灰），看不出可以下拉。

**决策**: 改成"金色圆形底 + 白色箭头"的明显下拉指示器，贴合项目金色 accent 风格：
- 20×20 圆形，accent-dim 半透明金底 + accent 金色箭头（常态）
- hover：实心金底 + 白色箭头（明显可点）
- 展开：箭头旋转 180°
- 用 SVG polyline 替代文本字符，粗细可控、不受字体影响

**实现**: page.js caret 由 `▾` 文本改为 inline SVG；page.css .style-dropdown-caret 重写（圆形底、flex 居中、transition），hover 态加深。

**验证**: playwright 实测——常态 20×20 金底金箭头；真实鼠标 hover 后背景变实心 rgb(125,95,31)、箭头变白；展开态菜单打开且箭头 rotate 180°（matrix(-1,0,0,-1,0,0)）；像素采样确认中心白色笔画+四周金色圆底；impeccable 0 告警。

**待办**: 无新增；此前待办不变（构建方法优化、推送部署等）。

## 2026-08-09（第九十一批）

### 风格下拉箭头：回退金色圆底，恢复文本 ▾ 仅放大
**背景**: 用户反馈上一批做的"金色圆底+SVG白箭头"太丑，要求恢复原来的下拉 icon，只放大即可。

**决策**: 回退金色圆底方案，恢复原文本字符 ▾，字号 10px→16px（仍保留展开旋转），颜色保持原 text-muted 弱灰。

**实现**: page.js caret 恢复为 `▾` 文本；page.css 删除金色圆底样式，改回 `font-size:16px; line-height:1`。

**验证**: playwright 实测——▾ 16px、text-muted、无 SVG、展开旋转 180° 正常；impeccable 0 告警。

**待办**: 无新增。

## 2026-08-09（第九十二批）

### 群聊/单聊切换 + 风格下拉：移入提问输入框左下角作为辅助选项
**背景**: 用户反馈「群聊/单聊」「风格切换」堆在输入框上方一行，页面显得碎、缺乏一体化感，希望作为辅助选项放在输入框左下角。

**决策**: 把模式切换、风格下拉（及单聊对象提示）从提问卡输入框上方移入 question-footer 左下角，与右下角「召集智囊团」主按钮分列两端：
- 输入框上方只留卡片标题「您的投资问题」，清空控件
- footer 改为 space-between：左 .question-aux（群聊/单聊紧凑按钮 + 群聊时风格下拉 / 单聊时对象胶囊），右 btn-submit
- 控件紧凑化：模式按钮 padding 11px→6px、字号 13→12；风格下拉去掉「风格」label 与 220px 整行约束；单聊对象从整行虚线框改为 999px 胶囊（头像 22px）

**实现**: page.js 移动结构；page.css .question-footer 两端布局 + .question-aux、模式/风格/单聊对象紧凑样式。

**验证**: playwright 实测——辅助区 y261 位于输入框底部 y244 之下（左下角）、按钮 x1125 在右侧；像素采样辅助区/按钮区金色均存在、输入框上方无多余控件；暗色主题 active 按钮金色底可读；单聊模式胶囊提示正常；impeccable 0 告警。

**待办**: 无新增。

## 2026-08-09（第九十三批）

### 输入框透明 + 群聊/单聊改Switch + 风格加"讨论风格"文案
**背景**: 用户三点要求：①输入框颜色不要白/米白，用透明；②群聊/单聊从 tab 切换改为 Switch 开关；③风格处带文案"讨论风格"。

**决策**:
- question-input 背景改 transparent（暗色本就透明，亮色原为 #faf8f3 米白 → 统一透明，靠边框区分）
- 群聊/单聊 tab 改为 Switch 开关：左"群聊"文字 + 中间 switch（38×21 胶囊 + 15px 圆thumb）+ 右"单聊"；aria-checked 控 thumb 位移（2px→19px）与金色；当前模式文字金色高亮
- 风格下拉前加回 label「讨论风格」

**实现**: page.js question-aux 内 chat-mode-switch/btn 替换为 chat-switch-wrap/cs-label/chat-switch/chat-switch-thumb，group-style 加 style-label；page.css 对应新增 switch 样式、输入框 background:transparent、暗色规则 .chat-mode-btn→.chat-switch。

**验证**: playwright 实测——初始群聊 thumb 左 2px 灰；点开关切单聊 thumb 右 19px 金色、单聊文字高亮、solo-target 显示；切回群聊「讨论风格」在、thumb 回位；暗色下 switch/输入框均 transparent 靠边框区分；像素采样暗色输入框区域 (28,26,22) 与背景一致（非白）；impeccable 0 告警。

**待办**: 无新增。

## 2026-08-09（第九十四批）

### 输入框可见性微调 + 删除「讨论风格」文案
**背景**: 用户反馈上一批全透明后输入框"看不出来了"，要求清晰一点；同时删掉「讨论风格」文案。

**决策**:
- 输入框从全透明改为柔和半透明浅底：亮色 rgba(255,255,255,0.5)（透出背景纹理、非死白）、暗色 rgba(255,255,255,0.045)（微亮，与卡片背景区分），边框由 border-subtle 提升为 var(--border)（更实）
- 删除 group-style 里的「讨论风格」label（及对应 CSS）

**实现**: page.css question-input 背景/边框调整、暗色覆盖规则由 transparent 改为 rgba 微亮、清理 style-label 规则；page.js 删除 style-label 节点。

**验证**: playwright 实测——亮色 inputBg rgba(255,255,255,0.5)/边框 rgb(212,207,196)、暗色 rgba(255,255,255,0.043)/边框 rgb(58,54,45)；像素采样暗色输入框内部 (45,42,38) 明显亮于卡片 (27,24,19)（清晰可辨）；styleLabel 已消失；impeccable 0 告警。

**待办**: 无新增。

## 2026-08-09（第九十五批）

### 删除辩论/探讨/教学模式选择，固定辩论模式
**背景**: 用户要求"辩论、探讨、教学这几个模式直接删掉，默认就是辩论模式"——彻底移除风格切换，不再提供选择。

**决策**:
- 删除 UI 上的风格下拉（StyleDropdown 组件、group-style 容器、样式）
- 删除 page.js 的 mode state、MODES import、所有 prompt 调用的 mode 参数
- prompts.js 删除 MODES 导出与 explore/teach 分支，固定辩论基调（MODE_RULES + 各 prompt 内联辩论文案）
- 侧栏提示文案"选一种风格"→"开启辩论"（中英双语）
- buildChatPrompt 单聊也去掉 mode 分支

**实现**: page.js（删组件/state/import/调用参数/JSX）；prompts.js（删 MODES、explore/teach，固定 debate）；page.css（删 style-dropdown/group-style 样式）；i18n messages 文案更新。

**验证**: node 直接调用全部 6 个 prompt 函数正常（固定辩论基调）；浏览器实测——风格下拉与 group-style 容器已消失、辅助区只剩群聊/单聊 switch、文案"开启辩论"生效；全仓 grep 无 MODES/StyleDropdown 残留（仅 DEEPSEEK_MODEL 环境变量无关）；impeccable 0 告警。

**待办**: 无新增。

## 2026-08-09（第九十六批）

### 单聊辅助胶囊移除 + 邀请浮层去「⚡即选」+ 海报折行修复与页脚删除
**背景**: 用户三点：①单聊模式下旁边不需要姓名头像；②邀请浮层快捷人选不要"即选"字样（信息冗余）；③海报还有折行，底部网址+风险说明可删。

**决策**:
- 删除 question-aux 里的 solo-target 胶囊（头像+姓名/提示），单聊辅助区只剩群聊/单聊 switch
- 删除邀请 chip 的「⚡即选」徽标（JSX + CSS）
- 海报：删除底部页脚「yieldglide.com · 日期 · 仅供研究参考，不构成投资建议」
- 海报折行根因修复：正文测量用常规字体 `400 26px`，但绘制 drawRichLine 数字用粗体 `700 26px`，粗体更宽导致实际绘制超出测量宽度→含数字的行溢出卡片框线/折行错乱。修复：wrapLines 支持富文本感知测宽（opts.boldDigits/boldFont，数字 token 用粗体测），contentLines 测量传入与绘制一致的字体
- 顺带健壮性：wrapLines 英文单词（含 / . - %）聚成原子 token 不拆行；新行首空白/标点清理

**实现**: page.js（删 solo-target 块、iqc-preset 徽标）；page.css（删 iqc-preset 样式）；poster.js（删页脚、wrapLines 富文本测宽+英文聚合+行首清理、measureLines 透传 opts、contentLines 传入 boldDigits）。

**验证**: playwright 实测——单聊辅助区只剩 switch；邀请 chip 无「⚡即选」；海报像素分析：卡片区 133 文字行最右 x=962 < 卡片右缘 995，无一行超出（修复前含数字行会超出）；左右边缘 0 文字侵入；底部 1500-1600 无页脚文字；二维码说明保留；impeccable 0 告警。

**待办**: 无新增；此前待办不变。

## 2026-08-09（第九十七批）

### 移除「查看大师主页」功能
**背景**: 用户要求把查看大师主页功能移除掉（此前该入口点击会 404，属于废弃功能）。

**决策**: 彻底移除整个大师主页功能：
- 删除 MasterProfileModal 里非自定义大师的「查看大师主页 →」Link 及 Link import；操作区改为仅自定义大师显示「从智囊团移除」
- 删除整个 /masters 路由（列表页 + [id] 详情页 + masters.css）
- sitemap 简化为仅首页
- 删除 page.css 的 profile-page-link 样式
- 保留 URL 查询参数 ?masters=id（独立选人功能，非主页路由）

**验证**: 浏览器实测——大师详情弹窗只剩「与 TA 单聊」按钮、无主页链接；/masters 返回 404；impeccable 0 告警。

**待办**: 无新增。

## 2026-08-09（第九十八批）

### 整体代码 Review（提交前检查）
**背景**: 用户要求整体 review 代码质量与改进点，确认没问题后更新 memory（过几天要提交代码）。

**Review 结论（代码质量良好）**:
- 32 位预置大师数据完整（id 唯一、核心字段齐全、都有头像）；quickPicks 28 人与 presetMasters 28 人一一对应
- 数据层 marketData.js（828 行）健壮：东财+Yahoo 双源容错、TTL 缓存（失败负缓存 60s）、业绩预告+机构一致预期、超时控制；实测 /api/context 返回宁德时代快照正常
- API 路由统一：/api/chat 带 60s 超时+重试、错误友好化；robots disallow /api
- 无 TODO/FIXME/console.log 残留；impeccable 全量 0 告警
- 主题/语言/聊天方式/讨论状态均本地持久化；SSR 与首帧一致（layout.js 内联脚本防闪烁）

**修复项（部署前必须）**:
- ❌→✅ 旧项目域名 yieldglide.com 残留：vercel.json 的 www→根域重定向已删除（本项目域名未定，重定向会指向错误站点）；poster.js getSiteUrl 兜底域名改为 think-tank.example.com（与 sitemap/layout 占位符一致）
- ⚠️ 部署前待办（未改，需用户操作）：.env.local 的 NEXT_PUBLIC_SITE_URL 仍是 your-domain.com 占位符，部署时必须改为真实域名（影响海报二维码、SEO canonical、sitemap、robots）

**未提交的旧残留（不处理）**: memory/project.md 是旧项目 YieldGlide 的残留文档（域名/架构过时），已忽略不提交；根目录 头像/、public/头像/ 为本地素材目录（git 未跟踪），prebuild 脚本 sync-avatars.mjs 从 public/头像 同步到 public/avatars（已跟踪）

**待办**: ①部署时改 NEXT_PUBLIC_SITE_URL 真实域名 ②推送 101 个本地领先提交到 origin ③线上验证行情注入（东财国内直连/Yahoo 美股港股）

## 2026-08-09（第九十九批）

### 输入框底色再弱化一档
**背景**: 用户反馈输入框底色还是偏强，要求再弱一点。

**决策**: 亮色半透明白 0.5→0.28，暗色微亮 0.045→0.03（保留边框区分，避免回到"看不见输入框"的老问题）。

**实现**: page.css .question-input 与暗色覆盖规则透明度下调。

**验证**: 浏览器实测亮色 rgba(255,255,255,0.28)、暗色 rgba(255,255,255,0.03)；impeccable 0 告警。

**待办**: 无新增。

## 2026-08-09（第一百批）

### 修复 favicon 模糊：ico 升级为多尺寸 + 图标声明优化
**背景**: 用户反馈网页 logo（favicon）发糊。

**原因**: 旧 public/favicon.ico 只含单张 16×16 图，高 DPI 屏幕（Retina/高分屏）放大显示必然模糊；favicon.png（180×180）清晰但浏览器默认优先用 .ico。

**决策**:
- 用 favicon.png（RGBA 有效）重新生成 favicon.ico，内含 16/32/48 三尺寸
- layout.js 图标声明加 sizes 属性（ico 48×48、png 180×180）并新增 apple-touch-icon（移动端主屏图标用 png）

**验证**: ICO 内部 3 张图（16/32/48）；页面 link 声明生效（icon 48×48、png 180×180、apple-touch-icon）；favicon.ico/png HTTP 200；impeccable 0 告警。

**待办**: 无新增。

## 2026-08-09（第一百零一批）

### 背景壁纸替换为「查理和芒格吵架」，旧图保留
**背景**: 用户提供新背景图（查理和芒格吵架.jpeg，2560×1440），要求放入素材库并替换网站背景壁纸，原图保留以便换回。

**决策**: 复制原图为 public/bg-argue.jpg（152KB，体积合理），page.js 背景引用 /bg-debate.png → /bg-argue.jpg；旧图 public/bg-debate.png 保留不删。

**验证**: 浏览器实测新背景 2560×1440 加载成功、滤镜（grayscale+sepia+blur 0.4px）与 opacity 0.22 正常。

**待办**: 后续如需换回，把 page.js 的 /bg-argue.jpg 改回 /bg-debate.png 即可。

## 2026-08-11（第一百零二批）

### 移植 UZI-Skill 深度分析能力（借鉴 wbh604/UZI-Skill 开源项目）
**背景**: 用户希望借用 UZI-Skill（66 评委股票深度分析开源项目）补全「大师吵股」的股票分析能力。它无法直接跑在 Vercel serverless（Python + 多轮 agent 工作流），故移植其数据维度与机构方法到现有 Next.js 数据层。

**新增 `src/app/api/chat/uziSkills.js`（约 1000 行，纯 Node + 免费公开接口）**:
- 五年财务历史（东财 MAINFINADATA，含营收/净利/ROE/毛利率/现金流净额/总股本/ROIC）
- PE/PB 近 5 年估值分位（K线年度收盘 × 年报 EPS/BPS 自算）
- K线技术指标（MA/RSI/MACD/KDJ/60日涨幅/波动率/回撤/放量；美股港股 Yahoo 优先、东财兜底）
- 龙虎榜（近 45 日，游资/机构席位识别）
- 研报评级（reportapi.eastmoney.com，近 12 月评级分布 + 平均预测 EPS）
- 6 平台社交热榜命中（微博/知乎/百度/抖音/头条/B站，单平台失败隔离）
- 杀猪盘量化扫描（K线异常/基本面热度脱节/跨平台联动，UZI 8 信号降级版）
- DCF 估值（移植 fin_models.py：WACC 6.96%、两段 5+5 年、永续 g=2.5%、安全边际）
- 同行对标（行情 f127 行业板块 → suggest 解析 BK 代码 → 同板块市值 Top8 可比）

**接入点**:
- `marketData.js`: 行情加 f127 行业板块字段；导出 getYahoo/withTimeout；美股 Yahoo 合并 freeCashflow/shares；ST 股防误判（"ST合力泰"不再误解析出美股 Sensata）
- `quoteContext.js`: 主快照后附加「深度分析快照」（最多 2 家公司并行，10s 预算，失败只缺该项）
- `prompts.js`: 大师发言需点明命中的数据点（估值分位/DCF/龙虎榜/杀猪盘等）；裁决 JSON 新增 valuationNote + trapWarning 字段（前端只读旧字段，兼容）

**验证**: /api/context 实测茅台/中际旭创/ST合力泰/英伟达 均正常（估值分位、DCF、龙虎榜、研报、同行对标、杀猪盘扫描齐全）；/api/chat 实测 DeepSeek 引用深度数据作答；生产构建通过。

**已知限制**:
- 北向资金个股持仓明细 2024-08 起交易所停发，整体净买入接口也返回空 → 该维度自动降级不展示
- 社交热榜部分平台（微博等）在海外服务器可能 403，单平台失败不影响其他
- 杀猪盘为量化信号扫描（8 信号降级版），非结论；DCF 为简化模型（净债桥缺失时 EV≈股权价值）
- 美股/港股深度维度较轻：K线/DCF/热榜有，财务历史/龙虎榜/研报为 A 股专属

## 2026-08-11（第一百零三批）

### 新增产品需求文档 PRD.md
**背景**: 用户希望有一份"像产品需求文档"的文档，记录目前实现的各种业务逻辑，方便翻阅追溯（也便于向潜在收购方介绍实现逻辑）。

**产出**: `memory/PRD.md`（318 行，中文）。内容覆盖：
- 产品概述与功能清单（群聊辩论/单聊深聊/追问/虚拟大师）
- 一次提问的完整链路流程图
- 问题解析流水线（LLM 提取→词典→英文代码→启发式→东财解析；ST 防误判）
- 数据查询层（东财/Yahoo 双源、缓存超时、字段映射）
- 深度分析引擎 9 大维度明细 + 杀猪盘信号阈值 + DCF 参数表
- 表达层风格控制（辩论基调/大师画像/各场景提示词/裁决结构/反幻觉约束）
- 快照组装规则、已知限制与降级策略、文件地图、关键常量阈值表

**说明**: 文档已写入 memory/PRD.md，与 architecture.md、log.md 同目录；若需随代码提交可在下次提交时一起 `git add memory/PRD.md`。

## 2026-08-11（第一百零四批）

### 长期约定：规则/能力更新必须同步 PRD.md
**用户要求**: 以后有规则、能力的提升，记得更新文档（memory/PRD.md）。
**约定（长期有效）**: 任何涉及业务规则、分析能力、提示词、数据源、阈值的改动，除按惯例更新本 log.md 外，**必须同步更新 memory/PRD.md** 的对应章节（尤其是 §6 深度分析引擎、§7 风格控制、§10 已知限制、§12 常量阈值表），保持文档与代码一致，便于翻阅追溯与交接。

## 2026-08-11（第一百零五批）

### 新增「小白解释」功能
**背景**: 用户要求在大师对话旁增加会话解释入口，文案「看不懂？小白点这里看解释」，点击弹出浮层，用大白话解释专业术语与大师思路。
**实现**:
- `prompts.js` 新增 `buildExplainPrompt`（术语翻译 + 核心思路，250-350 字，大白话）
- `page.js`：每条大师发言（含流式打完）下方新增解释按钮；点击打开浮层（复用 modal-overlay），调 `/api/chat`（不带快照）生成解释；同一条发言结果缓存，重复点击不重复调用；支持 Escape/点击遮罩关闭
- `page.css`：`.explain-btn`（虚线幽灵按钮）+ `.explain-modal` 浮层样式（头像/原话/解释区）
- `i18n/messages.js`：zh/en 文案（explainBtn/explainTitle/explainLoading/explainError/explainClose/explainSpeech）
**验证**: 页面编译通过；模拟调用解释接口，输出含「专业术语翻译（护城河/ROE/PE/估值分位/DCF/安全边际）+ 大师核心思路（买水果比喻）」的合格结果。
**文档**: 已同步更新 memory/PRD.md（§2 功能清单、§7.4 小白解释、§11 文件地图）。

## 2026-08-11（第一百零六批）

### 修复：小白解释 Markdown 星号裸显
**背景**: 用户反馈解释浮层直接显示 ** 星号，未按 Markdown 渲染。
**原因**: AI 生成的是 Markdown（**加粗**、- 列表），但浮层按纯文本 `<p>` 显示。
**修复（双保险）**:
- `prompts.js` buildExplainPrompt 增加第 6 条要求：禁用 Markdown 标记，术语用「1. 2. 3.」编号或「·」分隔（实测输出已为纯文本）
- `page.js` 新增 `inlineRich`/`renderExplainText` 轻量渲染：**加粗**→strong、- 列表→ul、编号行→段落、***→** 归一化；解释浮层改用渲染函数
- `page.css` 新增 .explain-list 样式
**验证**: 渲染解析逻辑单测通过（含 ***、** 混合、列表、空行）；新提示词实测输出无星号。
**文档**: 已同步更新 memory/PRD.md §7.4。

## 2026-08-11（第一百零七批）

### 优化：解释按钮位置 + 弹窗改右侧抽屉
**用户反馈**: ① 解释按钮想放在 💡 总结同一行的右下角；② 小白解释由弹窗改为侧边抽屉。
**实现**:
- `page.js`：两条渲染路径（旧/流式）的 explain-btn 从发言下方移入 `.speech-key` 行内（flex 布局，💡 左、按钮右，右下角）；解释展示由 modal-overlay 改为 drawer-overlay + 右侧滑入抽屉（explain-drawer，420px/94vw，含头部头像+标题+关闭、原话、解释正文，可滚动，点遮罩/Esc 关闭）
- `page.css`：.speech-key 改 flex space-between；.explain-btn 去上边距、nowrap、右侧；新增 .drawer-overlay/.explain-drawer/.explain-drawer-head/.explain-drawer-body/@keyframes drawer-in；移动端 94vw
**验证**: 页面编译通过。
**文档**: 已同步更新 memory/PRD.md §7.4。

## 2026-08-11（第一百零八批）

### 优化：小白解释抽屉加宽 + 排版透气
**用户反馈**: 抽屉内容太密、太窄。
**实现**（page.css）:
- 抽屉宽度 420px → 560px（移动端仍 94vw）
- 头/体留白加大（head 22px 28px、body 22px 28px 30px）
- 正文字号 14.5→15px、行高 1.95→2.0；列表项行高 1.9→2.0、间距 6→10px
- 段落间距 14px、术语/原文卡片 padding 加大、圆角 12px；动画 keyframes 改名 explain-drawer-in 避免与 invite-drawer 冲突
**验证**: 页面编译通过。
**文档**: 已同步更新 memory/PRD.md §7.4。

## 2026-08-11（第一百零九批）

### 优化：解释抽屉布局改为「大师说了啥 + 相关指标」，去掉发言原文
**用户反馈**: 抽屉里不要贴发言原文；把「大师说了啥」放上面，下面贴相关指标。
**实现**:
- `prompts.js` buildExplainPrompt 改为结构化 JSON 输出：`{summary（大师核心思路大白话，200-280字）, metrics（术语/指标通俗解释数组，最多8条）}`
- `page.js`：新增 parseExplain 解析 JSON（失败回退纯文本渲染）；抽屉 body 去掉原文卡片，改为两部分：「大师说了啥」（explainSummaryLabel）+「相关指标」（explainMetricsLabel 列表）；缓存改为存结构化数据
- `page.css`：新增 .explain-section/.explain-summary 分区间距；移除 .explain-original 样式
- `i18n/messages.js`：新增 explainSummaryLabel/explainMetricsLabel
**验证**: 新提示词实测返回合法 JSON（summary + 8 条 metrics），解析通过；页面编译通过。
**文档**: 已同步更新 memory/PRD.md §7.4。

## 2026-08-11（第一百一十批）

### 提速：小白解释改专用流式接口（SSE 边生成边显示）
**背景**: 用户反馈解释加载慢（"正在用大白话解释…"要等 4-5 秒）。
**原因**: 走通用 /api/chat 整段返回，必须等 DeepSeek 全部生成完。
**实现**:
- 新增 `src/app/api/explain/route.js`：专用流式接口（SSE，text/event-stream）。比 /api/chat 轻量：不注入快照、max_tokens 1500、25s 超时、不重试；逐 delta 推送
- `prompts.js` buildExplainPrompt 改为分段标记文本（【大师说了啥】/【相关指标】，指标「· 」开头），流式友好
- `page.js`：openExplain 改为 fetch /api/explain + ReadableStream 逐片读取，首段 delta 到达即隐藏 loading 并边收边渲染；缓存完整文本；修复 explainLoadingRef 未置 true 导致全程 loading 的 bug
- `page.css`：新增 .explain-inline-head（【】标记行渲染为小节标题）
**效果**: 首段内容约 1s 内出现，文字边生成边填充，总等待感知从 ~5s 降到 ~1s
**验证**: /api/explain 实测流式 438 片段、输出含【大师说了啥】+【相关指标】两段；页面编译通过。
**文档**: 已同步更新 memory/PRD.md §7.4。

## 2026-08-11（第一百一十一批）

### 新增：智能选角（随机5人 → 按问题匹配大师）
**背景**: 用户指出随机选人有问题——大师风格限定了回答倾向，短线问题让巴菲特价值派回答不合适；要求把「随机5人」改为按问题匹配最适合的人，最多 5 位。
**实现**:
- 新增 `src/app/api/match-masters/route.js`：把问题 + 大师风格清单发给 DeepSeek，按问题类型（短线/价值/成长/宏观/技术/困境/债券/量化/配置）挑选 1-5 位最合适的大师；返回 JSON {ids}；同一问题 30 分钟缓存；失败返回空（前端回退随机）
- `page.js`：群聊侧栏「🎲 随机5人」→「🎯 智能匹配5人」；smartSelectMasters 先取问题 → 调接口 → 选中匹配的 id；未输入问题或匹配失败时回退随机 5 人；匹配中按钮禁用显示「匹配中…」
- `i18n/messages.js`：新增 membersSmartMatch/membersMatching
- `ui.js` MiniBtn 支持 disabled（含禁用样式）
**验证**: 短线问题 → 利弗莫尔/德鲁肯米勒/泰珀；价值问题 → 巴菲特/芒格/林奇/费雪/马克斯；接口约 1.2s；页面编译通过。
**文档**: 已同步更新 memory/PRD.md（§2 功能清单、§9 前端交互、§11 文件地图）。

## 2026-08-11（第一百一十二批）

### 微调：智能匹配按钮文案与图标
- 去掉"5"字：按钮「智能匹配5人」→「智能匹配」（匹配人数本身是 1-5 不定）
- 图标换回骰子 🎲（匹配中仍显示 ⏳）
- 文档已同步 memory/PRD.md §9。

## 2026-08-11（第一百一十三批）

### 优化：选角改「智能匹配（默认）/ 手动选择」双模式，去掉全选
**用户要求**: ① 智能匹配默认为选中状态，该模式下大师列表不支持勾选；② 去掉全选功能，改为手动选择模式，切换后需手选。
**实现**:
- `page.js`：新增 selectMode（smart 默认 / manual）；侧栏双模式切换（MiniBtn active 高亮）；智能模式下列表锁定（smart-locked 半透明、onClick/键盘禁用）、提供「🎲 重新匹配」按钮；手动模式下只保留「✕ 清空」（删除全选按钮）；go() 提交时若为智能模式自动按问题选角（fetchMatchedIds：2-5 位、不足随机补足、失败回退随机 5 人），结果同步 UI
- `ui.js`：MiniBtn 支持 active 高亮态
- `i18n`：新增 membersManual/membersReMatch/smartModeHint/manualModeHint
- `page.css`：.sidebar-mode/.sidebar-mode-hint/.master-row.smart-locked
**注意**: 中间一次 patch 误把 JSX 反引号写成引号导致编译报错，已修复；dev 编译通过（未跑生产构建以免覆盖正在运行的 .next）。
**文档**: 已同步更新 memory/PRD.md（§2、§9）。

## 2026-08-11（第一百一十四批）

### 调整：去掉「重新匹配」按钮，智能模式每次提问自动选角
**用户澄清**: 智能匹配不需要手动"重新匹配"按钮；每次提问时自动按当次问题匹配最适合的人，人选每次可以不同。
**实现**:
- `page.js`：删除智能模式的「🎲 重新匹配」按钮（smartSelectMasters、matchingMasters 状态一并移除）；智能模式下侧栏无操作按钮，只有模式切换 + 提示；go() 提交时仍每次自动 fetchMatchedIds 按问题选角（人选随问题变化）
- `i18n`：smartModeHint 更新为"每次提问自动匹配最合适的大师（人选随问题变化）"
**验证**: dev 编译通过。
**文档**: 已同步更新 memory/PRD.md §9。

## 2026-08-11（第一百一十五批）

### 微调：清空按钮改小胶囊样式
**用户反馈**: 清空按钮太大。
**实现**: MiniBtn 新增 subtle 变体（.mini-btn-subtle：flex 0 0 auto 不占满整行、padding 4px 12px、字号 10.5px、胶囊圆角、虚线边框、弱化颜色）；手动选择模式的「✕ 清空」改用 subtle。
**验证**: dev 编译通过。

## 2026-08-11（第一百一十六批）

### 微调：清空按钮移到「已选 X / Y」同一行右侧
**实现**: 新增 .sidebar-count-row（flex space-between）；手动选择模式的「✕ 清空」小胶囊按钮从独立操作行移入计数行右侧（智能模式只显示计数）。首次 patch 因锚点不对未生效，用精确锚点重新应用；dev 编译通过。

## 2026-08-11（第一百一十七批）

### 新增免责声明 + 全局字体改无衬线
**用户要求**: ① 增加不构成投资意见的声明；② 页面字体改为无衬线（宋体读起来累），网页标题除外。
**实现**:
- `page.js`/`page.css`/`i18n`：main-layout 后新增页脚 .page-disclaimer「⚠️ 本站内容由 AI 生成，仅供学习交流与娱乐参考，不构成任何投资建议或意见，据此操作风险自负。」（中英文 disclaimer）
- `globals.css`：body 默认字体 --font-serif → --font-sans
- `page.css`：.question-input、.btn-submit 改 --font-sans；.header-title 显式保留 --font-serif（仅标题用衬线）
**验证**: dev 编译通过（GET / 200）。
**文档**: 已同步更新 memory/PRD.md §9。

## 2026-08-11（第一百一十八批）

### 修复：提交时智能选角无反馈的卡顿感 + 增加"正在搜寻大师"提示
**问题**: 智能模式下点「召集智囊团」，先要调 /api/match-masters 选角（1-3s），此步骤发生在 setLoading(true) 之前 → 点击后有一段时间无任何反馈。
**实现**:
- `page.js`：新增 matchingHint 状态；go() 智能匹配前先 setMatchingHint(true)，匹配完成后置 false；提交按钮匹配中禁用，文案变「⟳ 正在搜寻最适合回答您问题的大师…」；问题卡片内新增 .summon-status 状态行（🔍 + 文案 + 呼吸动画）
- `i18n`：新增 summonMatching（中/英）
- `page.css`：.summon-status + summon-pulse 动画
**效果**: 点击后立即出现"正在搜寻最适合回答您问题的大师…"反馈 → 选角完成 → 进入"大师们正在打车"加载 → 辩论开始，全程无空白等待
**验证**: dev 编译通过。

## 2026-08-11（第一百一十九批）

### 微调：智能匹配模式下隐藏大师勾选按钮
**实现**: 智能模式（selectMode==='smart' 且群聊）下，大师列表不再渲染 .master-check 勾选圆圈（列表锁定本就不可勾选，去掉视觉干扰）；手动模式仍正常显示勾选。
**验证**: dev 编译通过。

## 2026-08-11（第一百二十批）

### 微调：智能模式大师列表去掉灰度，仅不可点击
**用户反馈**: 智能模式下列表不要做成灰灰的效果，只是不可点击。
**实现**: .master-row.smart-locked 移除 opacity 0.6 半透明，改为正常外观 + cursor default；hover 不高亮（transparent），提示不可点击但不显得灰暗。
**验证**: dev 编译通过。

## 2026-08-11（第一百二十一批）

### 扩充大师库：按 UZI-Skill 66 人评审团名单补 38 位
**背景**: 用户希望按 UZI-Skill 的 66 位评审团名单扩充大师库，并给不同标签（价值投资/量化等）。
**实现**:
- 新增 `src/data/uziMasters.js`：38 位新大师，每人带 tag/tagEn 标签（成长投资/宏观对冲/做空侦察/技术趋势/中国价值/A股游资/量化投资/科技领袖/AI卡位）
  - B 成长：欧奈尔、安德森、格利、纳瓦尔、格斯特纳、查马斯（6）
  - C 宏观：罗伯逊、查诺斯（2）
  - D 技术：米内尔维尼、达瓦斯、江恩（3）
  - E 中国价值：段永平、张坤、朱少醒、谢治宇、冯柳、邓晓峰、张磊（7）
  - F A股游资（代表 12）：章盟主、赵老哥、炒股养家、佛山无影脚、作手新一、北京炒家、瑞鹤仙、陈小群、孙哥、呼家楼、方新侠、宁波桑田路
  - G 量化：索普、大卫·肖、阿斯尼斯（3）
  - H 科技领袖：黄仁勋、马斯克、奥特曼、塞勒（4）
  - I AI卡位：Serenity（1，注明方法论视角、身份未经审计）
- `masters.js`：BASE_MASTERS(32) + UZI_MASTERS 合并导出 PRESET_MASTERS，总计 70 位，无 id 冲突
- `page.js`：大师列表 .master-style 优先显示 tag 标签（新增大师显示"价值投资/量化投资"等，原有大师仍显示称号）
**验证**: dev 编译通过；智能选角实测：量化问题→asness/simons/thorp/greenblatt/shaw；中国价值问题→zhangkun/duan/buffett/munger/lilu；页面 200。
**说明**: 24 位游资仅收录代表 12 位（拉萨天团/成都帮/苏南帮/鑫多多/股海贼王等未收录），需要可再加。
**文档**: 已同步更新 memory/PRD.md（§2、§11）。

## 2026-08-11（第一百二十二批）

### 新增大师配真实头像（25 位）
**背景**: 用户希望给扩充的 38 位大师配上真实头像；能找到的用真实照片，找不到的保留占位。
**渠道**: 维基百科在本机网络不可达、百度百科 403，改用 cn.bing.com 图片搜索（带人物上下文关键词防认错，如"张坤 易方达 基金经理"）。
**结果**:
- 25 位配到真实照片并处理成方形 JPG 存入 public/avatars/<id>.jpg：欧奈尔/安德森/格利/纳瓦尔/格斯特纳/查马斯/罗伯逊/查诺斯/米内尔维尼/达瓦斯/江恩/段永平/张坤/朱少醒/谢治宇/冯柳/邓晓峰/张磊/索普/大卫·肖/阿斯尼斯/黄仁勋/马斯克/奥特曼/塞勒
- 13 位保留占位头像（匿名游资 12 位 + Serenity，无公开真实照片）
- uziMasters.js 对应 avatar 字段已更新
**注意**: 头像来自公开网页（新浪/搜狐/知乎/CNBC/公司官网等），仅供本地/学习使用；部分来源分辨率偏低（如谢治宇/张磊），列表小尺寸显示无碍。用户如发现某张不对或想换图可随时替换。
**验证**: 25 个文件齐全、首页 200、dev 编译通过。
**文档**: 已同步 memory/PRD.md §11（头像说明）。

## 2026-08-11（第一百二十三批）

### 头像统一深色冷调处理
**用户要求**: ① 占位头像统一深色背景，不要花哨暖色；② 真实照片头像也处理成深色冷色调，保持质感。
**实现**:
- 占位头像：`uziMasters.js` uziAvatar / `masters.js` avatarUrl 的 SVG 改为深蓝灰渐变（#2b3a52→#0e1624）+ 浅色文字，所有占位统一
- 真实照片：安装 Pillow（清华镜像），对所有 55 张（public/头像 源目录 30 张 + public/avatars 新增 25 张）做统一处理：降饱和 0.5 → 红降/蓝增（冷调）→ 压暗 0.78 → 微提对比 → 底部深蓝黑渐变叠层；public/头像 处理后运行 sync-avatars 同步
- 验证：采样图平均 RGB 均深色且蓝>红（如 musk (22,26,34)、duan (61,65,72)）；页面 200、dev 编译通过
**说明**: public/头像 为 git 跟踪源目录，改后可在 git 历史回退；根目录 头像/（未跟踪）保留原始照片备份。
**文档**: 已同步 memory/PRD.md §11。

## 2026-08-11（第一百二十四批）

### 修复：章盟主/炒股养家等占位头像仍显示暖色
**原因**: ① 占位 SVG 用了 `<linearGradient>` + `url(#g)` 引用，部分浏览器在 `<img>` 里解析失败 → MasterAvatar onError 回退成 `master.color` 暖色圆形+首字；② 新大师的 color 字段仍是暖色（紫/红等）。
**修复**:
- `uziMasters.js`/`masters.js` 占位 SVG 改为**纯色深蓝灰 #1c2736** + 浅色文字（去掉渐变/引用，杜绝解析失败）
- 38 位新大师的 `color` 全部统一为深蓝灰冷调（10 个深色系循环：#33415c/#3d4a63/#2f3e57/#42516e/…），即使回退圆形也是深色
**验证**: 无残留暖色 color；无 url(#g)；页面 200、dev 编译通过。提醒用户硬刷新（Cmd+Shift+R）避免缓存。
**文档**: 已同步 memory/PRD.md §11。

## 2026-08-11（第一百二十五批）

### 头像调色定稿：B 方案（更亮更彩）
**背景**: 用户反馈上一版头像"有点灰"，量化确认亮度过低（马斯克 13%）、饱和度低（塞勒 22%）。提供 A/B/C 三档对比（当前/更亮更彩/更深冷），用户选定 B。
**B 方案参数**: 不降饱和(1.00)、压暗极轻(0.96)、冷调轻微(R0.97/G0.99/B1.03)、对比1.08、底部轻渐变(40)。
**实现**: 全部 55 位重处理（30 位从根目录 头像/ 原始重处理→public/头像 再 sync；25 位从 /tmp/avatars_orig 重新抓取的原始图处理→public/avatars）。采样统计较前版亮度/饱和度均回升（如 musk 亮度13%→19%、duan 亮度31%→48% 饱和39%→60%）。
**已知**: 部分原图本身暗/灰（如马斯克舞台暗光照、张坤原图偏白），B 方案保持原图质感、仅轻微冷调压暗，个别仍偏暗属原图限制。
**文档**: 已同步 memory/PRD.md §11。

## 2026-08-11（第一百二十六批）

### 头像定稿：真实照片还原原图，占位头像前景改纯白
**用户反馈**: 调色版太灰/太暗，决定真实照片直接用原图；占位头像保留深色底但前景色改纯白（此前 #dbe4f0 偏暗）。
**实现**:
- 30 位原有：从根目录 头像/ 原始照片恢复 → public/头像 → sync 到 public/avatars
- 25 位新增：从 /tmp/avatars_orig 原始图恢复，仅做方形裁切+≤1024+JPEG（不调色）
- 占位 SVG（uziMasters.js/masters.js）：背景仍深蓝灰 #1c2736，文字 fill #dbe4f0 → #ffffff
**验证**: 采样图亮度/饱和度回升（如 duan 亮度 52%、saylor 41%）；页面 200。
**说明**: 若以后想再统一风格，可对全部原图做一键调色并保留原始备份（根目录 头像/ 始终是未处理源）。
**文档**: 已同步 memory/PRD.md §11。

## 2026-08-11（第一百二十七批）

### 新增：大师流派分类 + 分组展示筛选
**用户要求**: 给大师做分类（如价值派、量化派），支持分组。
**实现**:
- 新增 `src/data/masterGroups.js`：14 个流派分组配置（价值投资/成长投资/中国价值/宏观对冲/做空侦察/A股游资/量化投资/技术趋势/维权投资/科技领袖/AI卡位/指数投资/事件驱动/其他），含中英文标签、顺序、normalizeGroup 归一化（未知 tag → 其他）
- `masters.js`：原有 32 位大师全部补上 tag/tagEn（与 UZI 38 位标签体系统一，70 位全覆盖）
- `page.js`：侧栏大师列表上方新增**分组筛选 chips**（全部 + 各流派，带人数）；列表按流派分组展示（组头 + 组员），支持点选筛选；单聊/智能模式同样生效
- `page.css`：.master-group-filter/.mgroup-chip/.master-group/.master-group-head 等样式
**验证**: 70 位全部有 tag 且被 14 组覆盖；dev 编译通过、页面 200。
**文档**: 已同步更新 memory/PRD.md（§2 功能清单、§11 文件地图）。

## 2026-08-11（第一百二十八批）

### 微调：分组标签区默认一行，支持展开/收起
**实现**: 新增 groupFilterExpanded 状态；标签区默认 .collapsed（max-height 24px 单行 + overflow hidden），下方「▾ 更多 / ▴ 收起」切换按钮；i18n 新增 groupFilterMore/groupFilterLess。
**验证**: dev 编译通过、页面 200。

## 2026-08-11（第一百二十九批）

### 优化：分组展开/收起按钮改胶囊样式 + 底部渐隐
**用户反馈**: 之前的"▾ 更多"纯文字链接太弱。
**实现**: 收起时 chips 区底部加渐隐（.collapsed::after 渐变遮罩）；切换按钮改为**胶囊按钮**（accent 色描边 + 悬停填充）：收起态「▾ 展开全部分组」，展开态「▴ 收起」（弱化描边）。i18n 文案更新为"展开全部分组"。
**验证**: dev 编译通过。

## 2026-08-11（第一百三十批）

### 回退：分组展开按钮改回简洁文字链接（主题色）
**用户反馈**: 胶囊按钮+渐隐"太奇怪太丑"，不如原来。
**实现**: 移除 .collapsed::after 渐隐和胶囊按钮样式；切换改回纯文字链接（收起态「▾ 展开全部分组」主题色、展开态「▴ 收起」弱化），比最初版（灰色）更醒目但不抢戏。
**验证**: dev 编译通过。

## 2026-08-11（第一百三十一批）

### 微调：展开按钮文案改为「展开」+ 图标放大
- i18n groupFilterMore: '展开全部分组' → '展开'（en Expand）
- 图标 ▾/▴ 独立 span（.mgroup-chevron 13px），比文字稍大
- dev 编译通过

## 2026-08-11（第一百三十二批）

### 微调：分组展开/收起按钮移到右侧
- .mgroup-toggle 改为 display:block + margin-left:auto，右对齐
- dev 编译通过

## 2026-08-11（第一百三十三批）

### 微调：展开/收起按钮移到标签行最右侧（与「成长投资」同行）
- 收起时只渲染「全部 + 价值投资 + 成长投资」+ 行内「▾ 展开」按钮（margin-left:auto 右对齐，与标签同一行）；展开后显示全部分组 + 「▴ 收起」
- 移除旧的 max-height 裁剪方式
- dev 编译通过

## 2026-08-11（第一百三十四批）

### 微调：展开按钮绝对定位到第一行标签右侧
**用户反馈**: 按钮仍未对齐第一行标签右侧（此前随 flex 换行跑到第二行）。
**实现**: 收起态下 wrap 相对定位 + 标签容器 padding-right 预留 58px + 按钮 position:absolute top:0 right:0，确保按钮恒在第一行右上角；展开态按钮回到 flow（margin-left:auto 右对齐下方）。
**验证**: dev 编译通过。

## 2026-08-11（第一百三十五批）

### 微调：收起时第一行放两个分组标签 + 展开按钮
- 收起态标签更紧凑（字号 10px、padding 2px 7px、计数 8.5px），预留宽度 58→50px，使「全部 + 价值投资 + 成长投资 + ▾展开」一行放下
- dev 编译通过

## 2026-08-11（第一百三十六批）

### 大师多标签支持 + 短线/敢死队标签
**用户反馈**: 一个大师只有一个标签太少，应有多个；"短线"标签偏少，章盟主这类应属"敢死派"。
**实现**:
- 大师支持 `tags` 数组（多标签）：12 位游资全部加「短线」副标签；章盟主/赵老哥/炒股养家/佛山无影脚/作手新一/宁波桑田路加「敢死队」；部分原有大师补副标签（如格林布拉特=量化+价值、伯里/艾因霍恩=价值+做空、泰珀=宏观+困境、保尔森=事件驱动+困境、马克斯=价值+周期、维权派=维权+价值、利弗莫尔=技术+短线等）
- masterGroups.js 新增分组：短线/敢死队/做空/困境投资/周期（共 18 组）
- 列表大师名下行显示**全部标签小胶囊**（多标签可见）；筛选 chips 按任意标签计数（一位大师可命中多个标签，如 短线 13 人、敢死队 6 人）；选中副标签时展示所有带该标签的大师（仍按主标签分组）
- 智能选角请求带 tags，匹配更准
**验证**: 主标签 70 人全覆盖；副标签命中统计正确（A股游资 24、价值 22、短线 13、敢死队 6…）；dev 编译通过。
**说明**: 敢死队为风格标签（涨停敢死队/打板风格），非严格官方归类。
**文档**: 已同步更新 memory/PRD.md（§2）。

## 2026-08-12（第一百三十七批）

### 调整：多标签只用于筛选，行内恢复显示名言
**用户反馈**: 多标签不需要展示在名字下，只需能筛选；名字下方恢复显示名言/风格（跟之前一样）。
**实现**:
- 行内 .master-style 恢复为单行名言（截断 22 字加省略号），无名言时退回称号/风格；移除 .mtag 标签胶囊
- 多标签数据（tags）与按标签筛选功能保留（groupChips/filteredGroups 仍按全部标签计数与筛选）
- dev 编译通过

## 2026-08-12（第一百三十八批）

### 微调：名言截断时 hover 显示全文
- master-style 名言长度 >20 字时加 title 属性，鼠标移入显示完整名言（原生 tooltip，不被侧栏滚动裁剪）
- dev 编译通过

## 2026-08-12（第一百三十九批）

### 微调：名言 hover 浮层底色改深色
- .quote-tooltip 由 var(--bg-card)（亮色下为白）改为深蓝灰 #1c2736 + 浅色文字 #e8eef8，与占位头像/整体深色冷调风格统一
- dev 编译通过

## 2026-08-12（第一百四十批）

### 发版准备完成（提交 7477999），推送被网络阻塞
- ✅ 整体检查：生产构建通过（`npm run build`）；冒烟测试全部通过——/api/context（含深度分析快照）、/api/match-masters（智能选角）、/api/explain（SSE 流式）、/api/chat（DeepSeek 回复 1607 字）、首页 200；无残留调试文件；memory/project.md（旧残留）未提交
- ✅ 已提交：`7477999 feat: 移植 UZI-Skill 深度分析 + 大师库扩充至70位 + 智能选角等`（41 文件 +2628/-115）
- ⚠️ **推送未完成**：github.com:443 从本机不可达（HTTPS 超时、SSH 端口 22/443 也不通、无代理、无 SSH key）；codeload/api.github.com 可达但无法用于 git push。符合旧约定"等用户能翻墙后执行 git push"
- 下一步：开启代理/VPN 后重试 `git push origin main`（本地领先 origin 130 个提交）

## 2026-08-12（第一百四十一批）

### ✅ 推送成功，发版流程已触发
- 用户开启 VPN 后重试，`git push origin main` 成功：`5ac4852..d807888`（130 个提交全部推送）
- Vercel 已接入该仓库，推送后自动构建部署；线上域名验证待用户确认

## 2026-08-12（第一百四十二批）

### 新增：能力包注册层 + 缠论大师（第一个样例）+ 强制安全审查
**背景**: 用户提出"万物皆可大师"：围绕主题/能力定义角色，从 GitHub 找热门 skill 匹配，角色直接调用该 skill 能力。评估结论：不直接执行 skill 代码，而是把 skill 消化成"能力包"（方法论 + 数据钩子）挂到大师上；风控与安全为必检项。
**实现**:
- 新增 `src/data/capabilities.js`：能力包注册中心（chan_theory 缠论分析），含 knowledge 方法论片段、dataHooks、**强制 review 安全审查记录**
- `uziMasters.js`：新增「缠论分析大师」（capability: chan_theory，tag 技术趋势+缠论），大师总数 71
- `masterGroups.js`：新增「缠论」分组
- `uziSkills.js`：新增 `computeChanIndicators`（分型/笔/中枢/背驰，仅移植公开规则），getDeepAnalysis 计算、快照输出「缠论视角」行
- `prompts.js`：masterProfileLine 自动注入大师的能力包知识域
- 安全规则：只移植逻辑不执行第三方代码；每个能力包必须有 review 记录；输出有免责声明兜底
**验证**: 缠论问题智能选角命中 chan_master；茅台快照输出「上升笔，末端顶分型，最近中枢 1279.58–1344.70（价格在中枢内）」；修复了中枢判定过严（端点重叠→价格区间重叠）与函数定义漏存两个 bug；生产构建通过。
**文档**: memory/PRD.md 新增 §13 能力包注册与安全审查。

## 2026-08-12（第一百四十三批）

### czsc → 能力包 chan_czsc + 角色「缠中说禅」
**背景**: 用户要求把 GitHub 项目 czsc（缠中说禅技术分析工具）转为能力包，赋予角色"缠中说禅"，并在角色详情页介绍能力（星标/解决什么问题/擅长哪些），但不提具体项目名。
**实现**:
- `capabilities.js`：chan_theory → **chan_czsc**（缠论量化分析）。knowledge 扩展为 czsc 方法论体系（分型/笔/线段/特征序列/中枢/背驰/多级别联立/区间套/三类买卖点）；新增 `intro` 字段（zh/en：约 5.7k★、解决什么问题、擅长哪些，**不含项目名**）；review 记录引用内部来源 waditu/czsc（许可证 Other，仅借鉴方法论不复制代码）
- `uziMasters.js`：角色改名 **缠中说禅**（status deceased、称号"缠论体系创始人"、capability: chan_czsc）
- `MasterProfileModal.js` + CSS：详情页新增「能力来源」区（headline/problems/strengths，zh/en，不提项目名）
- 数据钩子复用 computeChanIndicators（已含分型/笔/中枢/背驰），快照输出「缠论视角」
**修复**: en headline 撇号未转义导致语法错误（改写避开）
**验证**: 缠论快照输出正常（茅台：上升笔/末端顶分型/中枢 1279.58–1344.70）；缠论问题选角命中 chan_master；生产构建通过。
**文档**: memory/PRD.md §13.2 已更新。

## 2026-08-12（第一百四十四批）

### 缠论对比测试：简化版 vs 原项目 czsc（诚实结论）
**背景**: 用户质疑"只移植方法论、不调第三方，效果是不是不好"，并要求对比。
**实测**（本机 pip 安装 czsc 1.0.1 + Rust 核心，同一份 320 根K线）:
- 分型：茅台 75→**116**（vs czsc 113）、旭创 89→**121**（vs 120）——补上**K线包含关系处理**后基本对齐 ✓
- 但笔/中枢/末笔方向/最近中枢仍不一致（茅台末笔方向：czsc 下降 vs 我们上升 ❌；旭创最近中枢：czsc 815-918 vs 我们 1169-1207 ❌）
- 根因：原项目有线段、特征序列、严格笔定义、中枢合并等规则（Rust 核心数千行），简化版无法 1:1
**已做**: computeChanIndicators 升级加入 K线包含关系处理（分型对齐），保留简化定位
**路线**（PRD §13.5）：A 接受简化+界面诚实标注（当前）；B 深度移植（数天）；C 外部服务跑原项目（最忠实，需常驻服务器）
**文档**: memory/PRD.md §13.5 对比结论。

## 2026-08-12（第一百四十五批）

### 本地部署 czsc 缠论服务（skill仓库/czsc-service）+ 记服务器待办
**背景**: 用户确认"本地先跑原项目体验精确缠论"；要求项目放在桌面《skill仓库》新建文件夹，并在 memory 记待办提醒未来租服务器部署。
**落地**:
- 位置：`/Users/zxiansheng/Desktop/skill仓库/czsc-service/`（独立 venv .venv，不污染系统 Python）
- `server.py`：FastAPI 包装原项目 czsc（HTTP 接口，端口 8701）
  - `POST /api/chan`：入参 secid/limit，出参 分型/笔/中枢/末笔方向/末端分型/最近中枢
  - 数据源三级回退：东方财富 push2his → 腾讯 ifzq → 新浪（东财近期反爬严重，腾讯/新浪兜底）
  - 可选鉴权：CZSC_API_TOKEN 环境变量
- `README.md` / `requirements.txt` 齐全；启动命令：`.venv/bin/uvicorn server:app --host 127.0.0.1 --port 8701`
**验证**: 茅台（113分型/16笔/3中枢/末笔向下/中枢1279.58-1344.70）与中际旭创（中枢815.23-918.99）均与原项目 czsc 输出逐项一致 ✓；东财被反爬时自动走腾讯/新浪 ✓
**待办（用户要求）**: 未来租服务器，把本服务部署上去供线上平台调用（见顶部当前待办）

## 2026-08-12（第一百四十六批）

### 优化：单聊专题化 + 流派数据偏好（方向 A/B/C 全做）
**背景**: 用户反馈 ① 单聊应像"小专题"完整透彻；② 缠中说禅回答偏价值派（枚举财报），应多用短线/技术/结构指标。确认根因：共享提示词硬性"必须用 PE/PB/ROE 举证"+ 快照财报数据丰盛，压过了能力包的"只按缠论体系"。
**实现**:
- `capabilities.js`：chan_czsc 新增 `dataFocus`（数据偏好：结构/技术面为主，基本面仅一句话风险提示）
- `prompts.js`：新增 `buildDataRule(master)` 按流派返回数据规则；buildOneSpeechPrompt（按发言者）、buildFollowUpPrompt（按流派各取所需）、buildChatPrompt 三处生效；单聊升级「小专题」结构（结论→分节→操作/风险，400-550 字）
- `page.js`：单聊首轮发言改用 buildChatPrompt（此前走辩论式发言）
- 顺手修复：fetchKlineYahoo 一直用被 yahoo-finance2 拒绝的 `range:'2y'`（之前修复未保存），改回 period1/period2 Date；否则线上美股/港股 K线会退化为东财兜底
**验证**: 缠中说禅单聊中钢国际实测：结论→日线→周线→月线→操作/风险，全程用底分型/背驰/中枢/量能/多级别，财报只出现在末段一句话风险提示；生产构建通过。
**文档**: memory/PRD.md §13.5 新增。

## 2026-08-12（第一百四十七批）

### 新约定：代码提交/推送必须等用户通知
**用户明确要求**：后续代码提交（commit/push）一律等用户通知后再执行，不要自行提交。
**当前待处理**：本地已提交 `6ddac78`（单聊专题 + 数据偏好 + Yahoo K线修复），推送因 github.com 网络不通未完成；等用户通知 + 网络恢复后再推。

## 2026-08-12（第一百四十八批）

### 修复：发言内容 ** 裸显 + 小白解释按钮位置
**用户反馈**: ① 单聊专题回复出现 **（AI 输出的 Markdown 加粗，发言气泡纯文本渲染导致裸显）；② 小白解释按钮跑到左侧（单聊无 keyPoint 时 speech-key 只剩按钮，space-between 让它落在左端）。
**修复**（本地未提交，等用户通知）:
- `page.js`：发言内容（旧/流式两条渲染路径）改用 renderExplainText 轻量 Markdown 渲染（**加粗、- 列表、【】标题、--- 分隔线→.markdown-hr）；流式打字中显示原文、完成后渲染
- `page.css`：.explain-btn 加 margin-left:auto（无论有无总结都靠右）；新增 .markdown-hr
**验证**: 渲染逻辑单测：** 全部被消费为加粗、列表正常、--- 变为分隔线；dev 编译通过。

## 2026-08-12（第一百四十九批）

### 修复：小白解释偶发"解释生成失败"
**用户反馈**: 点小白解释报"解释生成失败，请稍后重试"。
**原因**: DeepSeek 偶发生成慢/断流（日志见一次 /api/explain 耗时 15s），原路由 25s 超时或连接中断即抛错。
**加固**（本地未提交，等用户通知）:
- `route.js`：超时 25s→45s；首次请求失败自动重试一次；有部分内容时错误标记 partial
- `page.js`：已生成部分内容时保留文字、仅显示小提示"⚠️ 解释生成中断，可再次点击重试"；完全失败才整屏报错；新增 explainWarn 状态与 .explain-warn 样式
**验证**: 接口实测 4s 正常结束（done:true）；dev 编译通过。

## 2026-08-12（第一百五十批）

### 新增：针对某位大师发言的"回复 → 大师回辩"（可多轮）
**用户需求**: 像聊天一样，针对某个大师的发言回复，大师再回辩，内容流式往下展示。
**实现**（本地未提交，等用户通知）:
- `prompts.js`：新增 `buildReplyPrompt(question, master, userReply, context)`（单对单辩论，含数据偏好 dataRule）
- `page.js`：每条大师发言气泡的 💡 行新增「💬 回复」按钮；点击后气泡下方出现内联回复框（textarea + 发送/取消，Enter 快捷发送）；发送后复用 followUp 轮次 + 打字机流式揭示：先显示"回复 X：…"，再流式打出 X 的回辩；支持对同一大师多次回复（多轮）
- 复用现有 rounds/blocksFromRounds 渲染，不动核心结构
- `page.css`：.reply-btn/.reply-box/.reply-input/.reply-actions/.btn-reply-send/.btn-reply-cancel
**验证**: 缠中说禅回辩实测（用户质疑跌破4.8/底分型确认 → 用30分钟降维/底背驰/放量破位/三根K线确认/量能/二买回应并给明确立场）；生产构建通过。
**文档**: 待更新 PRD（功能清单）。

## 2026-08-12（第一百五十一批）

### 优化：大师发言生动化（数据少而精 + 激活人设）
**背景**: 用户反馈嘉宾像"数据罗列师"，全是指标数字、读起来干燥。根因：提示词三处硬性"必须用数据（PE/PB/ROE）"，但没有一条要求生动表达，AI 默认堆数据。
**实现**（本地未提交，等用户通知）:
- `prompts.js` 新增统一 `VIVID_RULE`：像大师本人开口说话，善用比喻/类比/生活化场景/金句；数据少而精——每个论点最多 1-2 个关键数据点，融进观点作佐证，不堆数字
- 注入四处：buildOneSpeechPrompt（群聊）、buildFollowUpPrompt（追问）、buildChatPrompt（单聊小专题）、buildReplyPrompt（回复回辩）；裁决加"生动收尾可用金句/比喻"
- 数据规则不变（仍要求有依据），只是表达方式升级
**验证**:
- 缠中说禅回辩：跌破4.8 →"所谓支撑不过是散户心里的那把尺子""止损不是价格，是结构破坏""你盯着价格，我盯着多空力量的转换"
- 巴菲特单聊：汉堡店/护城河/印钞机/大象比喻，数据仅 ROE/毛利率/PE15倍vs同行8倍/市值1.68万亿 四个点，金句收尾
- 两例均结构清晰、在人物、不再干燥
**文档**: 待更新 PRD（表达层）。

## 2026-08-12（第一百五十二批）

### 修复：发言 JSON 解析报错（Expected ',' or '}' after property value）
**用户反馈**: 抛异常 ⚠ Expected ',' or '}' after property value in JSON at position 29。
**原因**: 生动化鼓励金句/引用后，AI 在 JSON 的 content/summary 里更容易写出**未转义的英文双引号**（如 他说"xxx"），导致前端 JSON.parse 失败。
**修复**（本地未提交，等用户通知）:
- `prompts.js`：三个 JSON 输出提示词（发言/裁决/追问）加约束——字符串值里的引号一律用中文「」或“”，禁止英文双引号，防止破坏 JSON
- `page.js`：新增 safeJsonParse 容错解析（取首个 {...}，解析失败返回 null）；发言/裁决/追问三处解析失败时降级为整段文字（不崩溃）
**验证**: dev 编译通过、/api/chat 正常；双保险（提示词少犯错 + 前端不崩）。

## 2026-08-12（第一百五十三批）

### 微调：回复改「举手提问」+ 回辩加 loading 提示
**用户反馈**: ① 按钮"回复"改"举手提问"；② 举手提问后等大师回答时没有 loading 动效。
**实现**（本地未提交，等用户通知）:
- 按钮「💬 回复」→「💬 举手提问」；用户消息标签"回复 X："→"举手提问 X："；输入框 placeholder"向 X 举手提问…"
- 新增 replyingToName 状态：发送举手提问时记录目标大师名，API 等待期间在用户消息下方显示"X 正在组织回答…"（复用 followup-hint-inline 样式），完成/失败后清空
**验证**: dev 编译通过。

## 2026-08-12（第一百五十四批）

### 微调：举手提问图标改 ✋ + 按钮位置恒靠右
**用户反馈**: ① 举手提问图标换成举手标志；② 按钮位置不统一（有的居中有的偏右），要求都偏右。
**实现**（本地未提交，等用户通知）:
- 按钮「💬 举手提问」→「✋ 举手提问」
- `.speech-key` 由 justify-content: space-between 改为 **flex-end**（💡 文字用 flex:1 占左、按钮组恒靠右），杜绝因有无 keyPoint 导致的布局差异
**验证**: dev 编译通过。

## 2026-08-12（第一百五十五批）

### 所有回答统一带 keyPoint（💡 核心观点）
**用户需求**: 群聊、单聊、举手提问的回复都带上 keyPoint。
**实现**（本地未提交，等用户通知）:
- `prompts.js`：buildChatPrompt（单聊小专题）与 buildReplyPrompt（举手回复）改为输出 JSON `{content, keyPoint}`（keyPoint 15-25字一句话）；群聊发言本就带 keyPoint
- `page.js`：新增 parseChatResult 统一解析（取 content + keyPoint，失败降级整段文字）；三处接入——单聊首轮、追问单聊、举手回复
**验证**: 单聊小专题输出 keyPoint"短期底背驰反弹，中期需突破5.8-6.0元确认…"；举手回复 keyPoint"跌破4.8不必恐慌，看是否形成底背驰…"；dev 编译通过。

## 2026-08-12（第一百五十六批）

### 重构：举手提问改为"内联线程"，精确插在被回复发言正下方
**用户反馈**: 举手后页面停留在原回答，回复内容被追加到讨论最底部，用户不知道在哪；回复框也未精确出现在原回答正下方（此前按大师 id 定位，同一大师多条发言时定位不准）。
**重构**（本地未提交，等用户通知）:
- 每条发言生成稳定 key（`大师id|内容前24字`），回复框与线程都按 key 精确定位到对应发言下方（跨流式/rounds 模式稳定）
- 新增 `replyThreads` 状态：发送举手提问 → 在被回复发言正下方**乐观插入线程**（先显示"✋ 举手提问 X：…"+ loading"X 正在组织回答…"），回辩完成后线程内显示回答 + 💡 keyPoint + 可继续"✋ 举手提问"/"小白解释"（多轮嵌套）
- 去掉旧的"追加 followUp 轮次到最底部"方案；底部追问提示还原为仅追问使用
- CSS：.reply-thread（左竖线缩进）/loading/error/answer（淡入动画）
**验证**: dev 编译通过、生产构建通过。

## 2026-08-12（第一百五十七批）

### 重构：举手提问改为「侧边浮层单聊」（放弃内联线程方案）
**背景**: 内联线程方案（插在被回复发言下方）实现复杂且多次出问题（变量遮蔽 t、位置定位等），用户决定改为更简洁的侧边浮层。
**实现**（本地未提交，等用户通知）:
- 点「✋ 举手提问」→ 右侧滑入抽屉（复用 explain-drawer 外壳 + chat-drawer 样式），与所选大师一对一单聊
- 抽屉：头部（头像/姓名/称号/关闭）+ 消息区（用户消息靠右蓝底、大师回复靠左气泡 + 💡 keyPoint，打字机逐字揭示 + 自动滚底）+ 底部输入框（Enter 发送）
- 多轮：可在抽屉内连续提问，大师连续回辩；带"正在组织回答…"loading
- 移除内联回复框/线程相关代码（replyTarget/replyThreads/replyBoxFor/replyThreadsFor/blockKeyOf）
- 修复过程中一次热重载中间态报错（replyTarget 未定义）——最终文件已无残留、生产构建通过
**验证**: 首页 200、生产构建通过。

## 2026-08-12（第一百五十八批）

### 微调：举手提问浮层贴原始发言 + loading 文案
**用户反馈**: ① 浮层需要把被回复的原始回答内容贴上来；② "正在组织回答…"改为"正在疯狂打字中...."。
**实现**（本地未提交，等用户通知）:
- 浮层消息区顶部新增「你在回复 X 的发言：」+ 原始发言内容（灰色引用卡片，可滚动）
- loading 文案：抽屉内「X 正在疯狂打字中....」，发送按钮「疯狂打字中…」
**验证**: dev 编译通过。

## 2026-08-12（第一百五十九批）

### 微调：主持人发言字号与颜色修正
**用户反馈**: 主持人发言字号太小、颜色不对。
**原因**: .host-block p 用 12.5px + --text-muted（灰），比大师发言（14px、正文色）小且淡。
**修复**（本地未提交，等用户通知）: .host-block p → 14px、--text 正文色、行距/内边距微调；保留居中胶囊样式。
**验证**: dev 编译通过。

## 2026-08-12（第一百六十批）

### 新增「纯白」主题（三主题循环：暗色 → 亮色羊皮纸 → 纯白）
**背景**: 用户反馈页面（羊皮纸颗粒/暗色）长时间看累、看不清，想要纯白背景选项。
**实现**（本地未提交，等用户通知）:
- `globals.css`：新增 `[data-theme="white"]` 纯白主题（--bg-page #ffffff、纯白卡片、浅边框、正文深色），并关闭羊皮纸颗粒/暖晕（body::before display:none）
- `layout.js`：首帧脚本允许 'white'
- `page.js`：主题按钮改为三态循环（dark→light→white→dark），图标 🌙/☀️/⚪，title/aria 对应文案；读取 localStorage 支持 'white'
**验证**: dev 编译、生产构建通过。

## 2026-08-12（第一百六十一批）

### 修复：浮层内滚动带动背景滚动
**原因**: 抽屉/弹层内部滚动到底后，滚动事件继续传导（scroll chaining）到背后页面。
**修复**（本地未提交，等用户通知）: 给 .drawer-overlay/.modal-overlay/.explain-drawer/.chat-drawer/.invite-drawer 及各自 body 加 `overscroll-behavior: contain`，切断滚动链。
**验证**: dev 编译通过。

## 2026-08-12（第一百六十二批）

### 默认主题改为纯白
**用户反馈**: 纯白看着舒服，设为默认。
**实现**（本地未提交，等用户通知）: page.js 主题默认 'light'→'white'；layout.js 首帧脚本无存储/非法时默认 'white'。老用户 localStorage 存的是旧值则沿用旧值。
**验证**: SSR data-theme="white"，dev 编译通过。

## 2026-08-12（第一百六十三批）

### 整体测试 + 修复：平台 K 线无回退链
**测试清单**（生产构建 + 生产服务器冒烟）:
| 项目 | 结果 |
|---|---|
| 生产构建 npm run build | ✅ 通过 |
| 首页 | ✅ 200（SSR 默认纯白主题） |
| /api/context 茅台 | ✅ 深度快照/估值分位/DCF/技术面/缠论视角 全在 |
| /api/match-masters | ✅ 缠论问题命中 chan_master（有一次偶发空→重试正常，接口容错返回[] 前端回退随机，非 bug） |
| /api/explain 流式 | ✅ SSE 正常 done |
| /api/chat 群聊发言 | ✅ 合法 JSON + keyPoint + 生动表达 + 无未转义英文引号 |
**发现并修复的 bug**: 平台 K 线获取（uziSkills.fetchKlineEM）只有东财一个源，东财被反爬时导致技术面/缠论整块缺失 → 已加「东财→腾讯→新浪」三级回退（与本地 czsc 服务同款）；实测修复后茅台技术面+缠论恢复。
**未提交改动**（等用户通知提交）: src/app/page.js、page.css、lib/prompts.js、globals.css、layout.js、api/explain/route.js、api/chat/uziSkills.js、memory/log.md（共 8 项，memory/project.md 除外）

## 2026-08-12（第一百六十四批）· 发版总结

### 已发布到生产（commit 7393059，Vercel 自动部署中）
**发版内容**:
1. **单聊小专题**：先结论 → 按维度分节（短期/中期/中长期）→ 操作/风险提示，400-550 字，带 💡 keyPoint
2. **流派数据偏好（dataFocus）**：缠论派以结构/技术面为主、基本面仅一句话风险提示；其他流派走默认估值指标
3. **生动表达（VIVID_RULE）**：比喻/类比/金句激活人设，数据少而精（每论点 1-2 个关键点）
4. **举手提问 = 侧边浮层单聊**：贴原始发言、打字机回辩、多轮、loading"正在疯狂打字中"
5. **所有回答统一带 keyPoint**；JSON 引号约束 + 前端 safeJsonParse 容错
6. **纯白主题**（暗色/羊皮纸/纯白三循环，默认纯白）+ 主持人发言 14px 正文色
7. **浮层滚动隔离**（overscroll-behavior: contain）
8. **K线三级回退**（东财→腾讯→新浪，修复东财反爬导致技术面/缠论缺失）
9. **小白解释加固**：45s 超时 + 重试 + 部分内容保留提示
10. **大师库/能力包/缠论服务/智能选角/分组多标签**等（此前已发）
**验证**: 生产构建通过；首页/快照/选角/流式解释/群聊发言 冒烟全过；交互类（浮层单聊、主题切换、滚动隔离）待用户线上复核。
**说明**: 此条 memory 更新本身未提交（等用户通知）。

## 2026-08-14（早餐圆桌）· 新增栏目

### 新增：巴菲特早餐圆桌（/breakfast）
**定位**: 独立栏目页，不碰主页面（page.js 仅加了一张入口卡片 + Link import）。过去 24 小时新闻列表 → 默认展开第一条 → 巴菲特主持 + 按流派随机 3 位嘉宾圆桌穿透解读；切换新闻即重新解读；支持用户粘贴新闻。
**核心设计**:
1. **按流派各抽一位嘉宾**：从 价值/宏观/成长/中国价值/游资/短线/技术趋势/科技领袖/量化 等流派池随机抽 3 个不同流派，各抽 1 位大师（排除巴菲特，换嘉宾时排除当前阵容），保证观点碰撞。
2. **穿透性框架**：Prompt 内置「六镜头」——利益结构+链式传导 / 预期差+时间尺度 / 反着问+可证伪，三位嘉宾各分配一个镜头；产出固定三件套：事实核查 → 交锋 → 巴菲特收束 + 机会信号裁决（机会强度 0-10、机会点、可验证信号、主要风险、共识）。
3. **数据支撑**：接口复用 quoteContext（新闻里解析公司 → 拉最新行情/财务/深度分析快照），发言引用真实数据，严禁编造。
**新增文件（全部为增量，无改动既有逻辑）**:
- `src/data/sampleNews.js`（6 条示例新闻，明确标注演示用，后续可接自动抓取）
- `src/lib/breakfast.js`（getHost / pickGuestsByStyle / findMasterById）
- `src/lib/prompts.js`（追加 buildBreakfastPrompt，未动旧函数）
- `src/app/api/breakfast/route.js`（新闻→快照→DeepSeek→JSON 容错重试一次）
- `src/app/breakfast/page.js + page.css`（新闻列表/嘉宾栏/打字机圆桌/裁决卡/粘贴新闻/换嘉宾/跳过）
- `src/app/page.js + page.css`（首页入口卡片，仅此一处改动）
**验证**: `npm run build` 通过；本地 dev 实测 首页 200 / /breakfast 200（SSR 含入口卡片与页面关键元素）；`/api/breakfast` 端到端 200（英伟达新闻 → 自动拉 NVDA 行情快照 → 完整四段 JSON：hostOpening/discussion×3/hostClosing/verdict，机会强度 7/10）。交互类（打字机/切换新闻/换嘉宾/粘贴）待用户浏览器复核。
**待办**: 真实新闻源接入（24h 抓取）可参考 FinnewsHunter 模式；示例新闻需在接入真实源后替换。

## 2026-08-14（早餐圆桌 · 第一批优化）· 依据 impeccable 双代理评审（24/40）落地

### 已完成
1. **首页入口卡 → 克制栏目条**：去掉金色渐变横幅/大箭头/"新栏目"徽章，改为单行扁平栏目条（等宽「今日圆桌」胶囊 + 衬线标题 + 一行描述，14px 圆角对齐产品卡，极淡金底 + 细金边）；移动端只留标题一行。位置不变（标题下方、主工作区上方）。
2. **播放控制力（P1）**：加载期可「■ 停止」（AbortController 中止，停止后不自动重试）；「跳过/再来一轮」常驻动作条（不再条件渲染防布局跳动）；已读完（revealed）切走再切回整块直接显示不重播；中途切走会保存播放进度（progress），切回从断点继续；修复了结果返回后打字机可能不启动的 bug（visibleCount 未从 0 起步）。
3. **切换新闻不归零（P1）**：结果按「新闻×嘉宾」缓存，切回即现；同 key 去重 + force 重跑时中止在途请求；自动吸底仅在用户靠近底部时触发，回读不再被拽回。
4. **粘贴新闻（P1）**：解析预览卡（标题取第一行/正文预览，可回上方修改）、重复标题查重提示并禁用提交、提交防连点、用户投稿持久化到 localStorage（标注「本地保存」）。
5. **一致性**：「VS」改为「对阵」；keypoint 左侧金条补 impeccable-disable 注释。

### 验证
- `npm run build` 通过；本地 dev 首页/圆桌页 SSR 200，旧元素（箭头/新栏目徽章）已移除。
- impeccable 检测器扫描 4 个 UI 文件 → `[]`，exit 0（上一轮唯一 side-tab 误报已消）。
- 交互类（停止/跳过/断点续播/切回不重播/粘贴预览/查重）属浏览器行为，待用户复核；接口未改动。

### 待办（第二批，未做）
- 嘉宾条两行结构（主持人独立一行 + 嘉宾成组）、换一批需轻确认或保留上一批可切回；
- 裁决卡「金印」化（粗金边/顶部饰条、星级补数字、移动端信号/风险优先两格）；
- 打字机「直接看裁决」快捷路径（P1 中提到的效率项，先留到第二批）。
- 2026-08-14（第一批微调）：入口卡由通栏栏目条进一步压成紧凑胶囊（今日圆桌 + 标题，去描述，自适应宽度，18px 左对齐）。
- 2026-08-14（第一批微调2）：入口胶囊去掉「今日圆桌」文字，改为右对齐（bk-entry-row 包裹，与主内容区同宽右对齐）。

## 2026-08-14（首页选人/聊天方式简化）· 用户要求调整
- **移除「智能匹配/手动选择」两种模式按钮**：保留「选择成员」标题，列表始终可手动勾选。
- **新增提示**：「未手动选择时，系统将根据您的提问自动匹配最适合回答问题的大师」（i18n zh/en membersAutoHint）。
- **移除「群聊/单聊」切换开关**：聊天方式改为自动判定——1 位大师=单聊、≥2 位=群聊；未选人时提交先自动匹配，再按人数定模式。
- **大师详情页「与 TA 单聊」保留**：仍一键进入该大师的一对一深聊。
- 清理：chatMode/selectMode state 及其 localStorage（chat-mode-v1）、chat-switch/sidebar-mode/smart-locked CSS 全部移除；go() 不再有群聊/单聊人数报错分支（自动判定后不再出现）。
- 验证：build 通过；首页 SSR 无模式按钮/聊天开关，新提示文案生效。

## 2026-08-14（首页双 Tab 方案 A）· 用户确认
- **首页改为双 Tab（同页切换）**：顶部 Tab 栏「提问智囊团 / 🥐 早餐圆桌」，替换原右上角胶囊入口（胶囊与 bk-entry CSS 移除）。
- **圆桌工作台内嵌首页**：早餐页逻辑抽成 `src/components/BreakfastRoundtable.js`（去掉独立页头/返回链接；加 `active` prop，仅 Tab 激活时才自动发起解读，避免后台白耗 API）。首次激活后常驻挂载（display:none 隐藏），切换 Tab 不丢圆桌/提问状态。
- **URL 同步**：Tab 切换用 history.replaceState 同步 `?tab=breakfast`；刷新/分享保留；`/breakfast` 改为 307 跳转到 `/?tab=breakfast`（旧链接仍可用）。
- 圆桌样式由 page.js 统一导入 `breakfast/page.css`（组件不再各自引 CSS）。
- 验证：build 通过；`/` 200 含双 Tab 无胶囊；`/breakfast` 307→`/?tab=breakfast`；`/?tab=breakfast` 200。Tab 点击切换/状态保留属浏览器行为待复核。
- 2026-08-14（Tab 位置调整）：通栏顶部 Tab 改为**紧凑分段控件**，提问工作台放在主列输入框正上方（AppTabs 组件，aria-pressed）；圆桌工作台经 tabs prop 渲染在 bk-layout 首行（bk-tabs-row 跨两列）。移除 header 下通栏 nav；圆桌 Tab 激活时整个提问工作台（含侧栏）隐藏，圆桌占满整行（原有结构已满足）。Tab 改名：大师PK / 巴菲特的早餐·新闻解读。

## 2026-08-15（场景化导航 + 氛围感玻璃化）· 用户确认开始优化
**产品框架**：人与大师的交互（非人与信息）。三大场景：大师PK（多视角辩论）/ 巴菲特的早餐（新闻解读找机会，借势"巴菲特的午餐"梗）/ 能力专项研究（基本面、技术面等，未来扩展）。
### 已做
1. **场景栏**：去掉"输入框上方/圆桌顶部"两处重复分段控件，改为**顶部居中悬浮毛玻璃胶囊**（scene-nav：半透明 + blur(18px) + 金边 + 阴影），含 大师PK / 巴菲特的早餐·新闻解读 / **探索⌄下拉**（占位展示 基本面/技术面/估值/行业/公司对比/组合诊断，标注"即将上线"，结构为未来扩展预留）。
2. **背景透出**：bg-argue 提亮（亮色 0.22→0.27、暗色 0.3→0.34），mask 更柔，右侧巴菲特/芒格形象更可辨；右上角保持通透。
3. **玻璃面板**：globals 新增 --glass-card/--glass-main/--glass-strong（三主题）；早餐页面板（新闻卡/舞台头/嘉宾条/对话区/气泡/裁决卡）+ 提问输入框 半透明 + backdrop-blur；@supports 降级（不支持 backdrop-filter 回退实色保可读）。
4. 移除 AppTabs 双份渲染与 bk-tabs-row；AppTabs 重命名为 SceneNav（useRef 点击外部关闭下拉）。
### 验证
- build 通过；`/` 与 `/?tab=breakfast` 200；SSR 含 scene-nav/两场景/探索菜单。
- impeccable 检测器扫描 4 个 UI 文件 → 0 问题。
- 视觉/下拉/状态保留待浏览器复核。
- 2026-08-15（Tab 位置/样式微调）：去掉「新闻解读」角标；探索下拉此前已隐藏；场景栏改为**紧凑小 Tab**，嵌入提问卡标题行（您的投资问题 | 分隔线 | 大师PK/巴菲特的早餐），圆桌工作台顶部同款靠右一行（bk-tabs-row 恢复）。SceneNav 不再悬浮顶部，移除顶层实例。
- 2026-08-15（输入框可读性·设计skill判断）：不再在输入框上纠结厚薄——按 impeccable craft-floor（占位文字对比度≥4.5:1；玻璃作为有作用的层）与 demo 做法，**玻璃上移一层**：提问卡容器用 var(--glass-main) + blur(14px) 做毛玻璃面板（人物透出、画面稳定），输入框内部透明无边框，占位文字落在稳定玻璃面上自然清晰；聚焦时卡片金边高亮。移除占位文字光晕补丁。detector 0 问题。

## 2026-08-15（巴菲特的早餐 · 升级为事件穿透投资框架）· 用户提供提示词，逐步推演
- **框架落地**：新增 `src/lib/framework.js`（9 步：第零步事件核实 / 排除法 / 追钱法 / 拆解法 / 穿透法 / 验证法 / 标的落地 / 操作节奏 / 最后小结；每步含完整规则 + 负责人分工：巴菲特=第零/四/六/小结，嘉宾A=排除/验证，嘉宾B=追钱，嘉宾C=拆解/操作）。
- **逐步生成**：`/api/breakfast` 改为单步模式（stepKey + prevSteps 上下文，max_tokens 6000，JSON 修复一次 + 原文兜底）；前端逐步上屏，每步卡片=负责人头像+标题+正文+「想深挖？」追问 2-3 条；可停止/再来一轮/换嘉宾。
- **风险声明（用户强制要求）**：圆桌顶部常驻红色风险横幅（AI 生成·仅分析框架·不构成投资建议·风险自负·与开发者无关）；第六步标的落地 prompt 强制先输出风险声明再给标的。
- 已验证：build 通过；第零步（巴菲特核实）、第一步（索罗斯排除法，1748 字+追问）端到端 200；检测器 0 问题。全 9 步连跑与 UI 展示待浏览器复核。
- 2026-08-15（早餐页改输入框优先 + 手动解读 + 链接抓取）：
  - 左栏新闻列表改为**单列布局 + 新闻输入卡**（与大师PK提问卡同构：textarea + 右下角「开始解读」，金色按钮样式一致）；支持**粘贴新闻链接或文本**，示例新闻改为快捷点选 chips。
  - 链接支持：路由内自动抓取页面标题/描述（8s 超时，失败回退原文）；文本取第一行作标题。
  - 结果按「内容指纹×嘉宾」缓存，同内容重复粘贴直接命中；换嘉宾重跑。
  - 稳定性：DeepSeek 超时 60s→120s + 自动重试一次；JSON 解析失败时原文兜底不中断。
  - 已验证：文本步骤 0/1 端到端 200；链接抓取（example.com）端到端 200。
- 2026-08-15（圆桌美化 + 邀请大师 + 可扩展）：
  - 圆桌改为**金质圆桌**（渐变金圆盘 + 内环光影 + 「今日圆桌」），席位**沿椭圆圆周自动均匀分布**（人数变化自动重排），主持人巴菲特正上方（金色光圈强调），嘉宾席位带金环头像 + 姓名/流派。
  - 新增「＋ 邀请大师」：可追加随机大师上桌（最多 6 位），并保留「🎲 换一批」；嘉宾牵头步骤（排除/追钱/拆解/验证/操作）改为**按名单轮转**（guest0..4 取模），新嘉宾也会牵头步骤；单步 prompt 注入「在场嘉宾」名单。
  - 空对话容器仅在非 idle 时渲染；底部免责横条与小字声明均已删除。
- 2026-08-15（圆桌样式改为横向座位排，参考用户截图）：去掉金质椭圆，改为「头部信息行（今日圆桌 + ＋邀请大师/🎲换一批）+ 横向一排座位（圆形金环头像 + 姓名 + 流派，主持人首位带金环与「主持」徽章）」，人数变化自动换行。邀请上限 6 位，框架嘉宾步骤按名单轮转不变。
- 2026-08-15（浅/深解读开关 + 默认选人 + 文案清理）：
  - **浅度/深度开关**：输入卡底部左侧新增胶囊分段开关（浅度/深度）。浅度=单次快速解读（相关性初判🟢🟡⚪ + 2-3位嘉宾快速观点 + 巴菲特"是否值得深挖"结论，300-450字）；深度=原事件穿透框架 9 步逐步推演。API `/api/breakfast` 新增 `mode:'quick'|'deep'`（默认 deep），新增 `buildQuickBreakfastPrompt`；缓存 key 加入 mode，切换模式自动中止旧任务。
  - **默认选人修复**：`pickGuestsByStyle` 新增 `realAvatarOnly` 选项（只抽 avatar 为本地图片路径的大师）；早餐圆桌默认/换一批/邀请大师均只抽有真人头像的大师，避免 A股游资整组（12 位均为 SVG 字母占位头像）随机命中导致"占位符"观感。
  - **文案清理**：删除左侧新闻列表头的「点击填入」；删除输入框下方「支持链接或文本 · 链接会自动抓取标题」；同步清理 CSS（.bk-news-list-hint/.bk-input-hint）。
  - 验证：node --check 三个 JS 文件通过；页面 200、API 400 校验分支正常；客户端 bundle 含 bk-mode-switch/浅度，hint 类已移除。
- 2026-08-15（修复圆桌头像"?"bug）：圆桌座位数组直接 `[host, ...guests]`，嘉宾是 `{master, groupKey}` 包装对象，却被当作大师对象传给 MasterAvatar（拿不到 avatar/name → 回退显示"?"，姓名也为空）。改为统一座位结构 `{master, groupKey, isHost}`，头像/姓名改用 p.master.*，key 用 p.master.id。已确认页面 200、头像静态资源 200。
- 2026-08-15（早餐页改整页滚动）：去掉两处区间内滚——左栏新闻列 `.bk-news-col`（sticky+calc(100vh-120px)+overflow-y）改为普通列随页滚动；对话区 `.bk-dialog`（max-height 62vh+overflow-y）改为自然撑开。同步移除组件内 scrollRef/stickToBottom 自动吸底逻辑与 useEffect 依赖。已验证页面 200。
- 2026-08-15（核心结论前置 + 详情可折叠 + 框架小结升级）：
  - 框架最后一步由「最后·投资认知小结」升级为结构化「核心结论」（step.structured）：AI 输出 JSON（一句话反直觉结论 / opportunities 机会点列表≤3 / action 买卖止损周期 / risk / followUps）。buildFrameworkStepPrompt 按 structured 切换 JSON 指令；route 的 generateJson 支持自定义 schemaHint，结构化步骤透传字段。
  - 前端：完成后「核心结论」卡置顶（金色渐变卡：一句话结论 + 机会点带 tier/代码/逻辑/风险 + 操作行（能买/在哪买/亏到哪跑/周期）+ 风险行），8 步详情默认折叠成标题行（点击展开/收起，带 ▸/▾），生成中步骤保持展开；浅度结果（type:'quick'）同样始终展开。
  - 框架优化方向（待用户挑选）：初筛闸门前置、机会候选池贯穿各步、缩短追钱→拆解→穿透链、可证伪红线配对、快照注入大盘环境。
- 2026-08-15（框架五大优化全部落地）：
  1. **初筛闸门**：深度模式第一卡为「初筛 · 相关性判断」（巴菲特，结构化 verdict 🟢/🟡/⚪）；⚪ 返回 stop=true，前端立即完成、不跑后续步骤（省 token）。已冒烟：宁德时代新闻 → 🟢 强相关。
  2. **机会候选池贯穿**：step1..step6 每步 JSON 输出 pool（name/code/tier/logic/risk/falsify/status/reason，≤8 项），prevSteps 透传上一轮候选池，prompt 强制增量更新；展开的步骤卡内展示候选池 chips（排除/保留/落地 着色）。已冒烟：追钱拆解返回 5 项候选池。
  3. **压缩中间链**：追钱法+拆解法合并为「第二步 · 追钱拆解」（拆解只做刚性/弹性标注）；框架由 8 步 → 初筛+7步+核心结论，共 9 次调用（合并省下的名额补给初筛，总调用数不变）。
  4. **可证伪红线**：穿透法起每个机会点必须给 falsify（出现什么信号逻辑被推翻）；核心结论卡机会点新增「证伪信号」行。已冒烟：结论含 falsify。
  5. **快照注入大盘环境**：marketData 新增 getMarketOverview（上证/深成/创业板/科创50 点位+涨跌幅+成交额+沪市涨跌家数），quoteContext 快照头部注入【大盘环境】；操作节奏 step6 提示优先引用大盘判断情绪面。
  - 稳定性：新增 unwrapNested 解包（模型把内层 JSON 塞进 content 字符串时自动合并字段），quick/gate/conclusion/pool 步骤统一走解包。修复过程中遇到 quickParsed 重复声明 + generateJson 误改 normalized 的编译/运行时问题，已解决。
  - 验证：node --check 全过；gate/step2/conclusion 三个真实接口冒烟 200；页面 200。
- 2026-08-15（早餐页接入真实新闻 API）：
  - 新增 `src/app/api/news/route.js`：GET /api/news?page=N，双源并行——财联社电报（公开缓存接口，sha1+md5 签名）+ 东方财富 7x24 快讯（`var ajaxResult=` 包裹文本，正则取 JSON）；按标题+id 去重、按时间倒序、任一源失败降级、双源失败返回空（前端回退内置示例）。8s 超时 + UA/Referer。
  - **踩坑**：CLS 实际字段是 `data.roll_data`（不是 telegraph/roll）；EM 响应是 `var ajaxResult={...}` 非纯 JSON（res.json 会抛）；EM 时间字段是小写 `showtime`。三者不修则返回空列表。
  - 前端接线：BreakfastRoundtable 挂载即 fetch page=1，加载中先展示内置示例占位，成功后无缝换成实时列表；顶部「↻ 刷新」+ 底部「加载更多」分页（hasMore 时显示）；失败显示「实时源暂不可用，展示示例」。左栏标题「📰 默认新闻」→「📰 今日快讯」。
  - 验证：escalated curl page1=57 条（去重后）、page2=50 条均 200；bundle 含 今日快讯/加载更多/api/news 标记。
- 2026-08-15（早餐圆桌·追问可点击提问）：
  - 「想深挖？可以继续问我：」每条追问改为可点击按钮（＋ 前缀、虚线边框、hover 金色），点击即向该步骤负责人直接提问，无需复制文本；标签文案改为「想深挖？点击即可追问：」。
  - 新增 `/api/breakfast` stepKey='followup' 分支：单次简短问答（max_tokens 1600），由该步骤 lead 大师回答（250-400字）+ 巴菲特一句 hostNote 补充（lead 就是巴菲特时省略）；复用快照/候选池上下文，prompts 新增 buildFollowupPrompt。
  - 前端在对话区追加「💬 追问」轮次：问（金色 badge）+ 回答（大师头像/姓名）+ hostNote；loading 点点动画、失败可重试（按 fuId 原位替换）。followups 存在 cache 条目内、按缓存 key 隔离；不覆盖正在生成的步骤状态。
  - 已验证：页面 200、bundle 含新标记；followup 接口冒烟 200（林奇回答含快照数据 + 巴菲特补充「护城河不是合同，是技术」）。
- 2026-08-15（早餐圆桌·改为圆桌讨论式解读）：
  - 用户反馈原框架步骤是「单人大段独白」，没有圆桌讨论感。重构：每个框架段（含初筛/7步/结论/浅度）改为**一段圆桌讨论**——巴菲特抛题、嘉宾「抢答」式精简发言（2-4句、≤80字）、巴菲特中间穿插 1 次提问/点评控场、最后收束总结；嘉宾可用「我先来——」「我不同意」「补一刀」等开场，巴菲特用「收一下，我们进下一段」等控场。每人画像+流派风格注入。
  - JSON 增加 `turns:[{speaker:'host'|'guestN',text}]`（route 新增 extractTurns 校验），各步结构化输出不变：gate 保留 verdict/reason/stop、pool 步骤保留 pool、结论保留 opportunities/action/risk、followUps 保留。quick 也改为 turns+verdict。
  - 工程点：generateJson 增加 requireContent 参数（gate/quick 无 content 也判合法，避免误触发修复二次调用）；route 对 gate 的 content 校验放宽（turns/verdict 任一即可）；prompts 重写 buildFrameworkStepPrompt / buildQuickBreakfastPrompt（移除已无用的 resolveLeadForPrompt）。
  - 前端：新增 turnSpeaker 解析 + turnsBlock（.bk-discussion/.bk-turn，巴菲特金色主持行），gate/结论/普通/浅度卡统一渲染轮次（有 turns 时正文 content 作兜底不重复展示）；普通卡 header 支持 verdict 徽章（浅度）。
  - 已验证：gate（4轮：抛题→嘉宾判断→插一句→收束🟢）、conclusion（3轮+opportunities 2项+action）、quick（4轮含「我不同意」反驳）均 200；页面 200、bundle/css 含新标记。
  - token 评估：仍是 9 次调用，轮次精简后单段体量与此前长独白相当或更少。
- 2026-08-15（早餐圆桌·收敛为「一人负责一块、发言式展示」）：
  - 用户反馈上一版每段多人来回讨论 + 徽章/候选池/结论框展示太乱。改为：**每块由一位负责人以「发言」形式输出**（gate/step0/step3/step5/conclusion=巴菲特，step1=guest0、step2=guest1、step4=guest2、step6=guest3），prompt 强调第一人称、口语化、不强行用标题/编号/加粗组织，就是一段自然发言（300-600字）；初筛/收束的发言里要自然带出 🟢🟡⚪ 和标的/买卖区间/止损。
  - 背后逻辑仍高度结构化：gate 的 verdict/reason（stop 逻辑）、pool 步骤的 pool（候选池增量）、conclusion 的 opportunities/action/risk 都在 JSON 里保留、供后续承接与追问，只是**前端不再展示**这些结构（去掉 verdict 徽章、候选池 chips、结论机会点/操作/风险框、讨论气泡）。
  - 前端统一为「发言卡」：头像 + 负责人姓名 + 步骤标题 + 一段发言（可折叠，结论/初筛/浅度常开置顶）；追问保留为轻量可点击文本（去掉虚线框）。移除 turnSpeaker/讨论 CSS。
  - 已验证：gate（巴菲特自然发言含🟢）、step1（林奇发言式排除法，pool 4 项在背后）、conclusion（巴菲特收束发言 + action 在背后）、quick 均 200；页面 200，bundle 只含新发言卡类名，无 bk-step-pool/bk-discussion。
- 2026-08-15（早餐圆桌·loading 改为「某某大师正在进行新闻分析」且放进发言位）：
  - 去掉顶部居中「正在完成：第X步…」加载条；改为**发言位内 loading**：在步骤流末尾（下一步发言的位置）渲染一张 loading 发言卡——头像 + 大师姓名 + 「正在进行新闻分析…」点点动画 + 当前步骤名小字。
  - 实现：runAnalysis 在初始化/每步完成后记录 currentLead（resolveLeadId 把框架步骤负责人 host/guestN → 大师 id），quick 恒为巴菲特；渲染时 findMasterById(currentLead) 定位大师。旧 bk-loading/bk-dot CSS 移除，新增 bk-loading-speech 样式（金色左侧强调 + 复用 bk-dot-pulse 动画）。
  - 已验证：页面 200，bundle 含 bk-loading-speech/正在进行新闻分析/currentLead，旧 loading 类已从 CSS 移除。
- 2026-08-15（loading 文案融合为一句自然动作）：
  - 把「正在进行新闻分析…」+「第一步 · 排除法」两行合并为一句，如「正在用排除法，过滤新闻」。FRAMEWORK_STEPS 每步新增 loading 字段（gate=正在初筛，判断新闻与股价的相关性 / step1=正在用排除法，过滤新闻 / conclusion=正在收束全桌，给出核心结论 / 浅度=正在进行浅度快速解读 等）；组件用 currentAction 替代 currentStep，loading 卡只显示「大师姓名 + 一句动作」。
  - 已验证：页面 200，全部 loading 短语已编译进 page.js chunk。
- 2026-08-15（浅度→快速 + 快速模式每人一句 ≤100 字）：
  - 【浅度】全局改为【快速】：模式开关按钮/提示、loading「正在进行快速解读」、步骤标题「快速解读」，prompts/route 注释同步清理。
  - 快速模式改为「每人一句的极简发言」：巴菲特抛题 → 1-2 位嘉宾各一句（可反驳）→ 巴菲特收束（含 🟢🟡⚪ + 是否深挖）。prompt 硬性要求每条 text ≤100 字符；route 的 extractTurns 再兜底 slice(0,100)；requireContent=false（无 content，turns 为唯一交付）。
  - 前端：快速卡内渲染 bk-quick-turns 短发言行（头像+姓名+一句，巴菲特金色底），无 turns 时回退 content；turnSpeaker 恢复用于解析 host/guestN。
  - 已验证：快速接口 4 轮、每条 42-47 字（≤100）200；页面 200；src 无「浅度」残留。
- 2026-08-15（快速分析也补上「总结」）：
  - 快速模式在几句短发言（≤100字）之后，新增巴菲特「总结」（summary 字段，150-250 字、不受 100 字限制）：相关性结论 + 值不值得深挖 + 深挖重点看什么。prompt/route schema 加 summary，route 兜底：模型没给时取巴菲特最后一轮收束发言。
  - 前端：快速卡内短发言下方渲染金色「总结」块（bk-quick-summary）。深度模式「核心结论」保持既有总结卡。
  - 已验证：快速接口返回完整总结（约250字，含估值/风险/深挖主线）；页面 200、bundle 含 bk-quick-summary。
- 2026-08-15（圆桌标题改名 + 深度发言精简排版）：
  - 「今日圆桌」→「巴菲特带你读新闻」。
  - 深度分析 prompt 三处调整：① 每块长度收窄（初筛 100-200 / 普通块 200-350 / 收束 250-400 字）；② 强制分段——空行拆成 2-4 个短段落、每段 1-3 句；③ 关键结论/数字/公司代码用 **加粗** 标出（renderInline 已支持 **→strong，bk-step-body pre-wrap 已支持换行，无需前端改动）。
  - 已验证：step2 追钱拆解输出 433 字、3 段、11 处加粗（如「**刚性强，第一受益**」）；页面 200，bundle 含新标题、无「今日圆桌」。
- 2026-08-15（快速模式仪式感 + 深度加粗收敛）：
  - 快速模式仪式感：① 生成期间 loading 按「巴菲特正在解读→嘉宾逐个正在解读→巴菲特正在总结」轮换（quickSpeakers 轮转，800ms/人）；② 完成后短发言**逐条揭示**（480ms/条，先出下一位大师「正在解读…」占位，最后「正在总结…」再出总结），营造依次发言的仪式感。深度模式保持逐步生成不变。
  - 深度模式加粗收敛：prompt 改为「**加粗只用于最需要关注的 1-3 处**（核心结论/关键数字/重点标的），不要大面积加粗」。
  - 已验证：step1 排除法 369 字、3 处加粗、3 段；页面 200，bundle 含 quickLoadIdx/revealCount/正在解读/正在总结/pending 样式。
- 2026-08-15（第零步移除 / 左侧新闻初筛 / 去掉决策框）：
  1. **步骤重排**：去掉「第零步」，从第一步开始计数（第一步·事件核实 … 第七步·操作节奏，核心结论不变）；内部 key 保持 step0..step6 不变，仅改 title（disclaimer 仍按 key==='step5' 判断标的落地）。
  2. **左侧新闻初筛**：`/api/news` 新增 isFinanceNews 关键词过滤——强非财经信号（时政/灾害/军事/外交/人事/文体民生，如 总书记/应急响应/航母/总统/高考 等）优先剔除；否则必须有明确财经信号（公司/营收/订单/芯片/汽车/政策/市场 等 KEEP 词）才保留，默认剔除。效果：57→30 条，噪音归零。
  3. **去掉小白决策摘要框**：第七步 prompt 里删掉 ┌──┐ 字符画表格，改为「用两三句自然语言说清 能不能买/大致区间/跌破哪里走人，不要表格框线」，信息进总结；conclusion 规则里对第七步的引用同步更新。
  - 已验证：/api/news 30 条无噪音；页面 200；bundle 含 第一步·事件核实 / 第七步·操作节奏（· 转义为 \xb7），无 第零步。
- 2026-08-15（快速解读改为「一人一条、边分析边出结论」的真实逐条生成）：
  - 快速模式不再一次性返回；改为**逐人逐条调用**：巴菲特抛题 → 每位嘉宾一句 → 巴菲特收束 → 巴菲特总结，共 N+2 次调用。每次调用返回一句（≤100字，summary 150-250字），前端在快速卡内**实时逐条追加**：当前发言者显示「正在解读…/正在总结…」占位，出完一条再出下一条，边分析边出结论。
  - 后端新增 `/api/breakfast` stepKey='quickturn'（turnKey: host_open/guestN/host_close/summary + prevTurns 上下文），prompts 新增 buildQuickTurnPrompt（单轮生成、承接前文）；旧的一次性 quick 分支保留作兼容但前端不再使用。
  - 前端：runAnalysis 快速路径改为 turnSeq 循环逐条 fetch + setQuickStep 增量更新；移除 revealCount/quickLoadIdx 假揭示逻辑；快速模式隐藏底部 loading 卡（卡内 pending 行代替）。
  - 已验证：5 连调全过（抛题47字/林奇73/索罗斯60/收束62/总结300+，总结把三人观点都收进来）；页面 200，bundle 含 quickturn/setQuickStep。
  - 代价：5 次调用串行，总时长约 12s（单次 2-4.6s），token 量与一次性相当（每轮 max_tokens 600）。
- 2026-08-15（新闻输入框 → 左侧强入口 + 弹窗输入）：
  - 右侧输入卡（textarea+模式开关+开始按钮）整体移除；左侧新闻块顶部新增金色「＋ 输入新闻源」强入口（副标题「粘贴链接或文本，开启解读」，生成中禁用）。
  - 点击强入口弹出新闻输入弹窗（遮罩 + 卡片：标题「输入新闻源」/ textarea / 快速·深度开关 / 取消·开始解读，开始解读在无有效内容时禁用）；提交即 setInputText + 关弹窗 + 直接 runAnalysis（若当前有进行中任务先 abort）。
  - 点今日快讯任一条也改为弹窗预填（title+summary），可改模式后开始。
  - 圆桌头部 actions 新增主按钮（bk-start-mini）：无内容时「＋ 输入新闻」（开弹窗）/ 生成中「■ 停止」/ 完成后「↻ 再来一轮」/ 出错「↻ 重试」。
  - 已验证：页面 200，bundle 含 bk-news-entry/bk-modal-overlay/openNewsModal，bk-input-card 已从 JSX 移除。
- 2026-08-15（「输入新闻源」入口样式对齐大师PK「邀请一位大师」）：
  - 入口按钮由「圆角大卡(图标+标题+副标题)」改为大师PK 同款：通栏居中单行「✦ 输入新闻源」，透明底、金色文字、1px 金边、圆角 6px、加粗 13px、hover 金色浅底，margin 2px 0 10px。位置仍在左侧新闻块顶部（今日快讯列表上方）。
  - 已验证：页面 200，bundle 含新入口文案，旧 title/hint 类已移除，CSS 已编译。
- 2026-08-15（「输入新闻源」位置调整）：入口按钮从左侧列顶部移到「📰 今日快讯」标题下方、第一条新闻上方（今日快讯列表的头部内），样式不变。
- 2026-08-15（输入新闻源图标 + 默认模式）：「✦ 输入新闻源」图标改为「✎」（铅笔，表示输入/撰写）；新闻解读默认模式由深度改为快速（useState('quick')）。
- 2026-08-15（AI 连接偶发失败：重试加固）：
  - 用户遇到「连接 AI 服务失败（网络异常）」；复测四个方向（quickturn 单轮/总结/深度 gate）均正常，判定为 DeepSeek 连接偶发抖动。
  - callDeepSeek 重试策略由「1 次无间隔」改为「最多 3 次、间隔递增（600ms/1200ms）」，降低偶发网络/排队导致的失败。已验证改后接口正常。
- 2026-08-15（删除底部免责声明）：移除早餐圆桌底部「内容由 AI 基于公开数据生成，仅供学习参考，不构成投资建议。」及 .bk-disclaimer 样式（两处），源码/产物均无残留。
- 2026-08-15（网页结构：左侧树形导航 + 右主区）：
  - 页面结构从「顶部 Tab」改为「左栏侧边栏导航 + 右栏主视区」：新增 .app-shell（224px 导航列 + 1fr 主区），顶部 SceneNav 移除（导航进侧边栏），header 只留标题+操作按钮。
  - 新增 SidebarNav 组件（树形嵌套菜单，可折叠）：👥 大师互动→大师PK（ask）/ 📰 巴菲特的早餐→今日快讯（breakfast，带「新闻解读」角标）/ 🧭 能力专项→基本面研究·技术面研究（disabled + 敬请期待，占位未来扩展）。选中叶子金色高亮；移动端树摊平成横向可滚动。
  - i18n 新增 sidebar* 键（zh/en）。右主区内 main-layout/bk-layout 撑满（不再整页 1300 居中）。
  - 已验证：两 Tab 页面 200，SSR 渲染侧边栏树、选中态正确（ask=大师PK、breakfast=今日快讯），scene-nav 已移除，bundle/css 含新结构。
- 2026-08-15（修正树形导航层级）：用户纠正——选【大师PK】才出现子项「选择成员」；选【巴菲特的早餐】才出现子项「新闻列表」。去掉多余的「大师互动」外层分组和「今日快讯」独立层级；新结构：大师PK→(选中后展开)选择成员 / 巴菲特的早餐→(选中后展开)今日快讯 / 能力专项→基本面·技术面(置灰待上线)。选中模式的分组头金色高亮。已验证 SSR 两 Tab 正确。
- 2026-08-15（树形菜单放弃，改纵向 Tab 导航）：左侧导航简化为「纵向 Tab 切换」——⚔️ 大师PK（默认选中）/ 📰 巴菲特的早餐 / 🧭 能力专项（置灰·敬请期待占位）。删除全部 sb-* 树形样式与组件状态，改用 vt-nav/vt-item；移动端摊平成横向可滚动。已验证 SSR：大师PK 默认 active。
- 2026-08-15（左侧导航收窄）：.app-shell 左列 224px → 168px。
- 2026-08-15（左侧导航极简化）：去掉图标，纯文字纵向 Tab（大师PK 默认 / 巴菲特的早餐 / 能力专项弱化置灰）；选中态改为浅金底+金色字（不再深色填充）；宽度 168px → 120px。
- 2026-08-15（大标题/slogan 移入左侧导航栏顶部）：右头部只留操作按钮（右对齐、变矮）；左栏顶部新增品牌块 .sb-brand（大师吵股 + slogan，下边框分隔，移动端只显示标题）。
- 2026-08-15（右上角按钮移到底部左下角）：移除右区 header，换肤/历史按钮移到左栏底部一行（sb-foot-row），加微信改为单独一行「联系我」按钮（sb-contact，点击弹二维码，弹层向上弹出）；侧栏改 flex 列布局把底部推到最下。
- 2026-08-15（新闻列宽对齐大师列表）：早餐 .bk-layout 左列 300px → 280px，与大师PK .main-layout 左列（280px）一致。
- 2026-08-15（早餐顶栏分离 + 初始座位居中）：「巴菲特带你读新闻」信息行与右侧功能按钮（开始/邀请/换一批）从圆桌卡片中拆出，固定在主区顶部；初始未解读时仅大师座位排在剩余空间垂直居中（.bk-seats-center），解读后座位紧随顶部、下方接解读卡片。
- 2026-08-15（新增两个导航 Tab + 能力页）：
  1. **芒格教你读财报**（tab=munger，组件 MungerFinance + /api/munger）：财报解读（贴链接/文本 → 芒格深入浅出解读，分段加粗+追问）/ 每日知识点（生成 3 条财报投资知识点，像芒格讲课）。
  2. **禅宗说禅看短线**（tab=zen，组件 ZenShortTerm + /api/zen）：输入股票 → resolveSymbols 解析 + 实时行情/大盘 → 「禅宗说禅」大师（缠论禅师，复用 chan_czsc 能力）用缠论做短线分析（结构/关键位/短线倾向/禅式提醒，带"需对照实时K线验证"声明）。
  - 共享 `src/lib/ai.js`（callDeepSeek 3 次重试 + generateJson）；prompts.js 导出 masterProfileLine。导航 5 项：大师PK/巴菲特的早餐/芒格教你读财报/禅宗说禅看短线/能力专项(置灰)。
  - 已验证：两页 200；munger knowledge（3 条）、munger report（697 字芒格式解读+3 追问）、zen（宁德时代缠论分析）均 200。
- 2026-08-15（财报支持上传附件）：芒格页新增「📎 上传附件」——txt/csv/md/json 直接读进输入框；PDF 在**浏览器端用 pdfjs-dist** 抽文本（客户端 worker 正常）再走文本解读；>8MB 拦截。服务端 /api/munger 附件分支只做文本类解码（PDF 不再走后端解析）。已安装 pdfjs-dist（客户端打包正常），移除 pdf-parse。已验证页面 200、txt 附件解读正常。
- 2026-08-15（PDF 附件解析改为独立脚本方案）：
  - 浏览器端 pdfjs 抽文本（new URL worker）在用户机器上失败；`?url` 静态导入又导致构建 500。最终方案：**服务端收到 PDF → 写临时文件 → spawn 独立 Node 脚本 scripts/extract-pdf.mjs（pdfjs-dist legacy，纯 Node 运行，worker 正常）→ 取回文本 → 芒格解读**。绕开 webpack 打包的 worker 问题，无需重启/改配置。
  - 已验证：PDF 上传 200（702 字解读）、txt 上传 200、三个页面均 200。
- 2026-08-15（芒格财报页·举手提问）：解读结果里的「想深挖？」改为可点击追问 + 底部新增「举手提问芒格」输入框；/api/munger 新增 mode='followup'（携带财报要点+芒格前文解读，200-400字回答，可加粗），前端追加问答轮次（loading/失败重试）。已修复 mode 归一化漏掉 followup 的问题。已验证追问接口 200（坏账问题 412 字回答）、页面 200。
- 2026-08-15（禅宗短线没数据：行情源不稳定，加腾讯兜底）：push2.eastmoney.com 单股行情接口在本机网络时好时坏（返回空），导致禅宗分析"数据缺失"。新增腾讯 qt.gtimg.cn 兜底（股票行情 + 上证/深成/创业板指数，国内稳定），push2 失败自动切换。已验证：宁德时代分析引用实时价 393.93、支撑 385-390、压力 400/405。
- 2026-08-15（芒格页输入改为两种方式+补充说明）：财报解读改为「财报链接（支持 .pdf 链接自动解析）+ 📎 附件上传 + 补充说明（非必填）」；路由支持 link 抓取（HTML 抽正文 / .pdf 走脚本）、file 解析、note 合并进报告。修复 useCallback 依赖残留 text 导致的 500。已验证：链接+note、附件+note、页面 200。
- 2026-08-15（二维码被遮挡修复）：二维码弹层从可滚动的左侧栏内部挪到 page-root 顶层（app-shell 之后、其他浮层之前），CSS 改为顶层 fixed（bottom:64px/left:24px/z-index:2100），脱离侧栏滚动与堆叠环境，不再被遮挡。修复过程踩坑：移动时多删了一个 </div> 导致 500，已补回。
- 2026-08-15（新闻源过滤加强）：① 财经信号改为**只看标题**（正文偶发财经词不再误留）；② 补充非财经关键词（监察调查/纪律审查/空袭/真主党/以军/研学/交流活动/两岸/班列/通车/座谈会等），避开价格战/陆家嘴论坛/进博会/冲突等财经词避免误伤。实测 20→15 条，研学论坛/空袭/纪检/班列类已清除。
- 2026-08-15（早餐邀请大师挤一起修复）：空闲时钟 CLOCK_POS 从 4 个位置扩到 6 个（12/3/6/9 + 1:30 + 7:30），加人到 5-6 位不再重叠；位置下标改取模兜底。排查"邀请大师报错"：邀请逻辑/头像文件/渲染均无崩溃点（头像文件齐全），已修可见的拥挤问题，若报错仍在需用户提供具体错误文案。
- 2026-08-15（早餐页去掉邀请大师，固定 9 位）：默认/换一批都随机选 8 位嘉宾（+巴菲特=9 位，块状网格 3x3）；移除「＋ 邀请大师」按钮与 inviteGuest 函数。
- 2026-08-15（9 位大师修正）：有真人头像的流派只有 7 个（A股游资/短线是字母占位被过滤），pickGuestsByStyle(8) 原本最多出 7 位嘉宾。改为「流派不够时从已用流派再补」，凑满 8 位嘉宾（+巴菲特=9 位，块状 3x3）。
- 2026-08-15（早餐页删掉「🎲 换一批」）：移除换一批按钮与 rerollGuests 函数；大师固定随机 9 位（进入即随机）。
- 2026-08-15（同排头像文字不对齐修复）：巴菲特头像比嘉宾大导致下方两行字（姓名/流派）被顶低。同排头像统一大小——解读后一行全部 36px、初始 3x3 块状全部 60px，主持人仍靠金环/金名区分。
- 2026-08-15（新增预置大师「寒武纪的鳄鱼」）：从用户 Flomo 导出（104 条 #寒武纪鳄鱼 笔记、4.6万字）提炼画像，作为平台预置大师加入 BASE_MASTERS（巴菲特/芒格之后），id=crocodile，称号「生产资料猎手」，tag=中国价值，头像 public/avatars/crocodile.svg（深绿+金环+🐊）。profile 的 knowledge 字段浓缩语料：生产资料论/选股八条/道天地将法/中国市场观/操作。全量语料保存在 /tmp/寒武纪鳄鱼-notes.md。踩坑：插入时多拼了个逗号导致页面 500，已修。
- 2026-08-15（芒格页删掉每日知识点模块）：组件移除知识点 tab/生成按钮/结果渲染，路由移除 knowledge 分支；页面只留财报解读（链接/附件/补充说明 + 举手提问）。修了 JSX 残留导致的 500。
- 2026-08-15（新增「大师的选股池」能力项）：
  - 左侧导航新增 📊 大师的选股池（tab=pools），页面左侧池子列表（localStorage 持久化，手动粘贴6位代码新建）+ 右侧详情。
  - 详情：当日涨跌表格（红涨绿跌）+ 区间统计卡片（近30/60/120日）：等权区间涨幅 vs 沪深300、跑赢天数占比、今日涨跌只数、平均上涨天数占比；带「按当前持仓回溯、未考虑调仓」提示。
  - API /api/pools：resolveSymbols 解析 + 腾讯日K（股票 qfq + sh000300），逐日对齐算等权收益与跑赢天数。
  - 已验证：页面 200、3股池接口返回完整统计（等权+2.69% vs 沪深300 -2.45%、跑赢33/60天）；全网抓取大师持仓留作下一步。
- 2026-08-15（选股池升级：预置大师池 + AI 生成）：
  - 图标 📊→🎯；新增 src/data/masterPools.js 预置 10 位大师的 A 股池（巴菲特/芒格/张磊/林园/但斌/冯柳/邓晓峰/张坤/葛卫东/裘国根，按公开资料整理+提示），左侧列表可切换，预置池带「预置」徽章不可删，用户池可删。
  - 新增「🤖 AI 生成」：输入大师关键词 → /api/pools/suggest（DeepSeek）检索/推断 A 股候选池（最多10只+理由+来源标注公开/推测）→ 一键导入。实测「寒武纪的鳄鱼」→ 军工/稀土/垄断央企池（中航沈飞/航发动力/紫金矿业等），source 标「按其风格推测」。
  - 手动粘贴新建保留。已修 zh i18n tabsPools 漏加导致的导航显示 key。
- 2026-08-15（选股池细节优化）：① 隐藏「预置」徽章；② 日期筛选默认 30/60/120天，点「更多」展开 半年/一年/两年/三年（API 天数上限提到 800）；③ 表格全部左对齐；④ 新增「持仓价」可编辑列（手动填入，存 localStorage thinktank_costs，未填显示 —）+「持仓盈亏」列。
  - 长区间数据：东财前复权日K（push2his，快且调权）优先、腾讯兜底（长区间用不复权），返回后按天数截取最近 N 条。已验证 750 天窗口 2023-07→2026-08 正常、统计合理。
- 2026-08-15（选股池两功能重构：手动新建/AI生成 → 导入/搜寻）：
  - 左侧两个按钮取代旧的「手动新建/AI 生成」：📥 导入股票池（Tab 切换「我的股票池/大师的股票池」，大师类型名称栏提示"大师名"，创建自动加「· 选股池」后缀，source 标「手动整理」）+ 🔍 搜寻大师股票池（输入大师关键词 → POST /api/pools/suggest AI 检索/推断 A 股池 → 一键导入）。
  - 旧状态 submitManual/creating/suggestOpen 全部移除，统一 submitImport + runSuggest/importSuggest。
  - 修复 importSuggest 残留的 setSuggestOpen(false) 调用（旧状态已删，点击「导入此池」会抛 setSuggestOpen is not a function）→ 改为 setSearchOpen(false)；同步更新右侧空态占位文案（不再提"新建/AI 生成"）。
  - 已验证：页面 200；/api/pools/suggest（寒武纪的鳄鱼 → 航发动力/北方稀土/高争民爆等 10 只，source 按其风格推测）200；/api/pools 750 天（三年）区间正常（等权 +54.8% vs 沪深300 +21.4%、跑赢 362/750 天）；bundle 无 setSuggestOpen 残留。
- 2026-08-15（选股池导入/搜寻改为右侧抽屉）：左侧两个入口按钮「📥 导入股票池」「🔍 搜寻大师股票池」由并排改上下两行；圆角 999px 胶囊 → 6px（与「邀请一位大师」invite-entry 一致，flex 居中）。点击后在右侧滑出全高抽屉（复用 invite-drawer 结构：遮罩+标题+关闭+可滚动 body，移动端 100vw），抽屉内分别承载导入表单（我的/大师 Tab）与 AI 搜寻表单（关键词→候选池→一键导入）；主区 error 在抽屉打开时改在抽屉内展示。踩坑：node --check 不校验 JSX 标签配对（Node 的 JSX 转换只剥离不校验），锚点替换时漏掉一个 </div>，靠 div 开闭计数发现并补回，SWC 编译通过（页面 200）。已验证 bundle 含 drawer 标记、CSS 含 .sp-side-actions column / .sp-new radius 6px / .sp-form-drawer。
- 2026-08-15（选股池四项细节调整）：① 两个入口按钮的 emoji 图标换成简约线性 SVG（导入=下载进托盘、搜寻=放大镜），stroke=currentColor 随按钮金色；搜寻按钮改浅金底+金边框（.sp-new-ai 由灰字改金色系），两个按钮都呈金色。② 删除顶部简介文案与右侧空态占位文案。③ 进入页面默认选中第一个预置大师池（activeId 初始值 = PRESET_POOLS[0].id，巴菲特·伯克希尔），自动带出行情统计。④ 搜寻无结果时展示空态「未检索到 XX 的选股池」+「去手动导入」按钮（goImportMaster：切到导入抽屉、类型=大师、预填搜索关键词为大师名）。
- 2026-08-15（选股池导入升级：文本/链接提取 + loading 居中）：
  - 新增 /api/pools/extract：POST {text} 或 {url} → AI 提取报道/云文档/网页中的 A 股标的（6位代码+名称，最多30只，去重）；URL 走服务端 fetch（15s 超时 + UA，HTML 剥标签转纯文本）；AI 提取不到时正则兜底扫 6 位 A 股代码（前缀 0/3/4/6/8/9）。实测：纯文本 6/6 全部命中；只写公司名不带代码的报道也正确映射 8/8（宁德时代/隆基/比亚迪/北方稀土/茅台/五粮液/片仔癀/云南白药）。
  - 导入抽屉新增「输入方式」Tab：手动填写（原 6 位代码输入）/ 文本或链接提取（粘贴报道全文或云文档·网页链接 → 「提取股票」→ 提取结果列表可逐条 ✕ 删除 → 「提交到选股池」）；提取为空时给空态引导（换链接/文本或转手动填写）。source 仍可手动填，默认手动导入/手动整理。
  - 「正在拉取行情与统计…」loading 改为右侧区域居中（.sp-loading min-height 320px flex 居中）。
  - 已验证：页面 200；extract 文本/链接两路接口正常；bundle/CSS 均含新标记。
- 2026-08-15（选股池导入抽屉简化：删掉名称/来源输入框）：删除「池子名称」「来源」两个输入框；池子名称改为自动生成（nextPoolName：从搜寻跳转来的大师池保留大师名+「· 选股池」，其余按「我的股票池 N / 大师的股票池 N」递增），来源自动标注（手动=手动导入/手动整理，提取=接口返回的文本提取/链接提取）；submitImport/submitExtracted 去掉名称必填校验。
- 2026-08-15（提取链接报错细化）：/api/pools/extract 链接抓取错误分类——超时（15s）提示「网站响应慢或需登录/验证码」；打开成功但正文过短（<40字符，登录墙/JS渲染）单独提示「需登录或脚本渲染」；均引导复制正文粘贴。
- 2026-08-15（导入区分「我的/大师」：大师类型先选具体大师）：导入抽屉在「大师的股票池」类型下新增「选择大师」下拉（预置 10 位池大师短名 + 寒武纪的鳄鱼 + 自定义…，自定义显示名称输入框）；未选大师提交时报「请先选择大师」；我的股票池无需选择直接建。池名取所选大师名+「· 选股池」（如 巴菲特 · 选股池）。从搜寻跳转（goImportMaster）自动映射：命中预置选项直接选中，否则落到自定义并预填搜索词。masterSelect/customMaster 替代原 form.name 承载大师名。
- 2026-08-15（选中态颜色加深，与功能按钮区分）：新增 --accent-bg-strong 变量（暗色 0.20 / 白 0.13 / 浅色 0.16），选中态统一改为「金色实线边框 + 深金底 + 柔和阴影」：① 大师的选股池 .sp-pool.active（原 --accent-bg 淡底，与「搜寻大师股票池」按钮同色易混淆）；② 巴菲特的早餐新闻行新增 .bk-news-row.active（组件加 activeNewsId 状态，点击新闻行即持久高亮，标题转深金加粗）。hover 仍用淡 --accent-bg。
- 2026-08-15（大师选择器收紧：去掉自定义，仅大师PK名单）：导入大师股票池时「选择大师」下拉去掉「自定义…」选项与自定义名称输入框；选项改为仅来自大师PK现役大师名单 PRESET_MASTERS（72位，含巴菲特/芒格/寒武纪的鳄鱼/张磊/冯柳/邓晓峰/张坤/游资等）。goImportMaster 模糊匹配名单（如搜「巴菲特」→ 选中「沃伦·巴菲特」），名单外关键词不再预填，需用户手动从名单选。另：本机 dev server 曾停止导致 404/连不上，已重启（npm run dev）。
- 2026-08-15（搜寻大师股票池改为「只查证不编造」）：① 后端 /api/pools/suggest 重写——预置池大师（10位，如巴菲特/张磊/林园）确定性命中直接返回公开资料整理的池子（匹配收紧：精确/相近叫法如「沃伦·巴菲特」/标签命中如「高瓴」，杜绝「巴菲特的小舅子」误命中）；非预置走 AI 严格查证（仅返回可查证的公开持仓，禁止按风格推测，非真实投资人物返回空）；② 所有候选代码用腾讯行情逐只校验真实存在（GBK 解码取真实名；批量接口连续请求会截断，改逐只并发请求），滤掉编造代码；③ 前端搜索结果每行加 ✕ 删除按钮，「导入此池」仅导入剩余标的、删空后禁用；区分「搜不到（不编造）」与「已移除全部标的」两种空态。实测：巴菲特→比亚迪、张磊→5只、高瓴→张磊池、巴菲特的小舅子→空、张三丰炒股→空。
- 2026-08-15（手动导入支持股票名称）：之前「手动填写」只认 6 位代码，用户贴股票名称全被过滤。新增 /api/pools/resolve-names（东财 suggest 逐只解析：去 XD/XR/DR 前缀与空格，A股过滤兼容 Classify=AStock 与数字 23，精确匹配优先+唯一包含匹配兜底，并发 10）；前端手动填写改为「解析并预览 → 可逐条删除 → 创建股票池」，解析失败的名称单独提示未纳入。实测用户 63 只名单（含 安 纳 达 / XD中国铝 / ST应急 / 科创板新股）63/63 全部解析成功。
- 2026-08-15（名称解析修复：带空格名称被拆碎）：用户名单里「安 纳 达」因按空格拆分变成「安/纳/达」单字导致未识别。修复：/api/pools/resolve-names 新增 { text } 入参，后端智能拆分——按换行/逗号/顿号/分号分行；一行全为6位代码则拆代码；一行多个≥2字名称则各自成条目；含单字碎片（安 纳 达）则去空格合并成一个名称再解析。前端 parseManual 改为直接发送整段原文。实测：安 纳 达→安纳达、五 粮 液→五粮液、XD中国铝/ST应急/科创板新股正常、同行「齐翔腾达 002408」取代码、完整63只 missing=0。
- 2026-08-15（名称解析强规则归一化）：normalizeName 升级为强规则——去除全部空白变体（半角/全角空格、Tab、NBSP）与不可见字符（零宽 U+200B-200F、BOM、软连字符、U+2060）；括号转空格（齐翔腾达(002408)→提取代码）；剥离新股/除权/特殊前后缀（N/C/XD/XR/DR/S，ST/*ST 保留，-U/-W 后缀）；短名≥2字允许唯一包含匹配。实测 9 种刁钻输入（全角空格/零宽/括号代码/N中巨芯/百济神州-U/茅台/ST应急/XD中国铝/tab 名称+代码）全部识别；完整 63 只名单 missing=0。兜底：识别不了的始终进「未识别」名单展示，绝不静默丢弃。
- 2026-08-15（选股池细节两处）：① 池子列表排序改为用户导入/新建的池子排最前（[...userPools, ...PRESET_POOLS]）；② 「正在拉取行情与统计」加载态升级——放大镜 SVG 左右摆动动画（sp-scan 1.5s）+ 文字 15px 加粗金色 + 三个闪烁圆点（sp-blink），整体更醒目。
- 2026-08-15（选股池左侧列表全部可删除）：预置大师池也支持删除（隐藏），localStorage thinktank_hidden_presets 持久化；pools = [...userPools, ...PRESET_POOLS.filter(未隐藏)]；删除当前选中项后自动落到列表第一项；删除按钮对所有池子显示。
- 2026-08-15（修复：刷新后导入的池子丢失）：根因是挂载时 saveUserPools 在读取 localStorage 之前就用初始空数组覆盖写入（useEffect 竞态），某些刷新时机下把已保存数据冲掉。修复：新增 hydrated 标记，先一次加载 userPools/hiddenPresetIds/costs 并置 hydrated=true，加载完成前禁止写 localStorage（save 效果仅在 hydrated 后执行）。
- 2026-08-15（选股池短周期统计：默认今天/昨天/本周）：右上角日期选择器新增「今天/昨天/本周」置顶（默认选中今天，收起时仅显示这三项，更多展开 30/60/120天/半年/一年/两年/三年）。/api/pools 新增 short 统计（barsPeriods 按日K计算每只/指数今天、昨天、本周收益，本周=最近周一至今，等权+涨跌只数+日期）；今天非交易日时自动取最近交易日，前端卡片标签显示「最近交易日 · MM-DD」。默认视图展示 今天/昨天/本周 三张卡（选中项高亮），区间选择仍显示原区间统计。实测 600519/300750/000001 三股短周期数据正常（今日 08-14 收盘、昨日 08-13、本周）。
- 2026-08-15（日期「更多」改为下拉枚举）：去掉展开成一行日期按钮的方式；「更多 ▾」点击后从右侧弹出下拉菜单（.sp-days-menu，含 30/60/120天/半年/一年/两年/三年），选中即收起；选中区间后「更多」按钮显示当前值（如「60天 ▾」金色）；点外部遮罩关闭。今天/昨天/本周仍为常驻按钮。
- 2026-08-16（修复：导入纯代码查不出名称）：根因① resolve-names 接口对纯 6 位代码直接返回 name='' 不查名；② marketData.resolveSymbols 对纯代码只推导 secid 不带名，且 LLM/词典路径可能把名称设成代码本身（002408→"002408"）。修复：新增导出 fetchACodeName（腾讯行情 qt.gtimg.cn GBK 解码取官方名）；resolve-names 纯代码走它补名；resolveSymbols 返回前统一兜底——CN 标的名称缺失或等于代码本身时用行情补真实名。实测 8 只纯代码（齐翔腾达/贵州茅台/宁德时代/振华新材/中国铝业/五粮液/平安银行/比亚迪）全部带出名称。
- 2026-08-16（选股池拉取提速）：根因① resolveSymbols 对每只纯 6 位代码也调 DeepSeek 做公司名提取（首载每池 ~5s，缓存1h）→ 纯代码直接跳过 AI 与公司名启发式提取，走代码解析+行情补名；② 东财 K 线接口（push2his）在本机网络时好时坏，单只最多可干等 15s → fetchEmKline 超时收紧 3.5s，fetchKline 改「东财优先 + 1.2s 未响应即并行发腾讯兜底」（正常仅 1 请求，东财挂时 ~1.5s 内拿到腾讯数据）。实测 18 只：修复前 ~5s+，修复后 0.66s、名称价格全完整。注：连续高频压测会触发行情源限流导致偶发缺价，正常使用无碍。
- 2026-08-16（待办：UI/设计优化参考 refero 模板库）：浏览 refero.design 模板库后，已给「大师吵股」沉淀一套可落地的设计优化建议，**暂不动手**，后续用户再次提及 UI/设计优化时提醒其参考：
  - 重点参考 Origin Financial（金融 dark：近黑画布 + 衬线 300 细字重 96px 巨型标题、等宽大写数据标签、白底黑字 8px 主按钮、无阴影扁平色块卡）。
  - 其次 Peak Design（衬线斜体标题 + 压缩大写标签的编辑对比、整页黑白交替、单一强调色）与 Hyer Aviation（超大字重极紧字距、全胶囊按钮 + 直角卡、无阴影）。
  - 通用纪律：圆角要么 0-8px 要么全胶囊，不搞中间值；大标题用细字重轻声权威；靠 1px hairline 边框分层、不靠阴影；保留暖米白 + 暗金基底，暗金只用于关键标题/主 CTA。
  - 落地页面待定（大师的选股池 / 巴菲特的早餐 / 大师PK 三选一），动手前先与用户确认。
- 2026-08-16（选股池多轮修复 + 新浪行情兜底 + 区间口径统一 + 整体检查）：
  - 默认选中修复：刷新后默认选中列表第一项（activeId 初始 null，hydration 完成后再落 pools[0]），不再固定落在预置池第二条（巴菲特）。
  - 短周期默认与选中态：非交易日（周末）默认选中「本周」，「今天」卡不再默认高亮；今天/昨天/本周三个数据框去掉选中态（仅保留顶部日期按钮选中态）。
  - 「本周」日期标注改为区间：「周一 ~ 最后交易日」（如 08-10 ~ 08-14），不再只标单日。
  - 行情兜底：新增新浪日K（fetchSinaKline）作第三兜底（东财→腾讯→新浪），解决东财空/腾讯 WAF 风控导致的股价全空；新浪 JSONP 用 indexOf/lastIndexOf 截取括号解析（正则 /\(\[(.*)\]\)/s 对该响应不稳定）。
  - 区间口径统一：接口新增 period 参数 + sliceWindow 按选中周期截取窗口；表格「区间涨幅/上涨天数」分母 = 选中区间交易日数（本周=5、今天/昨天=1、30/60/120/250/500/750 精确对齐）；「本周」涨幅口径改为「上周五收盘→本周最后交易日」（与卡片一致）；跑赢大盘天数分母对齐；区间视图「今日上涨/下跌」→「区间内上涨/下跌」（按区间涨幅统计 upInRange/downInRange）；indexRet 按选中窗口计算。
  - 整体检查：npm run build 通过；无硬编码密钥；首页/早餐/大师PK/财报/禅/选股池 5 Tab 冒烟测试无 JS 报错；resolve-names/suggest/长区间（750天）接口正常；修复 data-theme hydration 警告（html 加 suppressHydrationWarning）。
- 2026-08-16（预置池：新增寒武纪的鳄鱼 + 巴菲特池扩充为真实持仓 + 美股行情支持）：
  - 新增预置池「寒武纪的鳄鱼 · 生产资料池」（65 只）：来自用户 flomo 笔记（标签#寒武纪鳄鱼，含理念+具体标的：中钨高新/东方钽业/金钼股份/锡业股份/株冶集团/国泰集团/凯龙股份/雪峰科技/高争民爆/巨化股份等）+ 用户手动整理的 63 只名单（央企资源/民爆/军工/化工），OCR 确认笔记截图无持仓清单。
  - 巴菲特池扩充：由仅比亚迪 1 只 → 13 只，按伯克希尔 2026-06-30 13F（8/14 披露）：苹果/美国运通/谷歌/可口可乐/美国银行/雪佛龙/西方石油/安达保险/穆迪/卡夫亨氏/达美航空/莱纳 + 比亚迪(A股映射)。
  - 美股行情支持：选股池 K 线链美股仅走东财（105/106 secid 原生支持）；腾讯美股 K 线接口只回首尾 2 条不可用、新浪美股 Service not valid、Yahoo 被墙 → 均防护返回 null，避免误用残缺数据；A 股仍东财→腾讯→新浪三级兜底。注意：东财限流时美股显示「无行情数据」，恢复后自动补上。
  - resolveSymbols 优化：纯美股代码（AAPL 等）跳过 LLM 公司名提取，直接东财搜索解析（首载提速）。
  - 修复 bug：①选股池接口股票上限 50 → 100（65 只池子只处理前 50）；②loadDetail 竞态——大池子加载慢时切池，旧响应覆盖新池子（加 loadSeq 请求序号守卫）；③resolveName/resolveTicker 东财搜索偶发返回空被当成成功缓存 24h 毒化（CB/MCO 消失）→ 空结果改按错误缓存（短 TTL 1min）。
  - 验证：寒武纪 65 只全有行情、巴菲特 13 只解析正确（美股价格待东财恢复）；build 通过；无 JS 报错。
- 2026-08-16（工作约定）：用户要求——代码改完**不要急着推送到 GitHub**，等用户明确通知「推送」再执行 `git push`；本地可保留 commit 供 review/回滚。
- 2026-08-16（持仓价默认预填）：持仓价列——优先取用户已保存的成本价；未保存时若有行情（现价可从网上查到）则默认用现价填入、可手动修改；无行情（如美股限流期间）则留空由用户手动填。持仓盈亏按「已保存成本 ?? 现价」计算（未保存时默认 0%）。
- 2026-08-16（持仓价修正 + 布局修复）：
  - 纠正：持仓价默认值不是现价，而是「预置池已知的持仓价」（公开报道的估算成本）。巴菲特池填入 4 只有公开成本价的重仓股：AAPL≈35（2021股东信披露）、KO≈3.25、AXP≈8.49、MCO≈10.05（均为美元，13F 不披露成本，属公开报道估算）；其余查不到的（美银/雪佛龙/西方石油/谷歌/达美/莱纳/比亚迪等）留空，用户手动填。优先级：用户已保存成本 > 池子已知持仓价 > 空。
  - 布局修复：巴菲特池日期组件被挤到第二行——.sp-detail-head flex-wrap:wrap 改 nowrap，左块 flex:1+min-width:0，日期组件 flex-shrink:0，保持右上角同行。
- 2026-08-16（选股池左侧列表滚动修复）：左侧大师/池子列表原先仅 sticky、无独立滚动容器，内容超一屏时滚轮会带动整个页面（右侧表格跟着滚）。修复：.sp-side 加 max-height: calc(100vh-40px) + overflow-y:auto + overscroll-behavior:contain，左侧独立滚动、页面不被带动。已验证。
- 2026-08-16（预置池精简为 5 个 + 新增段永平池）：
  - 按用户要求，默认预置池只保留 5 个：巴菲特·伯克希尔、寒武纪的鳄鱼·生产资料池、林园、但斌·东方港湾、段永平·H&H（新增）；移除芒格/张磊/冯柳/邓晓峰/张坤/葛卫东/裘国根。
  - 段永平池 8 只：AAPL、伯克希尔(BRK-B)、PDD、TSLA、GOOG、BABA、00700(腾讯HK)、600519(茅台)，依据 H&H 2026Q2 13F + 访谈自述核心（苹果/腾讯/茅台）。
  - 修复解析稳定性：resolveSymbols 整串命中词典（如「伯克希尔」）时跳过 LLM、以词典为准——否则 LLM/东财搜索会把伯克希尔解析成港股 07777 或不稳定（BRK.B/BRK-B 直接搜索会误匹配壁仞科技 06082）。
- 2026-08-16（预置池名称精简）：去掉大师名旁的小标题（如「· 伯克希尔」「· 东方港湾」「· H&H」「· 生产资料池」），列表/详情标题只显示大师名：巴菲特/寒武纪的鳄鱼/林园/但斌/段永平。
- 2026-08-16（新增 Dang大 / MR Dang 大师 + 选股池）：
  - 主大师库（masters.js BASE_MASTERS）新增「Dang大」(id: dang, tag 周期)：复用 presetMasters.js 里已有的 Dang大 画像（知乎金融大V、周期有色猎手、CPA、重安全边际）。大师总数 72→73。
  - 选股池新增「Dang大」预置池 21 只：紫金矿业/盐湖股份/云天化/中国宏桥(HK 01378)/新研股份/宏桥控股(002379)/宝丰能源/中金公司(601995)/南山铝业/大参林/九华旅游/国盾量子/同力股份(北交所 920599)/中核科技/东兴证券/佰维存储/渤海租赁/桐昆股份/兴业银锡/中国神华/华夏银行。「银发银行」无法识别，待用户确认后补充。
  - 北交所支持：resolveSymbols 纯代码识别加 4/8/9 开头；fetchACodeName 名称查询加 bj 前缀；pools tencentCode 加 bj 前缀（东财 secid 0.920599 原生支持）。
  - 注意：大师列表英文界面显示 nameEn（MR Dang），中文界面显示 Dang大；tag 必须用 masterGroups.js 里的合法 key（周期），否则被归入「其他」。
- 2026-08-16（大师PK 大师列表搜索）：在流派筛选与大师列表之间新增紧凑搜索框（放大镜图标 + 一键清空），按 名称/英文名/称号/风格/流派 实时过滤；i18n 增加 searchMasters 文案；CSS .master-search 圆角胶囊样式与整体一致。已验证：搜 Dang→MR Dang、搜 周期→周期流派、清空恢复 73 位。
- 2026-08-16（搜索框位置与样式调整）：大师PK 搜索框从流派筛选下方移到「邀请一位大师」按钮正下方；样式从白底胶囊改为与 invite-entry 同款（透明底 + accent 金边 + 6px 圆角 + 金色图标/占位符，hover/focus 金色浅底）。
- 2026-08-16（大师PK 侧栏滚动穿透修复）：搜索后大师列表变短，侧栏不再可内部滚动，滚轮会直接滚到页面导致右侧讨论区跟着动。修复：.sidebar 加 overscroll-behavior: contain，阻止滚动链式传导到页面（长/短列表均验证：页面 scrollY 保持 0）。
- 2026-08-16（选股池默认日期改回今天）：之前为避免「最近交易日」卡片默认高亮，周末默认选中本周；后来卡片高亮已全部去掉，故默认恢复为「今天」（非交易日数据自动取最近交易日，如 今天·08-14）。已验证默认按钮高亮为「今天」。
- 2026-08-16（选股池三处数据显示调整）：
  1. 中钢国际(000928)现价：接口实测可取到（4.97），此前为行情源偶发限流的瞬时失败，刷新即可，非系统性 bug。
  2. 卡片统计布局：今天/昨天/本周卡片的「X 涨 / Y 跌」从数值下方子行移到与数值同一行、右对齐（.sp-stat-val-row + .sp-stat-count）。
  3. 对比指数：沪深300 → 上证指数（接口 1.000300 → 1.000001，文案「vs 沪深300」→「vs 上证指数」）。
- 2026-08-16（选股池背景）：切换到「大师的选股池」(tab=pools) 时背景图使用 /bg-debate.png，其余设置与大师PK/财报等默认一致（滤镜/遮罩/透明度不变）。已验证各 Tab：PK→bg-argue、早餐→bg-breakfast、财报→bg-munger、禅→bg-argue、选股池→bg-debate。
- 2026-08-16（反扒 + SEO）：
  - 反扒①API 限流：新增 src/lib/rateLimit.js（进程内 IP+路由 滑动窗口），13 个 API 路由接入（chat20/breakfast10/munger10/zen10/virtual-master15/context30/explain20/match-masters30/news30/pools30/extract15/resolve-names20/suggest10，单位 次/分钟）；超限返回 429 + Retry-After。注意：Vercel 无服务器多实例，进程内计数仅基线防护，生产建议叠加 Cloudflare/Upstash。
  - 反扒②next.config 显式 productionBrowserSourceMaps:false。
  - 反扒③防提示词泄漏：新增 src/lib/security.js 的 SYSTEM_GUARD，在 chat/breakfast/munger/zen 的消息入口注入 system 保密指令。
  - 反扒结论：核心「事件穿透框架」(src/lib/framework.js) 与大师PK提示词 (src/lib/prompts.js) 目前在客户端 bundle（客户端驱动多步调用），可被扒；真正的根治是把编排/提示词移到服务端（建议后续做）。
  - SEO：layout.js 元数据（title/description/keywords/OG/Twitter）纳入 5 项能力；新增 JSON-LD（WebSite + WebApplication + ItemList 能力列表）；sitemap 加入 /breakfast（priority 0.8）。已验证 sitemap.xml、JSON-LD 渲染、限流 429。
- 2026-08-16（自建服务器/百度SEO 准备）：用户想拥有自己的服务器并在百度站长平台提交 sitemap（外部账户操作需用户本人，我备好一切本地可做项）：
  - next.config 开启 output:'standalone'（Docker 部署用，Vercel 兼容；已验证 .next/standalone 生成）
  - 新增 Dockerfile / .dockerignore / docker-compose.yml / deploy/nginx.conf.example / scripts/baidu-push.sh（百度主动推送，BAIDU_SITE_URL + BAIDU_PUSH_TOKEN 环境变量）/ DEPLOY.md（服务器购买建议[香港/海外免备案+Cloudflare]、Docker 部署、Nginx+HTTPS、百度 sitemap 提交与主动推送、Google Search Console）。
  - 待用户提供：百度 HTML 验证 meta 的 content 值（替换 layout.js 里的旧占位 codeva 值）。
- 2026-08-16（推送部署 yieldglide.com）：用户确认后推送 14 个本地提交（79a530f..744e82c），Vercel 自动部署成功。已线上验证：sitemap 含 https://yieldglide.com 与 /breakfast；新元数据标题上线；JSON-LD 为 WebSite+WebApplication+ItemList。百度后台用户可提交 sitemap 并做 API 主动推送。
- 2026-08-16（百度收录实操）：通过浏览器扩展认领用户 Chrome 中的百度站长后台标签页（站点 https://www.yieldglide.com，带 www），并完成：
  - API 主动推送：提交 https://www.yieldglide.com/ 与 /breakfast，返回 success:2（百度规则：有跳转提交跳转后链接；www 301 到不带 www 主域）。
  - sitemap 提交被卡：该站点无 ICP 备案号 → 今日 sitemap 配额 0，输入框禁用，无法提交；备案号可提高配额。
  - 结论：无备案时靠 API 推送即可收录；日后有备案号再补 sitemap。另提示：百度注册的是 www 站点，主域实际为不带 www 的 yieldglide.com（www 301），可考虑在百度再添加验证不带 www 的站点。
  - 备注：浏览器操控用的是 codex browser-client（chrome extension, user.openTabs/claimTab + playwright API）。
- 2026-08-16（百度添加不带www站点：进度）：
  - 已注册 https://yieldglide.com（协议 https、领域金融），验证文件 baidu_verify_codeva-yLkIid7GFu.html 内容已改为百度官方 32 位哈希（83f37ae8a39afcfac38c84ca152bc63e，与下载原版逐字节一致），已部署上线、HTTP 200、百度爬虫 UA 可访问。
  - 状态：站点仍显示「未验证」；自动化多次点「完成验证」未确认（百度反自动化滑块验证 + 页面 ERR_EMPTY_RESPONSE 连接波动）。待用户在 Chrome 手动完成验证；若仍失败，备用方案=改用 HTML标签验证（百度给 meta code，加到 layout.js 再部署）。
  - 经验：百度「文件验证」的文件内容是 32 位哈希（不是 meta 标签格式），务必下载官方文件逐字节替换。
