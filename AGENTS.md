# 项目协作规则

## 每次功能迭代或 Bug 修复必须同步文档

任何会影响用户行为、界面、数据流、接口、错误处理或业务规则的改动，都必须同时完成以下事项：

- [ ] 更新 `memory/log.md`，记录改了什么、为什么改、影响范围与验证结果。
- [ ] 更新相关 `memory/` 文档，例如 `project.md`、`architecture.md`、`PRD.md`。
- [ ] 更新 `memory/QA.md`：
  - 功能细节发生变化时，同步修改对应模块说明。
  - 增加、删除或调整测试用例时，同步更新 P0/P1 回归清单。
  - 分享、免费额度、持久化、接口、错误降级等全局规则变化时，必须更新对应章节。
- [ ] 有新增自动化测试时，将命令和覆盖范围写入 `memory/QA.md`。
- [ ] 完成 P0 快速回归；没有实际执行或验证的项目不得勾选。
- [ ] 页面文案、SEO、README 或部署方式变化时，同步更新相关文件。

如上述文档未同步更新，不得将任务标记为完成，也不得推送线上。
